<section class="pane">
    <h2>Danger Zone</h2>
    <p class="error">These actions can remove migration state, logs, artifacts, or reviewed mapping decisions. They require an elevated session and typed confirmation.</p>
    <p>No active reset or cleanup submit forms are available in Phase 12.</p>

    <section class="km-console-warning-panel">
        <h3>Reset migration state</h3>
        <p><strong>RESET MIGRATION STATE</strong></p>
        <p>Reset/truncate controls are disabled and deferred until deletion boundaries are specified.</p>
        <button type="button" class="btn error" disabled>Reset migration state deferred</button>
    </section>

    <section class="km-console-warning-panel">
        <h3>Cleanup artifacts</h3>
        <p><strong>DELETE ARTIFACTS</strong></p>
        <p>Artifact cleanup controls are disabled and deferred until deletion boundaries are specified.</p>
        <button type="button" class="btn error" disabled>Cleanup artifacts deferred</button>
    </section>
</section>
