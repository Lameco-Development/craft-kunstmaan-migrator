---
phase: 04.1
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - src/console/DoctorController.php
  - src/locale/LocalePreflight.php
  - tests/locale/LocalePreflightRung0Test.php
autonomous: true
requirements: [LOC-03]
must_haves:
  truths:
    - "Doctor 10th check consults DEFAULT_LOCALE from KunstmaanEnvReader as Rung 0"
    - "When env DEFAULT_LOCALE is null/blank: silent — no doctor row at all (D-13)"
    - "When env DEFAULT_LOCALE matches localeMap[0] handle: OK row"
    - "When env DEFAULT_LOCALE differs from localeMap[0]: WARN row with verbatim D-12 copy; NEVER FAIL"
    - "LocalePreflight::resolve()'s 3-rung ladder is unchanged — Rung 0 is purely advisory at the doctor seam"
  artifacts:
    - path: "src/console/DoctorController.php"
      provides: "checkLocalePreflightRung0() method, wired after checkKunstmaanEnvSource"
      contains: "checkLocalePreflightRung0"
    - path: "src/locale/LocalePreflight.php"
      provides: "static helper compareEnvDefaultLocaleToLocaleMap() for env-locale-vs-localeMap[0] comparison"
      contains: "compareEnvDefaultLocaleToLocaleMap"
    - path: "tests/locale/LocalePreflightRung0Test.php"
      provides: "3-outcome characterization tests for the doctor consult"
      contains: "final class LocalePreflightRung0Test"
  key_links:
    - from: "src/console/DoctorController.php (checkLocalePreflightRung0)"
      to: "Plugin::getInstance()->kunstmaanEnvReader->getDefaultLocale()"
      via: "direct call"
      pattern: "kunstmaanEnvReader->getDefaultLocale"
    - from: "checkLocalePreflightRung0"
      to: "Settings::localeMap (first key)"
      via: "array_key_first"
      pattern: "array_key_first"
---

<objective>
Add an advisory "Rung 0" doctor check that compares the Kunstmaan project's
`DEFAULT_LOCALE` (from `.env` via `KunstmaanEnvReader`) against the first handle
in `Settings::localeMap`. Mismatch → WARN; match → OK; env signal absent → silent.

Per D-11: Rung 0 is a *doctor consult only* — `LocalePreflight::resolve()`'s
existing 3-rung matching ladder (explicit map → exact handle → language prefix)
stays unchanged. The new code reads env + first localeMap key and emits a row.

Per D-12: never FAILs. NL-default Kunstmaan migrating into an EN-primary Craft
instance is a real and intentional pattern (content reflow); we don't second-guess.

Per D-13: when env DEFAULT_LOCALE is null/blank, NO row is emitted (silent — no
WARN about a missing optional signal).

Purpose: Surface drift between operator's primary-locale intent and the Kunstmaan
source-of-truth. Closes LOC-03.

Output: New private check method on DoctorController + characterization tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.1-polish-recovery-and-env-defaults/04.1-CONTEXT.md
@.planning/phases/04.1-polish-recovery-and-env-defaults/04.1-PATTERNS.md
@.planning/phases/04.1-polish-recovery-and-env-defaults/04.1-01-env-reader-PLAN.md
@src/console/DoctorController.php
@src/locale/LocalePreflight.php
@src/source/KunstmaanEnvReader.php

<interfaces>
From src/source/KunstmaanEnvReader.php (Plan 04.1-01 — already shipped):
```php
public function getDefaultLocale(): ?string;  // e.g. "nl" or null
```

From src/locale/LocalePreflight.php (current; UNCHANGED by this plan):
```php
public function detect(): array;
public function resolve(string $locale, array $sites, array $localeMap): ?int;
public function ensure(MigrationFilters $filters): ?array;
```

From src/models/Settings.php (the source of truth for operator-curated localeMap):
```php
public array $localeMap = [];  // ['nl' => 'default', 'en' => 'english', ...]
```

The semantics: localeMap is keyed by **legacy locale code** with values being
**Craft site handles**. CONTEXT D-11 says "first handle in Settings::localeMap";
PATTERNS.md uses `array_key_first($localeMap)` — which gives the FIRST KEY (legacy
locale code), not the value. The check compares env's DEFAULT_LOCALE to this first
key. (This matches the operator's expected mental model: localeMap is ordered
legacy-locale-first; Kunstmaan's DEFAULT_LOCALE *is* a legacy locale code.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing LocalePreflight Rung 0 doctor consult tests</name>
  <files>tests/locale/LocalePreflightRung0Test.php</files>
  <read_first>
    - tests/locale/LocalePreflightTest.php (sibling test; pure-PHPUnit shape, no Craft bootstrap)
    - src/locale/LocalePreflight.php (verify NO public Rung-0 method exists today; this plan does NOT add one — the consult lives in DoctorController)
    - src/source/KunstmaanEnvReader.php (the seam we'll stub)
    - .planning/phases/04.1-polish-recovery-and-env-defaults/04.1-PATTERNS.md ("MOD: src/locale/LocalePreflight.php" section — confirms Rung 0 is doctor-side, not LocalePreflight-side)
  </read_first>
  <behavior>
    The doctor consult logic (`checkLocalePreflightRung0`) is the unit under test.
    Since DoctorController is hard to instantiate without Craft bootstrap, extract
    the comparison logic into a pure helper for testability. Two options (planner
    picks A):
    (A) Add a static helper `LocalePreflight::compareEnvDefaultLocaleToLocaleMap(?string $envLocale, array $localeMap): array` that returns ['status' => 'silent'|'ok'|'warn'|'no-map', 'envLocale' => string|null, 'firstHandle' => string|null]. DoctorController calls it; tests call it directly.
    (B) Test DoctorController via a Craft-bootstrapped functional test (heavier, slower).

    Use option (A) — it mirrors `LocalePreflight::languagePrefix` (already a static helper) and PATTERNS.md's "pure-helper escape hatch" pattern.

    Tests (5 total):
    - testReturnsSilentWhenEnvLocaleNull: compareEnvDefaultLocaleToLocaleMap(null, ['nl' => 'default']) → status='silent'
    - testReturnsSilentWhenEnvLocaleEmptyString: ('', [...]) → status='silent'
    - testReturnsNoMapWhenLocaleMapEmpty: ('nl', []) → status='no-map' (info row in caller)
    - testReturnsOkWhenEnvLocaleMatchesFirstKey: ('nl', ['nl' => 'default', 'en' => 'english']) → status='ok', envLocale='nl', firstHandle='nl'
    - testReturnsWarnWhenEnvLocaleMismatchesFirstKey: ('nl', ['en' => 'english', 'nl' => 'default']) → status='warn', envLocale='nl', firstHandle='en'

    Pure PHPUnit, namespace lameco\kunstmaanmigrator\tests\locale.

    Tests will FAIL at this step (helper doesn't exist yet — Task 2 adds it).
  </behavior>
  <action>
    Create tests/locale/LocalePreflightRung0Test.php under namespace `lameco\kunstmaanmigrator\tests\locale`.

    Test class: `final class LocalePreflightRung0Test extends PHPUnit\Framework\TestCase`

    Five test methods per the <behavior> block. Each calls the static helper directly with hand-built locale maps; no fixture files; no Craft.

    Tests fail because the static helper doesn't exist yet.
  </action>
  <verify>
    <automated>composer test -- --filter LocalePreflightRung0Test 2>&1 | grep -qE "(Errors|Failures|Cannot|Undefined)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/locale/LocalePreflightRung0Test.php` exits 0
    - `grep -q "final class LocalePreflightRung0Test" tests/locale/LocalePreflightRung0Test.php` succeeds
    - `grep -c "public function test" tests/locale/LocalePreflightRung0Test.php` returns 5
    - `composer test -- --filter LocalePreflightRung0Test` reports failures (red phase: helper doesn't exist)
  </acceptance_criteria>
  <done>
    5 failing pure-PHPUnit tests exercising the env-locale-vs-first-handle comparison.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add static comparison helper to LocalePreflight + Doctor 10th check</name>
  <files>src/locale/LocalePreflight.php, src/console/DoctorController.php</files>
  <read_first>
    - src/locale/LocalePreflight.php (the languagePrefix() static-helper analog at the bottom of the class)
    - src/console/DoctorController.php lines 47-75 (action orchestration && chain — for adding the 10th check)
    - src/console/DoctorController.php lines 256-274 (checkAdapterPlugins — INFO/WARN doctor-row pattern)
    - .planning/phases/04.1-polish-recovery-and-env-defaults/04.1-PATTERNS.md "MOD: src/locale/LocalePreflight.php" + "MOD: src/console/DoctorController.php — Rung 0 doctor row" sections
    - tests/locale/LocalePreflightRung0Test.php (Task 1 — the contract this implementation must satisfy)
  </read_first>
  <action>
    PART A — `src/locale/LocalePreflight.php`: add static helper

    Append after the existing `languagePrefix()` static helper (or wherever the file's other static helpers live):

        /**
         * Phase 4.1 / D-11..D-13 — Rung 0 advisory comparison.
         * Pure helper; no I/O. Used by DoctorController::checkLocalePreflightRung0().
         *
         * Returns one of:
         *   ['status' => 'silent', ...]  — env signal absent (D-13)
         *   ['status' => 'no-map', 'envLocale' => string, 'firstHandle' => null]  — env present but localeMap empty
         *   ['status' => 'ok',     'envLocale' => string, 'firstHandle' => string]
         *   ['status' => 'warn',   'envLocale' => string, 'firstHandle' => string]
         */
        public static function compareEnvDefaultLocaleToLocaleMap(
            ?string $envLocale,
            array $localeMap,
        ): array {
            if ($envLocale === null || $envLocale === '') {
                return ['status' => 'silent', 'envLocale' => null, 'firstHandle' => null];
            }
            $firstHandle = $localeMap === [] ? null : (string) array_key_first($localeMap);
            if ($firstHandle === null) {
                return ['status' => 'no-map', 'envLocale' => $envLocale, 'firstHandle' => null];
            }
            $status = ((string) $firstHandle === (string) $envLocale) ? 'ok' : 'warn';
            return ['status' => $status, 'envLocale' => $envLocale, 'firstHandle' => $firstHandle];
        }

    PART B — `src/console/DoctorController.php`: add the 10th check method + wire into && chain.

    In actionIndex's && chain, add AFTER `checkKunstmaanEnvSource()` (the 9th from Plan 04.1-01):

        $ok = $this->checkLocalePreflightRung0() && $ok; // Phase 4.1 / D-11..D-13 (10th — info/warn/silent)

    Method body (verbatim from PATTERNS.md, with the helper now available):

        private function checkLocalePreflightRung0(): bool
        {
            $plugin = Plugin::getInstance();
            $envLocale = $plugin->kunstmaanEnvReader->getDefaultLocale();
            $localeMap = (array) $plugin->getSettings()->localeMap;
            $result = LocalePreflight::compareEnvDefaultLocaleToLocaleMap($envLocale, $localeMap);

            switch ($result['status']) {
                case 'silent':
                    return true; // D-13: no row
                case 'no-map':
                    $this->stdout(sprintf("  INFO DEFAULT_LOCALE=%s but no localeMap configured\n", $result['envLocale']), Console::FG_YELLOW);
                    return true;
                case 'ok':
                    $this->stdout(sprintf("  OK   DEFAULT_LOCALE=%s aligns with localeMap[0]\n", $result['envLocale']), Console::FG_GREEN);
                    return true;
                case 'warn':
                    $this->stdout(sprintf(
                        "  WARN Kunstmaan default locale `%s` but operator's `localeMap` lists `%s` first (Craft-primary). Reorder localeMap or confirm intent.\n",
                        $result['envLocale'],
                        $result['firstHandle'],
                    ), Console::FG_YELLOW);
                    return true; // D-12: WARN, never FAIL
            }
            return true;
        }

    Add `use lameco\kunstmaanmigrator\locale\LocalePreflight;` to DoctorController's import block if not already present.

    The WARN copy is verbatim from D-12 — DO NOT paraphrase.

    Re-run tests; all 5 must pass.
  </action>
  <verify>
    <automated>composer test -- --filter LocalePreflightRung0Test 2>&1 | grep -qE "OK \(5 tests" && grep -q "checkLocalePreflightRung0" src/console/DoctorController.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "compareEnvDefaultLocaleToLocaleMap" src/locale/LocalePreflight.php` succeeds
    - `grep -q "public static function compareEnvDefaultLocaleToLocaleMap" src/locale/LocalePreflight.php` succeeds
    - `grep -c "checkLocalePreflightRung0" src/console/DoctorController.php` returns at least 2 (declaration + && chain)
    - `grep -q "Reorder localeMap or confirm intent" src/console/DoctorController.php` succeeds (verbatim D-12 WARN copy)
    - `grep -q "DEFAULT_LOCALE=.* aligns with localeMap\\[0\\]" src/console/DoctorController.php` succeeds
    - `grep -q "but no localeMap configured" src/console/DoctorController.php` succeeds (no-map INFO row)
    - The check method always returns true (no `return false` in checkLocalePreflightRung0)
    - `composer test -- --filter LocalePreflightRung0Test` exits 0 (5/5)
    - `composer test` (full suite) exits 0
    - LocalePreflight::resolve() body is UNCHANGED (verify with `git diff src/locale/LocalePreflight.php` — only addition, no removal of resolve() lines)
  </acceptance_criteria>
  <done>
    Doctor 10th check consults env DEFAULT_LOCALE as Rung 0 with silent/info/ok/warn semantics. Comparison logic is unit-tested via a pure static helper. LocalePreflight's existing 3-rung matching ladder is untouched. WARN copy is verbatim D-12.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → doctor | env DEFAULT_LOCALE flows from `.env` (operator-controlled) into doctor output |
| Settings → doctor | operator-curated localeMap is read for the comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-03-01 | Information Disclosure | doctor row content | mitigate | Output reveals only the locale code (e.g. "nl") and the operator's first localeMap key — both are non-sensitive. No file paths, no DSNs. |
| T-04.1-03-02 | Tampering | localeMap reorder | accept | Operator can ignore the WARN and proceed. D-12 explicitly preserves operator agency: NL→EN content reflow is a real intentional pattern. |
| T-04.1-03-03 | Denial of Service | reader returns malformed locale | mitigate | Helper coerces with (string) cast; `array_key_first` on empty array returns null (handled); no exceptions escape. |
</threat_model>

<verification>
- `composer test -- --filter LocalePreflightRung0Test` is green (5/5)
- `composer test` (full suite) is green
- `grep -q "compareEnvDefaultLocaleToLocaleMap" src/locale/LocalePreflight.php` succeeds
- `grep -q "checkLocalePreflightRung0" src/console/DoctorController.php` succeeds
- LocalePreflight::resolve() body is byte-unchanged (verify diff)
- WARN copy is verbatim D-12: "Kunstmaan default locale `<X>` but operator's `localeMap` lists `<Y>` first (Craft-primary). Reorder localeMap or confirm intent."
</verification>

<success_criteria>
1. Operator with env DEFAULT_LOCALE=nl + localeMap=[nl=>default, en=>english]: doctor shows OK row.
2. Operator with env DEFAULT_LOCALE=nl + localeMap=[en=>english, nl=>default]: doctor shows WARN with verbatim D-12 copy; exit code unchanged.
3. Operator with no .env or DEFAULT_LOCALE missing: no doctor row for Rung 0 (silent, D-13).
4. LOC-03 closed.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-polish-recovery-and-env-defaults/04.1-03-locale-preflight-rung0-SUMMARY.md` (LOC-03 closed via Doctor 10th check).
</output>
