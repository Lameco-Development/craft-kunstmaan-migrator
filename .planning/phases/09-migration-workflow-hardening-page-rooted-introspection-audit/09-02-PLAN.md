---
phase: 09-migration-workflow-hardening-page-rooted-introspection-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/filter/MigrationFilters.php
  - src/filter/FilterFactory.php
  - tests/unit/filter/FilterFactoryTest.php
  - tests/unit/filter/MigrationFiltersReachabilityTest.php
autonomous: true
requirements: [PH9-04, PH9-05]
must_haves:
  truths:
    - "`--entities` means Kunstmaan source entity identity everywhere"
    - "CLI accepts both FQCN and basename forms"
    - "RelationGraph reachability is wired into filters"
  artifacts:
    - path: "src/filter/FilterFactory.php"
      provides: "source entity normalization and relationGraph injection"
    - path: "src/filter/MigrationFilters.php"
      provides: "source FQCN/basename matching and dependency reachability"
---

<objective>
Establish the source-domain filter core used by all later Phase 9 filter work.

User-facing outcome: scoped runs start from one authoritative Kunstmaan entity interpretation instead of mixing source FQCNs, basenames, and Craft handles.
</objective>

<scope>
In scope:
- Normalize FQCN/basename entity filters per D-14.
- Wire relationGraph into `MigrationFilters` per D-15.
- Keep `MigrationFilters` source-domain only; no Craft handles in the value object.

Non-goals:
- Do not update runtime stage consumers in this plan; `09-02B` and `09-02C` own integration.
- Do not add new filter flags.
</scope>

<context>
@.planning/phases/09-migration-workflow-hardening-page-rooted-introspection-audit/09-CONTEXT.md
@.planning/phases/09-migration-workflow-hardening-page-rooted-introspection-audit/09-PATTERNS.md
@src/filter/MigrationFilters.php
@src/filter/FilterFactory.php
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Normalize source entity filters</name>
  <files>src/filter/FilterFactory.php, src/filter/MigrationFilters.php, tests/unit/filter/FilterFactoryTest.php</files>
  <behavior>
    - `--entities=NewsPage,App\Entity\Pages\CaseStudyPage` normalizes deterministically.
    - Explicit filters match source FQCN and basename forms.
    - Normalization de-dupes without treating values as Craft handles.
  </behavior>
  <action>
    Per D-14, add a normalization helper in `FilterFactory` or a small source-identity helper. Preserve both exact FQCN and basename matching in `MigrationFilters::allows()` without changing the CLI flag surface.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/filter/FilterFactoryTest.php --testdox</automated>
  </verify>
  <done>Filter core tests prove source FQCN/basename normalization and no Craft-handle semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire relationGraph reachability into filters</name>
  <files>src/filter/MigrationFilters.php, src/filter/FilterFactory.php, tests/unit/filter/MigrationFiltersReachabilityTest.php</files>
  <behavior>
    - `relationGraph` DFS includes reachable source dependencies when supplied.
    - Empty graph preserves existing behavior.
    - Existing `fromCli()` call sites remain source-compatible.
  </behavior>
  <action>
    Per D-15, add an optional relation graph argument or loader seam to `FilterFactory::fromCli()` and pass it into `MigrationFilters`. Keep defaults backward-compatible. The graph represents Kunstmaan source identities only.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/filter/MigrationFiltersReachabilityTest.php --testdox</automated>
  </verify>
  <done>Reachability tests prove scoped source filters can include dependent source classes.</done>
</task>
</tasks>

<verification>
- `vendor/bin/phpunit tests/unit/filter/FilterFactoryTest.php tests/unit/filter/MigrationFiltersReachabilityTest.php --testdox`
- `composer test-unit`
</verification>

<success_criteria>
D-14 and D-15 are implemented at the source filter core; `09-02B` and `09-02C` build on this for Craft translation and cross-stage D-16/D-17 behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/09-migration-workflow-hardening-page-rooted-introspection-audit/09-02-SUMMARY.md`.
</output>
