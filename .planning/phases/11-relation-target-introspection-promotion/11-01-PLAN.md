---
phase: 11-relation-target-introspection-promotion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/source/KunstmaanGraphContract.php
  - src/source/CraftGraphContract.php
  - tests/support/GraphFixtureFactory.php
  - tests/unit/source/GraphContractTest.php
autonomous: true
requirements: [PH11-01, PH11-02, PH11-03, PH11-05, PH11-06, PH11-08, PH11-10]
must_haves:
  truths:
    - Both source and target graph artifacts expose explicit graphVersion values.
    - Graph data is normalized into registries with stable references, not duplicated nested blobs.
    - Relation intent categories are centrally defined as reference, promote, embed, drop, out_of_scope.
  artifacts:
    - path: src/source/KunstmaanGraphContract.php
      provides: Kunstmaan graph version, root/registry key constants, relation intent constants, and stable ref helpers.
    - path: src/source/CraftGraphContract.php
      provides: Craft graph version, root/registry key constants, and stable ref helpers.
    - path: tests/support/GraphFixtureFactory.php
      provides: Shared normalized graph fixtures for walker, analyze, compile, transform, and reporting tests.
  key_links:
    - from: src/source/KunstmaanGraphContract.php
      to: src/source/KunstmaanPageWalker.php
      via: Contract constants define walker output keys and source refs.
    - from: src/source/CraftGraphContract.php
      to: src/source/CraftEntryWalker.php
      via: Contract constants define walker output keys and target refs.
---

# Plan 11-01: Graph contracts/versioning and shared fixtures

<objective>
Define the versioned normalized Kunstmaan and Craft graph contracts that every Phase 11 walker, analyzer, compiler, validator, and test fixture will use.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Define versioned normalized graph contracts</name>
  <files>src/source/KunstmaanGraphContract.php, src/source/CraftGraphContract.php, tests/unit/source/GraphContractTest.php</files>
  <read_first>
    .planning/phases/11-relation-target-introspection-promotion/11-CONTEXT.md
    .planning/phases/11-relation-target-introspection-promotion/11-PATTERNS.md
    src/source/DoctrineEntityInfo.php
    src/source/DoctrineColumnInfo.php
    src/source/DoctrineRelationInfo.php
  </read_first>
  <action>
    Create final classes `KunstmaanGraphContract` and `CraftGraphContract` in namespace `lameco\kunstmaanmigrator\source` with `declare(strict_types=1)`. `KunstmaanGraphContract` must define `GRAPH_VERSION = 'kunstmaan-page-graph-v1'` and registry keys `graphVersion`, `roots`, `entities`, `relations`, `pageparts`, `pagepartUsages`, `assets`, `tables`, `samples`, and `constraints`. `CraftGraphContract` must define `GRAPH_VERSION = 'craft-entry-graph-v1'` and registry keys `graphVersion`, `roots`, `entryTypes`, `fields`, `matrixBlockTypes`, `matrixUsages`, `relationTargets`, `assetVolumes`, and `constraints`. Add relation intent constants exactly `reference`, `promote`, `embed`, `drop`, and `out_of_scope` in `KunstmaanGraphContract`. Add pure stable-ref helper methods: `pageRootRef(string $fqcn)`, `entityRef(string $fqcn)`, `pagepartRef(string $fqcn)`, `craftEntryTypeRef(string $handle)`, `craftFieldRef(string $entryTypeHandle, string $fieldHandle)`, and `matrixBlockRef(string $matrixFieldHandle, string $blockEntryTypeHandle)`. Do not add proof-environment names such as NewsPage, Employee, caseTeamMembers, or pageBuilderCondensed to production contract files.
  </action>
  <verify>
    vendor/bin/phpunit tests/unit/source/GraphContractTest.php --testdox
  </verify>
  <acceptance_criteria>
    - `grep -R "GRAPH_VERSION = 'kunstmaan-page-graph-v1'" src/source/KunstmaanGraphContract.php` returns 1 match.
    - `grep -R "GRAPH_VERSION = 'craft-entry-graph-v1'" src/source/CraftGraphContract.php` returns 1 match.
    - `grep -R "INTENT_REFERENCE\|INTENT_PROMOTE\|INTENT_EMBED\|INTENT_DROP\|INTENT_OUT_OF_SCOPE" src/source/KunstmaanGraphContract.php | wc -l` is at least 5.
    - `grep -R "App\\\\Entity\\\\Pages\\\\NewsPage\|caseTeamMembers\|pageBuilderCondensed" src/source/KunstmaanGraphContract.php src/source/CraftGraphContract.php` returns 0 matches.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Add shared graph fixtures for downstream tests</name>
  <files>tests/support/GraphFixtureFactory.php, tests/unit/source/GraphContractTest.php</files>
  <read_first>
    .planning/phases/11-relation-target-introspection-promotion/11-CONTEXT.md
    .planning/phases/11-relation-target-introspection-promotion/11-PATTERNS.md
    src/source/KunstmaanGraphContract.php
    src/source/CraftGraphContract.php
  </read_first>
  <action>
    Create `tests/support/GraphFixtureFactory.php` with static methods `kunstmaanNewsEmployeeGraph(): array`, `kunstmaanHomePagepartGraph(): array`, and `craftNewsHomeGraph(): array`. Fixtures may use proof-shape names such as `App\Entity\Pages\NewsPage`, `App\Entity\Employee`, `newsPage`, `homePage`, `caseTeamMembers`, `image`, and `pageBuilderCondensed`, but production code must remain generic. Fixtures must use contract constants for graphVersion and registry keys. The News/Employee source fixture must represent Employee once under `entities` and include inbound owner evidence from `NewsPage.employee_id`. The Home fixture must represent reusable pageparts once with usage refs. The Craft fixture must represent entry roots, field refs, Matrix block type refs, Entries/Assets constraints, and required native fields.
  </action>
  <verify>
    vendor/bin/phpunit tests/unit/source/GraphContractTest.php --testdox
  </verify>
  <acceptance_criteria>
    - `grep -R "function kunstmaanNewsEmployeeGraph" tests/support/GraphFixtureFactory.php` returns 1 match.
    - `grep -R "function kunstmaanHomePagepartGraph" tests/support/GraphFixtureFactory.php` returns 1 match.
    - `grep -R "function craftNewsHomeGraph" tests/support/GraphFixtureFactory.php` returns 1 match.
    - `grep -R "inboundOwners" tests/support/GraphFixtureFactory.php` returns at least 1 match.
    - `grep -R "pagepartUsages\|matrixUsages" tests/support/GraphFixtureFactory.php` returns at least 2 matches.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Graph contract drift silently changes mapping semantics. | High | Explicit `GRAPH_VERSION` constants and tests asserting required keys/refs. |
| Test fixtures leak into production assumptions. | Medium | Production contract acceptance criteria reject proof-specific class/handle strings. |
</threat_model>

<verification>
Run `vendor/bin/phpunit tests/unit/source/GraphContractTest.php --testdox`.
</verification>

<success_criteria>
- `KunstmaanGraphContract` and `CraftGraphContract` exist with graph version constants.
- Relation intent constants are centrally defined.
- Shared graph fixtures exist for News/Employee, Home/pageparts, and Craft News/Home proof shapes.
</success_criteria>
