---
phase: 11-relation-target-introspection-promotion
plan: 06
type: execute
wave: 3
depends_on: [11-05]
files_modified:
  - src/extract/ExtractService.php
  - src/transform/TransformService.php
  - src/load/EntryMigrationService.php
  - src/console/MigrateController.php
  - tests/unit/extract/ExtractServicePromotedTargetsTest.php
  - tests/unit/transform/TransformServicePromotedTargetsTest.php
  - tests/unit/load/EntryMigrationServicePromotedTargetsTest.php
  - tests/unit/console/MigrateControllerPromotedTargetsTest.php
autonomous: true
requirements: [PH11-06, PH11-07, PH11-08, PH11-09]
must_haves:
  truths:
    - Promoted/shared targets have their own extract, transform, load identity and state rows.
    - Owner extracts keep raw FK IDs and do not embed copied Employee columns.
    - Load creates promoted/shared targets before owners that reference them and reruns idempotently.
  artifacts:
    - path: src/extract/ExtractService.php
      provides: Promoted target extract identity and source-faithful owner extracts.
    - path: src/transform/TransformService.php
      provides: Promoted target transform identity, target payload, stateSource preservation, and owner FK/reference semantics.
    - path: src/load/EntryMigrationService.php
      provides: Promoted target load/state handling.
    - path: src/console/MigrateController.php
      provides: Promoted-before-owner orchestration.
  key_links:
    - from: src/compile/MappingCompiler.php
      to: src/extract/ExtractService.php
      via: Promoted target source identity and stateSource contract.
    - from: src/extract/ExtractService.php
      to: src/transform/TransformService.php
      via: Promoted target extracted records become standalone transformed payloads.
    - from: src/transform/TransformService.php
      to: src/load/EntryMigrationService.php
      via: Transformed promoted target stateSource and target entry type are loaded before owners.
    - from: src/load/EntryMigrationService.php
      to: migration state table
      via: State rows for promoted/shared targets.
---

# Plan 11-06: Promoted/shared target extract/transform/load support

<objective>
Add the minimal promoted/shared relation target extract, transform, and load ordering support needed for the NewsPage/Employee proof while preserving source-faithful owner extracts and idempotent state rows.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Extract promoted/shared relation targets under their own source identity</name>
  <files>src/extract/ExtractService.php, tests/unit/extract/ExtractServicePromotedTargetsTest.php</files>
  <read_first>
    .planning/phases/11-relation-target-introspection-promotion/11-CONTEXT.md
    .planning/phases/11-relation-target-introspection-promotion/11-PATTERNS.md
    src/extract/ExtractService.php
    src/compile/MappingCompiler.php
    tests/unit/extract/ExtractServiceFkJoinTest.php
  </read_first>
  <action>
    Extend `ExtractService` so compiled promoted/shared target contracts cause relation targets to be extracted as their own records under a deterministic source identity, for example `promoted:{sourceFqcn}:{legacyId}` or the existing state-source format if one is already established in compiled mapping. Owner page extracts must retain raw FK columns such as `employee_id` and asset FK IDs, and must not embed copied Employee columns into owner JSON. Keep any existing debug/expansion option opt-in only. The implementation must remain generic; NewsPage/Employee may appear only in unit fixture data, not as production conditionals.
  </action>
  <verify>
    vendor/bin/phpunit tests/unit/extract/ExtractServicePromotedTargetsTest.php tests/unit/extract/ExtractServiceFkJoinTest.php --testdox
  </verify>
  <acceptance_criteria>
    - `grep -R "promoted" src/extract/ExtractService.php` returns at least 1 match.
    - `grep -R "stateSource" src/extract/ExtractService.php` returns at least 1 match.
    - Tests assert owner JSON does not require relation-expanded `_rel:` helper blobs as canonical data.
    - `grep -R "NewsPage\|Employee" src/extract/ExtractService.php` returns 0 matches.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Transform promoted/shared relation targets under their own stateSource</name>
  <files>src/transform/TransformService.php, tests/unit/transform/TransformServicePromotedTargetsTest.php</files>
  <read_first>
    .planning/phases/11-relation-target-introspection-promotion/11-CONTEXT.md
    .planning/phases/11-relation-target-introspection-promotion/11-PATTERNS.md
    src/transform/TransformService.php
    src/compile/MappingCompiler.php
    src/extract/ExtractService.php
    tests/unit/transform/TransformServiceTest.php
  </read_first>
  <action>
    Extend `TransformService` so promoted/shared target extracted records transform as standalone target payloads using their compiled `sourceRef`, `targetRef`, `targetSection`, `targetEntryType`, `stateSource`, and relation intent contract. Add `TransformServicePromotedTargetsTest` proving an Employee-like promoted target record transforms under its own source identity/stateSource before owner load, while an owner NewsPage-like transform retains raw `employee_id`/reference semantics and does not embed copied Employee columns. If existing generic transform logic already supports this shape, add the test and only minimal adapter code needed to preserve `stateSource` and promoted target metadata. Production code must not contain `NewsPage` or `Employee` conditionals.
  </action>
  <verify>
    vendor/bin/phpunit tests/unit/transform/TransformServicePromotedTargetsTest.php --testdox
  </verify>
  <acceptance_criteria>
    - `grep -R "TransformServicePromotedTargetsTest" tests/unit/transform/TransformServicePromotedTargetsTest.php` returns 1 match.
    - `grep -R "stateSource" src/transform/TransformService.php tests/unit/transform/TransformServicePromotedTargetsTest.php` returns at least 1 match.
    - `grep -R "promoted" src/transform/TransformService.php tests/unit/transform/TransformServicePromotedTargetsTest.php` returns at least 1 match.
    - `grep -R "employee_id" tests/unit/transform/TransformServicePromotedTargetsTest.php` returns at least 1 match proving owner reference semantics.
    - `grep -R "NewsPage\|Employee" src/transform/TransformService.php` returns 0 matches.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Load promoted/shared targets before owners with idempotent state rows</name>
  <files>src/load/EntryMigrationService.php, src/console/MigrateController.php, tests/unit/load/EntryMigrationServicePromotedTargetsTest.php, tests/unit/console/MigrateControllerPromotedTargetsTest.php</files>
  <read_first>
    .planning/phases/11-relation-target-introspection-promotion/11-CONTEXT.md
    .planning/phases/11-relation-target-introspection-promotion/11-PATTERNS.md
    src/load/EntryMigrationService.php
    src/console/MigrateController.php
    src/transform/TransformService.php
    src/fields/handlers/RelationHandler.php
    src/load/TaxonomyMigrationService.php
    tests/unit/fields/RelationHandlerTaxonomyResolverTest.php
  </read_first>
  <action>
    Extend load orchestration so transformed promoted/shared target records compile into a pre-owner load group and create/reuse migration state rows before owner entries are loaded. Owner relation fields must resolve through existing state lookup using the promoted target `stateSource`; do not add unsafe lazy create behavior inside `RelationHandler` for arbitrary entries. Reruns must be idempotent: the same promoted source identity reuses the existing Craft element/state row instead of duplicating target entries. Preserve taxonomy lazy resolver behavior for explicitly taxonomy-backed relations and do not reroute all non-page entities to taxonomy.
  </action>
  <verify>
    vendor/bin/phpunit tests/unit/transform/TransformServicePromotedTargetsTest.php tests/unit/load/EntryMigrationServicePromotedTargetsTest.php tests/unit/console/MigrateControllerPromotedTargetsTest.php tests/unit/fields/RelationHandlerTaxonomyResolverTest.php --testdox
  </verify>
  <acceptance_criteria>
    - `grep -R "promoted" src/load/EntryMigrationService.php src/console/MigrateController.php` returns at least 2 matches.
    - `grep -R "stateSource" src/load/EntryMigrationService.php src/console/MigrateController.php` returns at least 1 match.
    - `grep -R "resolveReferenced" src/fields/handlers/RelationHandler.php` still points only to taxonomy-backed resolver behavior.
    - `grep -R "NewsPage\|Employee" src/load/EntryMigrationService.php src/console/MigrateController.php` returns 0 matches.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Promoted targets duplicate on rerun. | High | Deterministic source identity and existing migration state lookup. |
| Relation handler creates arbitrary entries lazily. | High | Owner relations resolve through preloaded state rows; taxonomy resolver remains separate. |
| Owner extracts become polluted with copied relation target fields. | Medium | Tests assert raw FK owner data remains canonical. |
</threat_model>

<verification>
Run `vendor/bin/phpunit tests/unit/extract/ExtractServicePromotedTargetsTest.php tests/unit/transform/TransformServicePromotedTargetsTest.php tests/unit/load/EntryMigrationServicePromotedTargetsTest.php tests/unit/console/MigrateControllerPromotedTargetsTest.php --testdox`.
</verification>

<success_criteria>
- Promoted/shared relation targets extract and transform under their own source identity/stateSource.
- Promoted/shared targets load before owner entries and reuse state rows on rerun.
- Owner page extracts retain raw FK data and no production code hardcodes NewsPage/Employee.
</success_criteria>
