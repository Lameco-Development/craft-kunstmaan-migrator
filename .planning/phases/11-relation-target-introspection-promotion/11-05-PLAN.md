---
phase: 11-relation-target-introspection-promotion
plan: 05
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/compile/GraphCompatibilityValidator.php
  - src/compile/MappingCompiler.php
  - src/console/CompileController.php
  - src/mapping/MappingAuditor.php
  - tests/unit/compile/GraphCompatibilityValidatorTest.php
  - tests/unit/compile/MappingCompilerPromotedTargetsTest.php
autonomous: true
requirements: [PH11-05, PH11-08, PH11-10]
must_haves:
  truths:
    - Compile rejects missing graph versions and graph-incompatible mapping proposals.
    - Relation FKs with evidence require mapping, drop, or out_of_scope decisions before live migration.
    - Promoted/shared target mappings require stateSource and target contracts.
  artifacts:
    - path: src/compile/GraphCompatibilityValidator.php
      provides: Deterministic graph proposal validation.
    - path: src/compile/MappingCompiler.php
      provides: Executable promoted/shared target mapping shape.
    - path: src/console/CompileController.php
      provides: Compile-time graph validation gate.
    - path: src/mapping/MappingAuditor.php
      provides: Operator-facing graph/relation-intent findings.
  key_links:
    - from: src/console/CompileController.php
      to: src/compile/GraphCompatibilityValidator.php
      via: Validate canonical graph pair before executable mapping write.
    - from: src/compile/MappingCompiler.php
      to: src/compile/GraphCompatibilityValidator.php
      via: Accepted proposal compatibility checks.
    - from: src/compile/MappingCompiler.php
      to: src/extract/ExtractService.php
      via: Promoted target stateSource and source identity contracts consumed by Plan 11-06.
---

# Plan 11-05: Compile/audit graph validation

<objective>
Add deterministic validation for graph-pair mapping proposals and compile/audit support for relation intents and promoted/shared target contracts.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Add graph compatibility validator</name>
  <files>src/compile/GraphCompatibilityValidator.php, tests/unit/compile/GraphCompatibilityValidatorTest.php</files>
  <read_first>
    .planning/phases/11-relation-target-introspection-promotion/11-CONTEXT.md
    .planning/phases/11-relation-target-introspection-promotion/11-PATTERNS.md
    src/source/KunstmaanGraphContract.php
    src/source/CraftGraphContract.php
    src/mapping/BlockAvailabilityValidator.php
    src/compile/CraftTargetIntrospector.php
    tests/unit/compile/CraftTargetIntrospectorTest.php
  </read_first>
  <action>
    Create `GraphCompatibilityValidator` in namespace `lameco\kunstmaanmigrator\compile`. Implement `public function validate(array $mapping, array $kunstmaanGraph, array $craftGraph): array` returning rows with `severity`, `code`, `sourceRef`, `targetRef`, and `message`. Reject missing/unknown graph versions, unknown source refs, unknown target refs, target fields not owned by target entry type, Matrix block refs not allowed by the Matrix field, relation target refs incompatible with Entries constraints, asset refs incompatible with allowed sources/kinds/volumes, and non-empty relation evidence without an accepted `reference`, `promote`, `embed`, `drop`, or `out_of_scope` decision. Use severity `fatal` for compile/load unsafe incompatibilities and `warning` for advisory candidate issues. Do not classify `App\Entity\Employee` or any arbitrary non-page entity as taxonomy by class-name heuristic.
  </action>
  <verify>
    vendor/bin/phpunit tests/unit/compile/GraphCompatibilityValidatorTest.php --testdox
  </verify>
  <acceptance_criteria>
    - `grep -R "final class GraphCompatibilityValidator" src/compile/GraphCompatibilityValidator.php` returns 1 match.
    - `grep -R "function validate" src/compile/GraphCompatibilityValidator.php` returns at least 1 match.
    - `grep -R "missing_graph_version\|unknown_source_ref\|unknown_target_ref\|relation_intent_required" src/compile/GraphCompatibilityValidator.php` returns at least 4 matches.
    - `grep -R "App\\\\Entity\\\\Employee" src/compile/GraphCompatibilityValidator.php` returns 0 matches.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Integrate graph validation into compile and audit</name>
  <files>src/compile/MappingCompiler.php, src/console/CompileController.php, src/mapping/MappingAuditor.php, tests/unit/compile/MappingCompilerPromotedTargetsTest.php, tests/unit/compile/GraphCompatibilityValidatorTest.php</files>
  <read_first>
    .planning/phases/11-relation-target-introspection-promotion/11-CONTEXT.md
    .planning/phases/11-relation-target-introspection-promotion/11-PATTERNS.md
    src/compile/MappingCompiler.php
    src/console/CompileController.php
    src/mapping/MappingAuditor.php
    src/mapping/MappingFile.php
    src/compile/GraphCompatibilityValidator.php
    tests/unit/compile/MappingCompilerValidationTest.php
  </read_first>
  <action>
    Wire `GraphCompatibilityValidator` into compile before executable mapping is written. `CompileController` must read canonical `kunstmaan-schema.json` and `craft-schema.json` graph artifacts, validate versions, run compatibility checks, print fatal diagnostics, and return `ExitCode::CONFIG` before writing executable mapping when fatal rows exist. Extend `MappingCompiler` to accept promoted/shared target proposal rows and emit executable mapping sections for promoted relation targets and owner relation fields with complete `stateSource`, `sourceRef`, `targetRef`, `targetSection`, `targetEntryType`, and relation intent data. Extend `MappingAuditor` to render relation-intent and graph-compatibility findings, distinguishing unresolved relation evidence from intentional `drop`/`out_of_scope` decisions.
  </action>
  <verify>
    vendor/bin/phpunit tests/unit/compile/MappingCompilerPromotedTargetsTest.php tests/unit/compile/GraphCompatibilityValidatorTest.php --testdox
  </verify>
  <acceptance_criteria>
    - `grep -R "GraphCompatibilityValidator" src/console/CompileController.php src/compile/MappingCompiler.php src/mapping/MappingAuditor.php` returns at least 2 matches.
    - `grep -R "stateSource" src/compile/MappingCompiler.php` returns at least 1 match.
    - `grep -R "promotedTargets\|promotedRelationTargets" src/compile/MappingCompiler.php` returns at least 1 match.
    - `grep -R "ExitCode::CONFIG" src/console/CompileController.php` returns at least 1 match.
    - `grep -R "drop\|out_of_scope" src/mapping/MappingAuditor.php src/compile/MappingCompiler.php` returns at least 2 matches.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Tampered or stale graph artifacts compile into unsafe mappings. | High | Missing/unknown graph versions become fatal validation rows. |
| LLM proposes invalid target refs or incompatible relation/asset mappings. | High | `GraphCompatibilityValidator` rejects unknown refs and ownership/constraint violations. |
| Non-empty relation FK evidence disappears silently. | High | `relation_intent_required` fatal rows block compile/live until mapped or intentionally dropped/out_of_scope. |
</threat_model>

<verification>
Run `vendor/bin/phpunit tests/unit/compile/GraphCompatibilityValidatorTest.php tests/unit/compile/MappingCompilerPromotedTargetsTest.php --testdox`.
</verification>

<success_criteria>
- Graph compatibility validation exists and is wired into compile/audit.
- Compile rejects missing graph versions and incompatible proposal rows.
- Promoted/shared target mapping contracts include state source and target contract fields.
</success_criteria>
