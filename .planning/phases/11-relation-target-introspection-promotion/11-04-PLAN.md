---
phase: 11-relation-target-introspection-promotion
plan: 04
type: execute
wave: 3
depends_on: [11-02, 11-03]
files_modified:
  - src/Plugin.php
  - src/console/AnalyzeController.php
  - src/analyze/LlmClassifier.php
  - tests/unit/console/AnalyzeControllerGraphArtifactsTest.php
  - tests/unit/analyze/LlmClassifierGraphPromptTest.php
autonomous: true
requirements: [PH11-03, PH11-04, PH11-05, PH11-06]
must_haves:
  truths:
    - Analyze writes graph-shaped `kunstmaan-schema.json` and `craft-schema.json` as canonical artifacts.
    - The LLM receives the Kunstmaan/Craft graph pair as its primary mapping input.
    - Heuristics remain deterministic pre/post-processing and do not replace ambiguous graph mapping.
  artifacts:
    - path: src/console/AnalyzeController.php
      provides: Canonical graph artifact orchestration and graph-pair handoff.
    - path: src/analyze/LlmClassifier.php
      provides: Anthropic analyze-stage graph-pair prompt input.
  key_links:
    - from: src/console/AnalyzeController.php
      to: src/source/KunstmaanPageWalker.php
      via: Walk scoped roots before writing `kunstmaan-schema.json`.
    - from: src/console/AnalyzeController.php
      to: src/source/CraftEntryWalker.php
      via: Walk candidate entry roots before writing `craft-schema.json`.
    - from: src/console/AnalyzeController.php
      to: src/analyze/LlmClassifier.php
      via: Pass `kunstmaanGraph` and `craftGraph` as primary mapping context.
---

# Plan 11-04: Analyze/prompt integration

<objective>
Wire the two walkers into analyze so the canonical schema artifacts become graph-shaped and the LLM mapping prompt consumes the graph pair first.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Wire graph walkers into Plugin and AnalyzeController artifacts</name>
  <files>src/Plugin.php, src/console/AnalyzeController.php, tests/unit/console/AnalyzeControllerGraphArtifactsTest.php</files>
  <read_first>
    .planning/phases/11-relation-target-introspection-promotion/11-CONTEXT.md
    .planning/phases/11-relation-target-introspection-promotion/11-PATTERNS.md
    src/Plugin.php
    src/console/AnalyzeController.php
    src/mapping/MappingFile.php
    src/source/KunstmaanPageWalker.php
    src/source/CraftEntryWalker.php
    tests/unit/console/AnalyzeControllerDualSchemaDumpTest.php
  </read_first>
  <action>
    Register `kunstmaanPageWalker` and `craftEntryWalker` components in `Plugin::config()` and wire dependencies in `Plugin::init()` using existing sibling-DI style. In `AnalyzeController::actionIndex()`, after source path and locale preflight pass, call `KunstmaanPageWalker::walk($filters->entities)` with CLI-scoped roots and call `CraftEntryWalker::walk($candidateEntryTypeHandles)` where candidate handles come from accepted/current mapping rows when available and otherwise from existing Craft target schema candidate surfaces. Write the returned graph arrays to canonical paths `kunstmaan-schema.json` and `craft-schema.json` through `MappingFile::writeAtomicJson()`. Fail with `ExitCode::UNSPECIFIED_ERROR` if either graph is missing `graphVersion` or cannot be written. Keep legacy `pageStructure.json` and `relation-graph.json` only as compatibility/debug artifacts if needed; the canonical pair must be the graph pair. Leave content-only pages as factual graph columns and do not emit hardcoded generic implicit-content mapping policy.
  </action>
  <verify>
    vendor/bin/phpunit tests/unit/console/AnalyzeControllerGraphArtifactsTest.php tests/unit/console/AnalyzeControllerDualSchemaDumpTest.php --testdox
  </verify>
  <acceptance_criteria>
    - `grep -R "kunstmaanPageWalker" src/Plugin.php src/console/AnalyzeController.php` returns at least 3 matches.
    - `grep -R "craftEntryWalker" src/Plugin.php src/console/AnalyzeController.php` returns at least 3 matches.
    - `grep -R "kunstmaan-schema.json" src/console/AnalyzeController.php` returns at least 1 match.
    - `grep -R "craft-schema.json" src/console/AnalyzeController.php` returns at least 1 match.
    - `grep -R "writeAtomicJson" src/console/AnalyzeController.php` returns at least 2 matches for canonical graph writes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Make LLM mapping consume the graph pair first</name>
  <files>src/analyze/LlmClassifier.php, src/console/AnalyzeController.php, tests/unit/analyze/LlmClassifierGraphPromptTest.php</files>
  <read_first>
    .planning/phases/11-relation-target-introspection-promotion/11-CONTEXT.md
    .planning/phases/11-relation-target-introspection-promotion/11-PATTERNS.md
    src/analyze/LlmClassifier.php
    src/console/AnalyzeController.php
    src/analyze/HeuristicProposer.php
    src/source/KunstmaanGraphContract.php
    src/source/CraftGraphContract.php
  </read_first>
  <action>
    Extend the existing Anthropic-only analyze-stage prompt path so `LlmClassifier` accepts and renders primary graph-pair context containing `kunstmaanGraph` and `craftGraph`. The prompt must instruct the model to propose graph-to-graph mapping rows using stable source refs and target refs, relation intent values exactly `reference`, `promote`, `embed`, `drop`, `out_of_scope`, and target candidate evidence. Keep runtime-zero-AI outside analyze. In `AnalyzeController`, pass the graph pair to `LlmClassifier` before legacy markdown/context snippets; keep heuristic output only as deterministic candidate narrowing/safety hints and obvious exact-match proposals. Do not remove deterministic validation; LLM output remains proposed mapping input only.
  </action>
  <verify>
    vendor/bin/phpunit tests/unit/analyze/LlmClassifierGraphPromptTest.php tests/unit/console/AnalyzeControllerGraphArtifactsTest.php --testdox
  </verify>
  <acceptance_criteria>
    - `grep -R "kunstmaanGraph" src/analyze/LlmClassifier.php src/console/AnalyzeController.php` returns at least 2 matches.
    - `grep -R "craftGraph" src/analyze/LlmClassifier.php src/console/AnalyzeController.php` returns at least 2 matches.
    - `grep -R "reference.*promote.*embed.*drop.*out_of_scope\|out_of_scope.*drop.*embed.*promote.*reference" src/analyze/LlmClassifier.php` returns at least 1 match.
    - `grep -R "Anthropic" src/analyze/LlmClassifier.php` still returns existing Anthropic-only path matches.
    - `grep -R "LlmClassifier" src/load src/fields src/extract src/compile` returns 0 matches.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| LLM output becomes executable without deterministic validation. | High | Plan only changes analyze proposal input; compile validation remains required. |
| Runtime AI is accidentally introduced outside analyze. | High | Acceptance criteria reject `LlmClassifier` references under load/fields/extract/compile. |
| Content-only policy stays hidden in analyzer heuristics. | Medium | Analyzer must keep content-only fields factual and policy-free in graph output. |
</threat_model>

<verification>
Run `vendor/bin/phpunit tests/unit/console/AnalyzeControllerGraphArtifactsTest.php tests/unit/analyze/LlmClassifierGraphPromptTest.php --testdox`.
</verification>

<success_criteria>
- Analyze writes graph-shaped `kunstmaan-schema.json` and `craft-schema.json`.
- LLM prompt receives `kunstmaanGraph` and `craftGraph` as primary context.
- Existing deterministic heuristics remain helper/candidate surfaces, not the only source of ambiguous mapping truth.
</success_criteria>
