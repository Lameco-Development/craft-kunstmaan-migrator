---
phase: 11-relation-target-introspection-promotion
plan: 07
type: execute
wave: 4
depends_on: [11-04, 11-05, 11-06]
files_modified:
  - src/audit/PageRootedSurfaceDiscovery.php
  - src/audit/PageRootedCoverageAuditor.php
  - src/load/MigrationReport.php
  - tests/unit/audit/PageRootedCoverageAuditorGraphRelationsTest.php
  - tests/unit/audit/PageRootedSurfaceDiscoveryGraphRelationsTest.php
  - .planning/rehearsal/v1.0/cqm/README.md
autonomous: true
requirements: [PH11-01, PH11-02, PH11-03, PH11-04, PH11-05, PH11-06, PH11-07, PH11-08, PH11-09, PH11-10]
must_haves:
  truths:
    - Reporting distinguishes unresolved relation evidence from intentional drop/out_of_scope decisions.
    - Live release remains blocked when non-empty relation FK evidence has no accepted mapping/drop/out_of_scope decision.
    - Scoped NewsPage/HomePage rehearsal proves relation-heavy and pagepart/Matrix-heavy flows.
  artifacts:
    - path: src/audit/PageRootedSurfaceDiscovery.php
      provides: Graph-backed relation coverage discovery.
    - path: src/audit/PageRootedCoverageAuditor.php
      provides: Release-blocking relation classification.
    - path: src/load/MigrationReport.php
      provides: Relation unresolved/drop/out_of_scope/promoted report counters and rows.
    - path: .planning/rehearsal/v1.0/cqm/README.md
      provides: Scoped proof commands and expected evidence.
  key_links:
    - from: src/audit/PageRootedCoverageAuditor.php
      to: src/source/KunstmaanGraphContract.php
      via: Relation evidence and intent categories.
    - from: src/load/MigrationReport.php
      to: .planning/rehearsal/v1.0/cqm/README.md
      via: Runbook expected evidence rows.
    - from: .planning/rehearsal/v1.0/cqm/README.md
      to: CQM local storage/migration artifacts
      via: Manual scoped rehearsal commands.
---

# Plan 11-07: Verification/reporting and scoped rehearsal

<objective>
Add graph-backed relation coverage/reporting gates and document the scoped NewsPage/HomePage proof commands and expected evidence.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Add graph-backed relation coverage/reporting gates</name>
  <files>src/audit/PageRootedSurfaceDiscovery.php, src/audit/PageRootedCoverageAuditor.php, src/load/MigrationReport.php, tests/unit/audit/PageRootedCoverageAuditorGraphRelationsTest.php, tests/unit/audit/PageRootedSurfaceDiscoveryGraphRelationsTest.php</files>
  <read_first>
    .planning/phases/11-relation-target-introspection-promotion/11-CONTEXT.md
    .planning/phases/11-relation-target-introspection-promotion/11-PATTERNS.md
    src/audit/PageRootedSurfaceDiscovery.php
    src/audit/PageRootedCoverageAuditor.php
    src/load/MigrationReport.php
    src/source/KunstmaanGraphContract.php
    src/compile/GraphCompatibilityValidator.php
    .planning/phases/10-generic-migration-rehearsal-gap-closure/10-06-SUMMARY.md
  </read_first>
  <action>
    Extend Page-rooted coverage discovery/auditing to consume relation evidence from the Kunstmaan graph when available. Classify non-empty relation FK evidence as blocking/unresolved unless accepted mapping contains an explicit relation intent of `reference`, `promote`, `embed`, `drop`, or `out_of_scope`. Render intentional `drop` and `out_of_scope` as non-blocking intentional decisions, not unresolved gaps. Add `MigrationReport` counters/rows for `relation.unresolved`, `relation.intent.drop`, `relation.intent.out_of_scope`, and `relation.promoted`. Keep Phase 10 evidence-based behavior: do not recreate no-input synthetic blockers when graph lacks relation evidence.
  </action>
  <verify>
    vendor/bin/phpunit tests/unit/audit/PageRootedCoverageAuditorGraphRelationsTest.php tests/unit/audit/PageRootedSurfaceDiscoveryGraphRelationsTest.php --testdox
  </verify>
  <acceptance_criteria>
    - `grep -R "relation.unresolved\|relation.intent.drop\|relation.intent.out_of_scope\|relation.promoted" src/audit src/load tests/unit/audit` returns at least 4 matches.
    - `grep -R "out_of_scope" src/audit/PageRootedCoverageAuditor.php src/audit/PageRootedSurfaceDiscovery.php` returns at least 1 match.
    - `grep -R "not-discovered" tests/unit/audit/PageRootedSurfaceDiscoveryGraphRelationsTest.php` asserts graph absence does not create synthetic blockers.
    - `grep -R "NewsPage\|Employee" src/audit/PageRootedSurfaceDiscovery.php src/audit/PageRootedCoverageAuditor.php src/load/MigrationReport.php` returns 0 matches.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Document and verify scoped NewsPage/HomePage rehearsal commands</name>
  <files>.planning/rehearsal/v1.0/cqm/README.md, tests/unit/audit/PageRootedCoverageAuditorGraphRelationsTest.php</files>
  <read_first>
    .planning/phases/11-relation-target-introspection-promotion/11-CONTEXT.md
    .planning/phases/11-relation-target-introspection-promotion/11-PATTERNS.md
    .planning/rehearsal/v1.0/cqm/README.md
    .planning/phases/10-generic-migration-rehearsal-gap-closure/10-VERIFICATION.md
  </read_first>
  <action>
    Update the CQM rehearsal README with scoped proof commands for `NewsPage` and `HomePage` only. Include commands for analyze, compile, and dry-run/live rehearsal using existing CLI names and filters, for example `php craft kunstmaan-migrator/analyze --entities=NewsPage,HomePage`, `php craft kunstmaan-migrator/compile`, and the existing scoped migrate command form used in the repo. State expected artifact evidence: `kunstmaan-schema.json` has graphVersion and NewsPage employee/image/pagepart evidence; `craft-schema.json` has graphVersion and newsPage/homePage field/Matrix/asset/relation constraints; owner extracts keep `employee_id` raw; promoted Employee target has separate identity/state rows when mapping chooses promotion; HomePage pagepart/Matrix ownership is visible. Do not add broader project rollout commands beyond NewsPage/HomePage.
  </action>
  <verify>
    composer test
  </verify>
  <acceptance_criteria>
    - `grep -R "NewsPage,HomePage\|NewsPage\|HomePage" .planning/rehearsal/v1.0/cqm/README.md` returns at least 2 matches.
    - `grep -R "kunstmaan-schema.json" .planning/rehearsal/v1.0/cqm/README.md` returns at least 1 match.
    - `grep -R "craft-schema.json" .planning/rehearsal/v1.0/cqm/README.md` returns at least 1 match.
    - `grep -R "employee_id" .planning/rehearsal/v1.0/cqm/README.md` returns at least 1 match.
    - `grep -R "Simac\|Enreach" .planning/rehearsal/v1.0/cqm/README.md` does not add mandatory rehearsal commands for non-proof projects.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Unresolved relation evidence is reported as success. | High | Coverage rows distinguish unresolved from drop/out_of_scope/promoted and block live release. |
| Synthetic blockers reappear without graph evidence. | Medium | Tests assert no graph evidence means no synthetic blocker. |
| Rehearsal scope expands beyond the proof targets. | Low | Runbook acceptance criteria limit mandatory commands to NewsPage/HomePage. |
</threat_model>

<verification>
Run all focused Phase 11 test groups and then `composer test`. In CQM, run scoped proof commands from `.planning/rehearsal/v1.0/cqm/README.md`.
</verification>

<success_criteria>
- Reporting distinguishes unresolved relation evidence from intentional drop/out_of_scope decisions.
- Live release is blocked when relation FK evidence lacks a decision.
- The runbook documents scoped NewsPage/HomePage proof evidence without broadening the phase.
</success_criteria>
