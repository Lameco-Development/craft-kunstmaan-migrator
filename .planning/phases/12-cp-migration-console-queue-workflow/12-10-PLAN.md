---
phase: 12-cp-migration-console-queue-workflow
plan: 10
type: execute
wave: 6
depends_on: [12-04, 12-09]
files_modified:
  - templates/_console/index.twig
  - templates/_console/_tabs.twig
  - templates/_console/_readiness.twig
  - templates/_console/_analyze.twig
  - templates/_console/_mapping.twig
  - templates/_console/_compile.twig
  - templates/_console/_runs.twig
  - templates/_console/_run-detail.twig
  - templates/_console/_reports.twig
  - templates/_console/_danger-zone.twig
  - tests/unit/templates/ConsoleTemplateContractTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - Operators can navigate the CP console using the required tab order.
    - Every mutation form uses Craft CP POST, CSRF, and actionInput patterns.
    - Live controls are visually separated and disabled on failed/unverifiable gates; reset/cleanup controls are visibly deferred until exact deletion contracts exist.
  artifacts:
    - path: templates/_console/index.twig
      provides: Main CP console shell
      contains: Kunstmaan Migration Console
    - path: templates/_console/_danger-zone.twig
      provides: Deferred reset/cleanup panels and live safety controls
      contains: DELETE ARTIFACTS
  key_links:
    - from: templates/_console/_analyze.twig
      to: MigrationConsoleController::actionQueueAnalyze
      via: actionInput
      pattern: migration-console/queue-analyze
    - from: templates/_console/_runs.twig
      to: MigrationRunService
      via: runs variable
      pattern: Run ID
---

# Plan 12-10: CP-native console templates

<objective>
Build Craft CP-native tabbed templates for readiness, analyze, mapping, compile, runs, reports, and danger zone using the approved Phase 12 UI design contract.
</objective>

<tasks>
<task type="auto">
  <name>Build console shell, tabs, readiness, analyze, and compile templates</name>
  <files>templates/_console/index.twig, templates/_console/_tabs.twig, templates/_console/_readiness.twig, templates/_console/_analyze.twig, templates/_console/_compile.twig, tests/unit/templates/ConsoleTemplateContractTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-UI-SPEC.md
    templates/_mapping/index.twig
    src/controllers/MigrationConsoleController.php
  </read_first>
  <action>
    Expand the minimal shell from Plan 12-09 into Craft CP-native Twig templates following `templates/_mapping/index.twig` and `src/templates/_settings.twig` patterns. Use title `Kunstmaan Migration Console` and exact subtitle `Review readiness, mapping coverage, queued runs, logs, and artifacts for the Kunstmaan → Craft migration. CLI remains the canonical workflow.` Tabs in this order: `Readiness`, `Analyze`, `Mapping`, `Compile`, `Runs`, `Reports`, `Danger Zone`. Readiness shows `Environment`, `Connectivity`, `Mapping & Compile`, `Queue`, and `Latest run` cards with statuses `Passed`, `Warning`, `Blocked`, and `Unknown`, and fields for environment name, production status, admin status, elevated session status, legacy DB reachability, Anthropic API key presence, Kunstmaan source path validity, `storage/migration` writability, mapping file path, mapping coverage summary, unsupported/warning row counts, latest compile timestamp, fatal compile warning count, queue component availability, queue worker/readiness status, and latest run details. Analyze form posts to `kunstmaan-migrator/migration-console/queue-analyze` and includes entity, locale, since filters plus checkbox copy `I understand analyze may send schema and mapping context to Anthropic.` If API key presence gate fails, show exact disabled copy `Analyze is unavailable because no Anthropic API key is configured. Add an API key in plugin settings or run the CLI with an approved environment.` Compile tab renders latest compile timestamp, status, fatal warnings, warning count, artifact paths, link to logs/report, compile readiness, equivalent CLI command, and a form posting to `kunstmaan-migrator/migration-console/queue-compile`. Use `csrfInput()` and `actionInput(...)` for all mutation forms.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/templates/ConsoleTemplateContractTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "Kunstmaan Migration Console\\|Kunstmaan → Craft\\|Readiness\\|Analyze\\|Mapping\\|Compile\\|Runs\\|Reports\\|Danger Zone\\|queue-analyze\\|queue-compile\\|csrfInput()\\|actionInput\\|I understand analyze may send schema and mapping context to Anthropic." templates/_console tests/unit/templates/ConsoleTemplateContractTest.php` returns at least 14 matches.
    - `grep -R "Environment\\|Connectivity\\|Mapping & Compile\\|Queue\\|Latest run\\|storage/migration\\|latest compile\\|fatal warnings\\|equivalent CLI" templates/_console tests/unit/templates/ConsoleTemplateContractTest.php` returns at least 9 matches.
  </acceptance_criteria>
  <done>
    Console shell, tab navigation, readiness fields, analyze form, and compile summary/readiness UI render with exact CP-native copy and form patterns.
  </done>
</task>

<task type="auto">
  <name>Build mapping, runs, reports, and run-detail templates</name>
  <files>templates/_console/_mapping.twig, templates/_console/_runs.twig, templates/_console/_run-detail.twig, templates/_console/_reports.twig, tests/unit/templates/ConsoleTemplateContractTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-UI-SPEC.md
    templates/_mapping/index.twig
    src/controllers/MigrationConsoleController.php
  </read_first>
  <action>
    `_mapping.twig` includes or adapts the improved mapping review from plan 12-04 inside the Mapping tab and includes mapping empty heading `No mapping rows found`, mapping empty body `Run analyze and compile first, then return here to review mapping coverage.`, and filter empty body `No rows match the current filters. Clear filters to view all mapping rows.` `_runs.twig` renders table columns exactly `Run ID`, `Stage`, `Mode`, `Status`, `Filters`, `Initiated by`, `Queue job IDs`, `Progress`, `Started`, `Finished`, `Artifacts`, and `Actions`. Row actions must include `View details`, `View log`, `View artifacts`, `Copy CLI command`, and `Retry safe stage`. Empty state heading is exactly `No migration runs yet` and body is exactly `Queue a dry run after readiness checks pass, or run the equivalent CLI command from your terminal.` `_run-detail.twig` renders gate snapshot, filters/options, initiating admin, queue job IDs, timestamps, log excerpt, log empty body `No log lines have been written for this run yet. Refresh after the queue worker starts processing.`, artifacts, artifact empty body `No artifacts have been recorded for this run yet. Artifacts are written under storage/migration.`, failure details, readable summary, and raw JSON inside `<details>`. `_reports.twig` lists `REPORT.md`, `VERIFY-`, `PAGE-ROOTED-COVERAGE.md`, `MAPPING-AUDIT.md`, `kunstmaan-schema.json`, `craft-schema.json`, and artifact paths. Add a CP-native queued verify/report form with CTA `Queue verify/report`, `csrfInput()`, and `actionInput('kunstmaan-migrator/migration-console/queue-verify')`, and show that progress will appear in the current run record.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/templates/ConsoleTemplateContractTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "Run ID\\|Stage\\|Mode\\|Status\\|Filters\\|Initiated by\\|Queue job IDs\\|Progress\\|Started\\|Finished\\|Artifacts\\|Actions\\|View details\\|View log\\|View artifacts\\|Copy CLI command\\|Retry safe stage\\|No migration runs yet\\|REPORT.md\\|VERIFY-\\|PAGE-ROOTED-COVERAGE.md\\|MAPPING-AUDIT.md\\|kunstmaan-schema.json\\|craft-schema.json\\|<details\\|Queue verify/report\\|queue-verify" templates/_console tests/unit/templates/ConsoleTemplateContractTest.php` returns at least 27 matches.
    - `grep -R "No log lines have been written\\|No artifacts have been recorded\\|No mapping rows found\\|Run analyze and compile first\\|No rows match the current filters" templates/_console tests/unit/templates/ConsoleTemplateContractTest.php` returns at least 5 matches.
  </acceptance_criteria>
  <done>
    Mapping, run list/detail, and report templates expose the exact required empty states, plural queue job IDs, row actions, artifact/log copy, and report artifacts.
  </done>
</task>

<task type="auto">
  <name>Build dry-run/live controls and Danger Zone</name>
  <files>templates/_console/_readiness.twig, templates/_console/_runs.twig, templates/_console/_danger-zone.twig, tests/unit/templates/ConsoleTemplateContractTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-UI-SPEC.md
    src/controllers/MigrationConsoleController.php
    src/safety/MigrationGateService.php
  </read_first>
  <action>
    Add dry-run CTA `Queue dry run` posting to `queue-dry-run` when dry-run gates pass. Dry-run gate UI must show gates `non-production`, `admin`, `mapping-file-exists`, `recent-compile-exists`, `no-fatal-compile-warnings`, `filters-options-valid`, and `queue-can-accept-jobs`. Add visually separated live panel with destructive styling, disabled when any gate is `blocked` or `unknown`, error copy `This action cannot start because one or more safety gates failed. Review the failed gates below, fix the issue, then try again or use the shown CLI command.`, queue unavailable copy `Queue readiness could not be verified. Live migration is blocked from the Control Panel; use the CLI after confirming a worker is running.`, backup checkbox text exactly `I have a restorable database and asset backup for this environment.`, mapping warning/unsupported acceptance checkbox named `warningsAccepted` with exact copy `I understand selected warning/unsupported mapping rows may be skipped, dropped, or migrated imperfectly.`, typed phrase helper `MIGRATE LIVE`, and action `queue-live`. Danger Zone must not render active reset/cleanup submit forms in Phase 12; instead show disabled/remediation panels for reset and cleanup with phrases `RESET MIGRATION STATE` and `DELETE ARTIFACTS` labeled as deferred until deletion boundaries are specified. Use only `km-console-` custom classes and Craft CP classes. Do not add shadcn, Vite, npm, or a CSS build pipeline.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/templates/ConsoleTemplateContractTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "Queue dry run\\|Queue live migration\\|MIGRATE LIVE\\|I have a restorable database and asset backup for this environment.\\|I understand selected warning/unsupported mapping rows\\|warningsAccepted\\|RESET MIGRATION STATE\\|DELETE ARTIFACTS\\|km-console-" templates/_console tests/unit/templates/ConsoleTemplateContractTest.php` returns at least 9 matches.
    - `grep -R "mapping-file-exists\\|recent-compile-exists\\|no-fatal-compile-warnings\\|filters-options-valid\\|queue-can-accept-jobs\\|This action cannot start because one or more safety gates failed\\|Queue readiness could not be verified\\|deferred until deletion boundaries are specified" templates/_console tests/unit/templates/ConsoleTemplateContractTest.php` returns at least 8 matches.
    - `grep -R "shadcn\\|vite\\|npm" templates/_console` returns 0 matches.
  </acceptance_criteria>
  <done>
    Dry-run/live controls render every required gate, live blocks failed/unknown gates with exact remediation copy, and Danger Zone destructive reset/cleanup remains disabled/deferred rather than unsafe.
  </done>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Operator cannot distinguish blocked, warning, and ready states. | Medium | Status text labels and CP-native notices/cards for each gate group. |
| Destructive actions lack exact deletion contracts. | High | Reset/cleanup controls are disabled/deferred in Phase 12 until exact deletion boundaries and tests exist. |
| Mutation forms bypass Craft CSRF/action routing. | High | Every mutation form uses POST, `csrfInput()`, and `actionInput(...)`. |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/unit/templates/ConsoleTemplateContractTest.php --testdox`
</verification>

<success_criteria>
- Required tab order and CP-native layout are implemented.
- Runs, reports, and danger zone are first-class console surfaces.
- Live controls are gated, reset/cleanup controls are visibly deferred, and no frontend build pipeline is introduced.
</success_criteria>
