---
phase: 12-cp-migration-console-queue-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/migrations/m260429_000001_create_migration_runs.php
  - src/records/MigrationRunRecord.php
  - src/runs/MigrationRunService.php
  - tests/unit/runs/MigrationRunServiceTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - Operators can see migration runs independently of Craft's generic queue UI.
    - Each queued stage has a durable run record with status, filters, options, logs, artifacts, summary, and failure details.
    - Run logs and artifacts remain inspectable under storage/migration outside the CP.
  artifacts:
    - path: src/migrations/m260429_000001_create_migration_runs.php
      provides: kunstmaanmigrator_runs table schema
      contains: kunstmaanmigrator_runs
    - path: src/records/MigrationRunRecord.php
      provides: ActiveRecord for run rows
      contains: MigrationRunRecord
    - path: src/runs/MigrationRunService.php
      provides: Run creation, progress, success, failure, artifact APIs
      contains: createRun
  key_links:
    - from: src/runs/MigrationRunService.php
      to: "{{%kunstmaanmigrator_runs}}"
      via: MigrationRunRecord
      pattern: MigrationRunRecord
---

# Plan 12-01: Run records and file-backed artifacts

<objective>
Add first-class migration run records so CP and queued work can show durable status, progress, logs, artifacts, summaries, and failures without relying only on Craft's generic queue UI.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Create the migration run schema and record</name>
  <files>src/migrations/m260429_000001_create_migration_runs.php, src/records/MigrationRunRecord.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    .planning/phases/12-cp-migration-console-queue-workflow/12-PATTERNS.md
    src/migrations/Install.php
    src/load/MigrationStateService.php
  </read_first>
  <action>
    Create migration `m260429_000001_create_migration_runs` following `src/migrations/Install.php` idempotent migration style. It must create `{{%kunstmaanmigrator_runs}}` with columns `id`, `stage`, `mode`, `status`, `filters` JSON, `options` JSON, `gateSnapshot` JSON nullable, `initiatedByUserId` integer nullable, `queueJobId` string nullable for the first/current job, `queueJobIds` JSON nullable for staged/batched job IDs, `progress` float or decimal, `logPath` string nullable, `artifactPaths` JSON nullable, `summary` JSON nullable, `failure` text or JSON nullable, `dateStarted` nullable, `dateFinished` nullable, `dateCreated`, and `dateUpdated`. Add indexes for `status`, combined `stage, mode`, `queueJobId`, `dateCreated`, and `dateUpdated`. Preserve rows in `safeDown()`. Create `MigrationRunRecord` with `tableName()` returning `{{%kunstmaanmigrator_runs}}`.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/runs/MigrationRunServiceTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "kunstmaanmigrator_runs" src/migrations/m260429_000001_create_migration_runs.php src/records/MigrationRunRecord.php` returns at least 2 matches.
    - `grep -R "gateSnapshot\|artifactPaths\|queueJobId\|queueJobIds\|dateFinished" src/migrations/m260429_000001_create_migration_runs.php src/records/MigrationRunRecord.php` returns at least 5 matches.
    - `grep -R "function safeDown" src/migrations/m260429_000001_create_migration_runs.php` returns 1 match.
  </acceptance_criteria>
  <done>
    Run table and record class exist, preserve run data, and can store both current `queueJobId` and plural `queueJobIds` for staged/batched queue execution.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Create MigrationRunService lifecycle APIs</name>
  <files>src/runs/MigrationRunService.php, tests/unit/runs/MigrationRunServiceTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    .planning/phases/12-cp-migration-console-queue-workflow/12-PATTERNS.md
    src/load/MigrationStateService.php
    src/records/MigrationRunRecord.php
  </read_first>
  <action>
    Implement `MigrationRunService` following `src/load/MigrationStateService.php` service/repository style with methods `createRun(string $stage, string $mode, array $filters, array $options, ?int $initiatedByUserId, ?array $gateSnapshot = null): MigrationRunRecord`, `markQueued(int $id, string|int $queueJobId): void`, `appendQueueJobId(int $id, string|int $queueJobId): void`, `markRunning(int $id): void`, `updateProgress(int $id, float $progress, ?array $summary = null): void`, `markSucceeded(int $id, array $summary = [], array $artifactPaths = []): void`, `markFailed(int $id, string $message, array $failure = []): void`, `latest(?string $stage = null, ?string $mode = null): ?array`, `find(int $id): ?array`, `list(int $limit = 50): array`, and `appendArtifact(int $id, string $path): void`. Use statuses exactly `draft`, `queued`, `running`, `succeeded`, `failed`, `cancelled`, and `blocked`. `markQueued()` stores the first/current job in `queueJobId` and appends the same ID to `queueJobIds`; `appendQueueJobId()` appends later chained/batched job IDs without overwriting earlier IDs.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/runs/MigrationRunServiceTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "function createRun\|function markQueued\|function appendQueueJobId\|function markRunning\|function updateProgress\|function markSucceeded\|function markFailed\|function appendArtifact" src/runs/MigrationRunService.php` returns at least 8 matches.
    - `grep -R "draft\|queued\|running\|succeeded\|failed\|cancelled\|blocked" src/runs/MigrationRunService.php tests/unit/runs/MigrationRunServiceTest.php` returns at least 7 matches.
  </acceptance_criteria>
  <done>
    `MigrationRunService` exposes lifecycle, progress, artifact, and plural queue job ID APIs needed by staged jobs and CP run detail views.
  </done>
</task>

<task type="auto">
  <name>Add lifecycle and artifact contract tests</name>
  <files>tests/unit/runs/MigrationRunServiceTest.php</files>
  <read_first>
    tests/unit/mapping/MappingFileTest.php
    tests/unit/load/MigrationStateServiceTerminalStateTest.php
    src/runs/MigrationRunService.php
    src/records/MigrationRunRecord.php
  </read_first>
  <action>
    Add tests or source-level assertions proving the table name, lifecycle method names, status strings, JSON field names, plural `queueJobIds`, and artifact/log path fields exist. If a full Craft DB is unavailable in unit tests, use reflection/source assertions rather than bootstrapping Craft.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/runs/MigrationRunServiceTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "storage/migration\|artifactPaths\|logPath\|gateSnapshot\|queueJobIds" tests/unit/runs/MigrationRunServiceTest.php` returns at least 5 matches.
  </acceptance_criteria>
  <done>
    Run record tests lock lifecycle statuses, table/field names, log/artifact paths, and plural queue job ID storage.
  </done>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Run history can be repudiated or lost. | High | Persist initiating admin ID, queue job ID, timestamps, gate snapshot, artifacts, summary, and failure. |
| Run records become destructive uninstall collateral. | Medium | Preserve rows in `safeDown()` unless explicitly reset by an operator action. |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/unit/runs/MigrationRunServiceTest.php --testdox`
</verification>

<success_criteria>
- `{{%kunstmaanmigrator_runs}}` schema exists and is idempotent.
- `MigrationRunService` exposes all lifecycle methods needed by queue jobs and CP views.
- Run records include filters, options, gate snapshots, progress, logs, artifacts, summaries, and failures.
</success_criteria>
