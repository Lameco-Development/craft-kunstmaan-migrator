---
phase: 12-cp-migration-console-queue-workflow
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/Settings.php
  - src/templates/_settings.twig
  - tests/unit/models/SettingsBeforeValidateTest.php
  - tests/unit/Plugin/SettingsHtmlTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - CP Settings expose stable, site-safe options only.
    - Advanced project-shape hints remain configurable through config/kunstmaan-migrator.php, not CP Settings.
    - Settings UI remains concise.
  artifacts:
    - path: src/templates/_settings.twig
      provides: Concise CP Settings fragment
      contains: Execution
    - path: src/models/Settings.php
      provides: Queue/execution and retention defaults
      contains: allowCpQueueActions
  key_links:
    - from: src/templates/_settings.twig
      to: src/models/Settings.php
      via: field names
      pattern: allowCpQueueActions
---

# Plan 12-05: Stable CP Settings boundary

<objective>
Keep plugin settings useful for safe site-level options while keeping advanced project-shape hints in config files instead of turning CP settings into a migration mapper.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Add stable execution and retention settings fields</name>
  <files>src/models/Settings.php, tests/unit/models/SettingsBeforeValidateTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    src/models/Settings.php
    src/templates/_settings.twig
  </read_first>
  <action>
    Add public settings `bool $allowCpQueueActions = true`, `bool $allowCpLiveQueueAction = false`, `int $runRecordRetentionDays = 30`, `int $artifactRetentionDays = 30`, and `array $defaultFilters = []`. Keep advanced fields `genericContentBlockOverrides` and `relationMirrorRules` as model/config properties but do not expose them in Twig. Keep optional adapter toggles `seoEnabled` and `retourEnabled` as stable site-safe CP settings.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/models/SettingsBeforeValidateTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "allowCpQueueActions\|allowCpLiveQueueAction\|runRecordRetentionDays\|artifactRetentionDays\|defaultFilters" src/models/Settings.php` returns at least 5 matches.
    - `grep -R "genericContentBlockOverrides\|relationMirrorRules" src/models/Settings.php` returns at least 2 matches.
  </acceptance_criteria>
  <done>
    Settings model exposes stable CP queue, live, retention, filter, and adapter settings while keeping advanced project-shape settings config-only.
  </done>
</task>

<task type="auto">
  <name>Slim CP Settings Twig to stable site-safe groups</name>
  <files>src/templates/_settings.twig, tests/unit/Plugin/SettingsHtmlTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    .planning/phases/12-cp-migration-console-queue-workflow/12-UI-SPEC.md
    src/templates/_settings.twig
    src/models/Settings.php
  </read_first>
  <action>
    Organize CP Settings into groups exactly `Connectivity`, `Mapping`, `Execution`, `Adapters`, and `Retention`, following `src/templates/_settings.twig` Craft `_includes/forms` macro style. Keep fields for legacy DB connection, Anthropic key, Kunstmaan source path, mapping path, locale map, default filters, `joinFkRelations`, adapter toggles, CP queue allowance, CP live queue allowance, run retention, and artifact retention. Remove CP inputs for `genericContentBlockOverrides` and `relationMirrorRules`; those remain config-only. Do not add settings tabs or a mapping-authoring product.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/Plugin/SettingsHtmlTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "Execution\\|Adapters\\|allowCpQueueActions\\|allowCpLiveQueueAction\\|runRecordRetentionDays\\|artifactRetentionDays" src/templates/_settings.twig` returns at least 6 matches.
    - `grep -R "genericContentBlockOverrides\\|relationMirrorRules" src/templates/_settings.twig` returns 0 matches.
  </acceptance_criteria>
  <done>
    Settings Twig presents only stable site-safe groups and contains no CP fields for advanced project-shape hints.
  </done>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| CP settings become project-specific and harm portability. | Medium | Expose only stable site-safe options; keep shape hints config-only. |
| Live queue action is enabled accidentally. | High | Default `allowCpLiveQueueAction` to `false`. |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/unit/models/SettingsBeforeValidateTest.php --testdox`
- `vendor/bin/phpunit tests/unit/Plugin/SettingsHtmlTest.php --testdox`
</verification>

<success_criteria>
- Stable CP queue, live, retention, adapter, and filter settings exist.
- Advanced project-shape hints remain absent from `_settings.twig`.
- Settings UI stays concise and Craft CP native.
</success_criteria>
