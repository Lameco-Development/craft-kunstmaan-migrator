---
phase: 12-cp-migration-console-queue-workflow
plan: 06
type: execute
wave: 2
depends_on: [12-01, 12-05]
files_modified:
  - src/safety/MigrationSafety.php
  - src/safety/GateResult.php
  - src/safety/MigrationGateService.php
  - tests/unit/safety/MigrationSafetyTest.php
  - tests/unit/safety/MigrationGateServiceTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - Production is blocked independently for CP controllers and queue jobs.
    - Live migration cannot be queued unless every mandatory gate passes.
    - Unverifiable live gates block CP live execution and show CLI remediation.
  artifacts:
    - path: src/safety/MigrationSafety.php
      provides: CP/job production hard-block helper
      contains: assertNotProductionForJob
    - path: src/safety/MigrationGateService.php
      provides: Structured readiness and live gate evaluation
      contains: liveGates
  key_links:
    - from: src/safety/MigrationGateService.php
      to: src/runs/MigrationRunService.php
      via: dry-run and compile history checks
      pattern: MigrationRunService
---

# Plan 12-06: Safety and live gates

<objective>
Create CP/job-safe production guards and structured migration gates so readiness, analyze, compile, dry-run, and live actions can render and enforce the exact Phase 12 safety contract.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Create CP and job safe production helper</name>
  <files>src/safety/MigrationSafety.php, tests/unit/safety/MigrationSafetyTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    .planning/phases/12-cp-migration-console-queue-workflow/12-PATTERNS.md
    src/NeverProductionTrait.php
  </read_first>
  <action>
    Create `MigrationSafety` with methods `isProduction(): bool`, `environmentName(): string`, `assertNotProductionForCp(): void`, and `assertNotProductionForJob(): void`. Use `CRAFT_ENVIRONMENT === 'production'` semantics compatible with `NeverProductionTrait`. Exception messages must include exactly `Migration actions are disabled in production. Run this workflow only from a development or staging Craft environment.`
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/safety/MigrationSafetyTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "function isProduction\|function environmentName\|function assertNotProductionForCp\|function assertNotProductionForJob" src/safety/MigrationSafety.php` returns 4 matches.
    - `grep -R "Migration actions are disabled in production. Run this workflow only from a development or staging Craft environment." src/safety/MigrationSafety.php tests/unit/safety/MigrationSafetyTest.php` returns at least 1 match.
  </acceptance_criteria>
  <done>
    CP and queue contexts can share a production hard-block helper with the exact operator-facing production refusal copy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Create structured GateResult value object</name>
  <files>src/safety/GateResult.php, tests/unit/safety/MigrationGateServiceTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    .planning/phases/12-cp-migration-console-queue-workflow/12-UI-SPEC.md
  </read_first>
  <action>
    Create `GateResult` with fields `id`, `label`, `status`, `severity`, `message`, `remediation`, `cliCommand`, and `blocking`. Allowed statuses are exactly `passed`, `warning`, `blocked`, and `unknown`. Live gate `unknown` must always be blocking. Provide `toArray(): array` with all fields.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/safety/MigrationGateServiceTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "passed\|warning\|blocked\|unknown\|toArray\|cliCommand" src/safety/GateResult.php tests/unit/safety/MigrationGateServiceTest.php` returns at least 6 matches.
  </acceptance_criteria>
  <done>
    Gate results expose deterministic array data for status, severity, messages, remediation, CLI commands, and blocking behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Implement readiness, dry-run, analyze, compile, and live gates</name>
  <files>src/safety/MigrationGateService.php, tests/unit/safety/MigrationGateServiceTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    .planning/phases/12-cp-migration-console-queue-workflow/12-UI-SPEC.md
    src/runs/MigrationRunService.php
    src/models/Settings.php
    src/mapping/MappingFile.php
  </read_first>
  <action>
    Implement `readiness(): array`, `analyzeGates(bool $aiConfirmed): array`, `compileGates(): array`, `dryRunGates(array $filters, array $options): array`, and `liveGates(array $filters, array $options, bool $backupAcknowledged, string $typedPhrase, bool $warningsAccepted): array`. `readiness()` must return checks for IDs exactly `environment-name`, `production-status`, `admin-status`, `elevated-session-status`, `legacy-db-reachability`, `anthropic-api-key-presence`, `kunstmaan-source-path-validity`, `storage-migration-writable`, `mapping-file-path`, `mapping-coverage-summary`, `unsupported-warning-row-counts`, `latest-compile-timestamp`, `fatal-compile-warning-count`, `queue-component-availability`, `queue-worker-readiness`, `latest-run-details`, `cp-queue-actions-enabled`, and `cp-live-queue-action-enabled`. `dryRunGates()` must include IDs exactly `non-production`, `admin`, `mapping-file-exists`, `recent-compile-exists`, `no-fatal-compile-warnings`, `filters-options-valid`, `queue-can-accept-jobs`, and `cp-queue-actions-enabled`. `compileGates()` must expose latest compile fields `latest-compile-timestamp`, `compile-status`, `fatalWarningCount`, `warningCount`, `artifactPaths`, `logReportLink`, and `compileReadiness`. Live gates must include IDs exactly `non-production`, `admin`, `elevated-session`, `typed-confirmation`, `successful-dry-run-same-options`, `recent-compile-no-fatals`, `queue-worker-readiness`, `backup-acknowledgement`, `mapping-coverage`, `warning-unsupported-acceptance`, `cp-live-queue-action-enabled`, and `job-production-hard-block`. Required live phrase is exactly `MIGRATE LIVE`. Read settings `allowCpQueueActions` and `allowCpLiveQueueAction`; any CP queue action gate blocks when `allowCpQueueActions === false`, and live gates block when `allowCpLiveQueueAction === false`. Queue readiness `unknown` must block live and use remediation copy exactly `Queue readiness could not be verified. Live migration is blocked from the Control Panel; use the CLI after confirming a worker is running.`
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/safety/MigrationGateServiceTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "non-production\|admin\|elevated-session\|typed-confirmation\|successful-dry-run-same-options\|recent-compile-no-fatals\|queue-worker-readiness\|backup-acknowledgement\|mapping-coverage\|job-production-hard-block" src/safety/MigrationGateService.php tests/unit/safety/MigrationGateServiceTest.php` returns at least 10 matches.
    - `grep -R "environment-name\|legacy-db-reachability\|anthropic-api-key-presence\|storage-migration-writable\|latest-compile-timestamp\|fatal-compile-warning-count\|mapping-file-exists\|recent-compile-exists\|queue-can-accept-jobs" src/safety/MigrationGateService.php tests/unit/safety/MigrationGateServiceTest.php` returns at least 9 matches.
    - `grep -R "allowCpQueueActions\|allowCpLiveQueueAction\|cp-queue-actions-enabled\|cp-live-queue-action-enabled\|warning-unsupported-acceptance" src/safety/MigrationGateService.php tests/unit/safety/MigrationGateServiceTest.php` returns at least 5 matches.
    - `grep -R "MIGRATE LIVE\|Queue readiness could not be verified" src/safety/MigrationGateService.php tests/unit/safety/MigrationGateServiceTest.php` returns at least 2 matches.
  </acceptance_criteria>
  <done>
    Gate service enumerates every UI-SPEC readiness field, dry-run gate, compile summary/readiness field, settings gate, warning acceptance gate, and live safety gate with deterministic IDs and blocking semantics.
  </done>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Production execution bypasses console-only trait. | Critical | CP/job-safe `MigrationSafety` rechecks production outside console controllers. |
| Live migration starts with stale or unverifiable readiness. | Critical | `liveGates()` blocks any failed or unknown gate. |
| Operator cannot remediate a blocked gate. | Medium | Gate results include `remediation` and `cliCommand` fields. |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/unit/safety/MigrationSafetyTest.php --testdox`
- `vendor/bin/phpunit tests/unit/safety/MigrationGateServiceTest.php --testdox`
</verification>

<success_criteria>
- Production safety works for CP controllers and queue jobs.
- Live gates include every locked Phase 12 safety gate.
- Unknown queue readiness blocks CP live migration with CLI remediation copy.
</success_criteria>
