---
phase: 12-cp-migration-console-queue-workflow
plan: 07
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03, 12-06]
files_modified:
  - src/Plugin.php
  - tests/integration/PluginConsoleRegistrationTest.php
  - tests/integration/PluginBootstrapTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - Phase 12 services are available as plugin components.
    - Existing Craft Utility registration pattern remains unchanged.
    - No top-level CP section is registered.
  artifacts:
    - path: src/Plugin.php
      provides: Component registration and DI wiring
      contains: migrationRunService
  key_links:
    - from: src/Plugin.php
      to: src/runs/MigrationRunService.php
      via: component
      pattern: migrationRunService
---

# Plan 12-07: Plugin wiring

<objective>
Register and wire the Phase 12 services in the plugin while preserving the existing Utility-only CP surface and avoiding a top-level CP section.
</objective>

<tasks>
<task type="auto">
  <name>Register Phase 12 service components</name>
  <files>src/Plugin.php, tests/integration/PluginBootstrapTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-PATTERNS.md
    src/Plugin.php
  </read_first>
  <action>
    Add `use` imports and `@property-read` docblocks for `MigrationRunService`, `MigrationSafety`, `MigrationGateService`, `AnalyzeWorkflow`, `CompileWorkflow`, `MigrateWorkflow`, and `VerifyWorkflow`. Add component IDs exactly `migrationRunService`, `migrationSafety`, `migrationGateService`, `analyzeWorkflow`, `compileWorkflow`, `migrateWorkflow`, and `verifyWorkflow` to `Plugin::config()`.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/integration/PluginBootstrapTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "migrationRunService\|migrationSafety\|migrationGateService\|analyzeWorkflow\|compileWorkflow\|migrateWorkflow\|verifyWorkflow" src/Plugin.php` returns at least 7 matches.
    - `grep -R "@property-read.*MigrationRunService\|@property-read.*MigrationGateService" src/Plugin.php` returns at least 2 matches.
  </acceptance_criteria>
  <done>
    Plugin config declares all Phase 12 service components and docblocks expose them as plugin properties.
  </done>
</task>

<task type="auto">
  <name>Wire sibling dependencies without adding a CP section</name>
  <files>src/Plugin.php, tests/integration/PluginConsoleRegistrationTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    .planning/phases/12-cp-migration-console-queue-workflow/12-PATTERNS.md
    src/Plugin.php
  </read_first>
  <action>
    In `Plugin::init()`, wire public sibling dependencies needed by workflows and gates, including `migrationGateService->migrationRunService`, `migrationGateService->mappingFile`, `migrationGateService->settings`, and workflow dependencies to existing services where the service classes require them. Preserve `Utilities::EVENT_REGISTER_UTILITIES` registration of `KunstmaanMappingUtility`. Do not add `EVENT_REGISTER_CP_NAV_ITEMS`, `Cp::EVENT_REGISTER_CP_NAV_ITEMS`, or any top-level CP section registration.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/integration/PluginConsoleRegistrationTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "migrationGateService->migrationRunService\|KunstmaanMappingUtility::class" src/Plugin.php` returns at least 2 matches.
    - `grep -R "EVENT_REGISTER_CP_NAV_ITEMS\|Cp::EVENT_REGISTER_CP_NAV_ITEMS" src/Plugin.php` returns 0 matches.
  </acceptance_criteria>
  <done>
    Phase 12 services are wired as plugin siblings while the existing Utility registration remains the only CP entry point.
  </done>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Phase 12 accidentally adds a broad CP product surface. | Medium | Preserve Utility registration and reject CP nav event registration. |
| Services are available in CLI but not queue/CP runtime. | High | Register services as plugin components and wire dependencies in `init()`. |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/integration/PluginBootstrapTest.php --testdox`
- `vendor/bin/phpunit tests/integration/PluginConsoleRegistrationTest.php --testdox`
</verification>

<success_criteria>
- All Phase 12 services are registered as plugin components.
- Utility registration remains the CP entry point.
- No top-level CP section/nav is registered.
</success_criteria>
