---
phase: 12-cp-migration-console-queue-workflow
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workflow/MigrateWorkflow.php
  - src/workflow/VerifyWorkflow.php
  - src/console/MigrateController.php
  - src/console/VerifyController.php
  - tests/unit/workflow/MigrateWorkflowTest.php
  - tests/unit/workflow/VerifyWorkflowTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - Queued dry-run and live jobs reuse the same migrate behavior as CLI.
    - Verify/report generation can run as a queued safe stage without inline CP execution.
    - Production hard-block remains present in CLI and is rechecked in queue jobs later.
  artifacts:
    - path: src/workflow/MigrateWorkflow.php
      provides: Shared migrate orchestration
      contains: MigrateWorkflow
    - path: src/workflow/VerifyWorkflow.php
      provides: Shared verify/report orchestration
      contains: VERIFY-
  key_links:
    - from: src/console/MigrateController.php
      to: src/workflow/MigrateWorkflow.php
      via: controller adapter
      pattern: MigrateWorkflow
    - from: src/console/VerifyController.php
      to: src/workflow/VerifyWorkflow.php
      via: controller adapter
      pattern: VerifyWorkflow
---

# Plan 12-03: Migrate and Verify workflow extraction

<objective>
Extract migrate and verify/report orchestration into shared services while preserving CLI-canonical dry-run/live semantics, logs, reports, filters, and safety behavior.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Extract migrate orchestration into MigrateWorkflow</name>
  <files>src/workflow/MigrateWorkflow.php, src/console/MigrateController.php, tests/unit/workflow/MigrateWorkflowTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    .planning/phases/12-cp-migration-console-queue-workflow/12-PATTERNS.md
    src/console/MigrateController.php
    src/load/MigrationOptions.php
    src/load/MigrationReport.php
  </read_first>
  <action>
    Create `MigrateWorkflow::run(array $options, ?callable $progress = null): array` while preserving orchestration from `src/console/MigrateController.php`, `src/load/MigrationOptions.php`, and `src/load/MigrationReport.php`. Supported option keys are exactly `live`, `confirm`, `preloadAssets`, `force`, `entities`, `locales`, `since`, `noSeo`, `noRetour`, `noRelJoin`, `includeUnreferencedTaxonomies`, `limit`, `onlyId`, `verbose`, `batchOffset`, and `batchLimit`. Preserve dry-run default, live behavior, report/log writing, compiled mapping preflight, and existing production refusal in the CLI controller. Return keys `status`, `mode` (`dryRun` or `live`), `filters`, `artifactPaths`, `logPath`, `summary`, `failure`, `processed`, `total`, and `nextBatchOffset`. `batchOffset` and `batchLimit` are required for Plan 12-08 staged/batched queue execution; CLI can pass `null` to run the full existing flow.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/workflow/MigrateWorkflowTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "class MigrateWorkflow\|function run\|dryRun\|live\|logPath\|artifactPaths\|preflightCompiledMapping\|batchOffset\|batchLimit\|nextBatchOffset" src/workflow/MigrateWorkflow.php src/console/MigrateController.php` returns at least 10 matches.
    - `grep -R "MigrateWorkflow" src/console/MigrateController.php` returns at least 1 match.
    - `grep -R "shell_exec\|php craft" src/workflow/MigrateWorkflow.php` returns 0 matches.
  </acceptance_criteria>
  <done>
    CLI migrate delegates to `MigrateWorkflow`, workflow supports existing full-run behavior plus batch offsets/limits for queued staged execution.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Extract verify/report orchestration into VerifyWorkflow</name>
  <files>src/workflow/VerifyWorkflow.php, src/console/VerifyController.php, tests/unit/workflow/VerifyWorkflowTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    .planning/phases/12-cp-migration-console-queue-workflow/12-PATTERNS.md
    src/console/VerifyController.php
    src/verify/CountGateService.php
    src/verify/BaselineCounterService.php
  </read_first>
  <action>
    Create `VerifyWorkflow::run(array $options, ?callable $progress = null): array` while preserving orchestration from `src/console/VerifyController.php`, `src/verify/CountGateService.php`, and `src/verify/BaselineCounterService.php`. Supported option keys are exactly `baseline`, `urlSpotCheck`, `baselineDir`, `countTolerance`, `urlDiffThreshold`, `entities`, `locales`, `since`, `captureBaseline`, `captureBaselineHtml`, `output`, and `outputDir`. Preserve markdown artifact behavior for `VERIFY-<timestamp>.md`. Return keys `status`, `artifactPaths`, `summary`, and `failure`.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/workflow/VerifyWorkflowTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "class VerifyWorkflow\|VERIFY-\|artifactPaths\|countTolerance\|urlDiffThreshold" src/workflow/VerifyWorkflow.php` returns at least 5 matches.
    - `grep -R "VerifyWorkflow" src/console/VerifyController.php` returns at least 1 match.
    - `grep -R "shell_exec\|php craft" src/workflow/VerifyWorkflow.php` returns 0 matches.
  </acceptance_criteria>
  <done>
    CLI verify/report delegates to `VerifyWorkflow`, and queued verify can receive pass/fail metadata plus artifact paths.
  </done>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| CP dry-run/live behavior diverges from CLI. | High | Queue jobs call `MigrateWorkflow`, and CLI adapts to the same service. |
| Long-running verification runs inline in web requests. | Medium | `VerifyWorkflow` is service-only and later invoked by queue jobs, not controllers. |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/unit/workflow/MigrateWorkflowTest.php --testdox`
- `vendor/bin/phpunit tests/unit/workflow/VerifyWorkflowTest.php --testdox`
</verification>

<success_criteria>
- Migrate and verify/report behavior is reusable by CLI and queue jobs.
- Dry-run remains default, live remains explicit and gateable.
- Report and log artifact paths are returned to run records and CP.
</success_criteria>
