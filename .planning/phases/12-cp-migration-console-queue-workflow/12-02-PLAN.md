---
phase: 12-cp-migration-console-queue-workflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workflow/AnalyzeWorkflow.php
  - src/workflow/CompileWorkflow.php
  - src/console/AnalyzeController.php
  - src/console/CompileController.php
  - tests/unit/workflow/AnalyzeWorkflowTest.php
  - tests/unit/workflow/CompileWorkflowTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - CLI remains canonical while CP and queue jobs reuse analyze and compile orchestration.
    - Analyze remains the only workflow allowed to call Anthropic.
    - Compile produces gate-usable status, artifact paths, and fatal warning counts.
  artifacts:
    - path: src/workflow/AnalyzeWorkflow.php
      provides: Shared analyze orchestration
      contains: AnalyzeWorkflow
    - path: src/workflow/CompileWorkflow.php
      provides: Shared compile orchestration
      contains: fatalWarningCount
  key_links:
    - from: src/console/AnalyzeController.php
      to: src/workflow/AnalyzeWorkflow.php
      via: controller adapter
      pattern: AnalyzeWorkflow
    - from: src/console/CompileController.php
      to: src/workflow/CompileWorkflow.php
      via: controller adapter
      pattern: CompileWorkflow
---

# Plan 12-02: Analyze and Compile workflow extraction

<objective>
Extract analyze and compile orchestration into shared services so CLI, CP, and queue jobs can use the same deterministic behavior without shelling out or duplicating console controllers.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Extract analyze orchestration into AnalyzeWorkflow</name>
  <files>src/workflow/AnalyzeWorkflow.php, src/console/AnalyzeController.php, tests/unit/workflow/AnalyzeWorkflowTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    .planning/phases/12-cp-migration-console-queue-workflow/12-PATTERNS.md
    src/console/AnalyzeController.php
    src/filter/FilterFactory.php
    src/mapping/MappingFile.php
  </read_first>
  <action>
    Create `AnalyzeWorkflow::run(array $options, ?callable $progress = null): array` while preserving orchestration from `src/console/AnalyzeController.php`. Supported option keys are exactly `entities`, `locales`, `since`, `noAi`, `autoAcceptHigh`, `auditStrict`, `sourceStrict`, `noLayout`, and `noProviders`. Move analyze orchestration behind this service without `shell_exec`, without building `php craft`, and without duplicating controller-only behavior. Keep `NeverProductionTrait` enforcement in `AnalyzeController` for CLI. Return keys `status`, `filters`, `artifactPaths`, `summary`, and `failure`. Ensure Anthropic usage remains inside analyze only and only when `noAi === false`.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/workflow/AnalyzeWorkflowTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "class AnalyzeWorkflow\|function run\|noAi\|autoAcceptHigh\|artifactPaths" src/workflow/AnalyzeWorkflow.php` returns at least 5 matches.
    - `grep -R "AnalyzeWorkflow" src/console/AnalyzeController.php` returns at least 1 match.
    - `grep -R "shell_exec\|php craft" src/workflow/AnalyzeWorkflow.php` returns 0 matches.
  </acceptance_criteria>
  <done>
    CLI analyze delegates to `AnalyzeWorkflow`, the workflow exposes artifact/summary/failure results, and no workflow shell-out path exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Extract compile orchestration into CompileWorkflow</name>
  <files>src/workflow/CompileWorkflow.php, src/console/CompileController.php, tests/unit/workflow/CompileWorkflowTest.php</files>
  <read_first>
    .planning/phases/12-cp-migration-console-queue-workflow/12-CONTEXT.md
    .planning/phases/12-cp-migration-console-queue-workflow/12-PATTERNS.md
    src/console/CompileController.php
    src/compile/MappingCompiler.php
    src/compile/GraphCompatibilityValidator.php
  </read_first>
  <action>
    Create `CompileWorkflow::run(array $options, ?callable $progress = null): array` while preserving orchestration from `src/console/CompileController.php`, `src/compile/MappingCompiler.php`, and `src/compile/GraphCompatibilityValidator.php`. Supported option keys are exactly `overwrite` and `dryRun`. Preserve graph compatibility fatal blocking, mapping overwrite refusal, and artifact writing. Return keys `status`, `artifactPaths`, `summary`, `fatalWarningCount`, `warningCount`, `compiledAt`, and `failure`. Update `CompileController::actionIndex()` to adapt CLI flags into `CompileWorkflow::run()` and render equivalent operator output.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/unit/workflow/CompileWorkflowTest.php --testdox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "class CompileWorkflow\|fatalWarningCount\|warningCount\|compiledAt\|GraphCompatibilityValidator" src/workflow/CompileWorkflow.php` returns at least 5 matches.
    - `grep -R "CompileWorkflow" src/console/CompileController.php` returns at least 1 match.
    - `grep -R "shell_exec\|php craft" src/workflow/CompileWorkflow.php` returns 0 matches.
  </acceptance_criteria>
  <done>
    CLI compile delegates to `CompileWorkflow`, compile artifacts and fatal/warning counts are returned for gates, and no workflow shell-out path exists.
  </done>
</task>
</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| CP/queue behavior diverges from CLI behavior. | High | Console controllers become adapters over shared workflow services. |
| Anthropic is called outside analyze. | High | Keep Anthropic access only in `AnalyzeWorkflow` and gated by `noAi === false`. |
| CP queues shell out to CLI. | Medium | Acceptance criteria reject `shell_exec` and `php craft` in workflows. |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/unit/workflow/AnalyzeWorkflowTest.php --testdox`
- `vendor/bin/phpunit tests/unit/workflow/CompileWorkflowTest.php --testdox`
</verification>

<success_criteria>
- CLI analyze and compile delegate to reusable workflow services.
- Analyze remains the only AI-capable workflow.
- Compile results expose fatal warning counts and artifact paths for gates.
</success_criteria>
