---
phase: 10-generic-migration-rehearsal-gap-closure
plan: 10-05
type: execute
wave: 5
depends_on: ["10-01", "10-02", "10-03", "10-04"]
files_modified:
  - src/load/TaxonomyMigrationService.php
  - tests/integration/load/TaxonomyMigrationTest.php
  - src/console/MigrateController.php
  - tests/unit/console/MigrateControllerFailureExitTest.php
  - .planning/rehearsal/v1.0/cqm/README.md
autonomous: true
gap_closure: true
requirements: [PH10-04, PH10-08]
user_setup: []
must_haves:
  truths:
    - "Taxonomy locale fallback resolves Craft site handles from mapping.sites values, including compiled mapping shape ['nl' => 'default', 'en' => 'enUs'], while preserving array-shaped compatibility."
    - "Transform handler warnings, including taxonomy/relation resolver failures, reach the main MigrationReport and REPORT.md instead of being discarded with the __report sentinel."
    - "Live relation/taxonomy handler failures block a clean live migration outcome before load can silently save pages with missing page-owned relation values."
    - "The CQM rehearsal runbook uses the supported default dry-run command `php craft kunstmaan-migrator/migrate` and documents `--live` as write mode."
  artifacts:
    - path: "src/load/TaxonomyMigrationService.php"
      provides: "Craft site handle extraction from mapping.sites values for taxonomy locale fallback"
      contains: "siteHandle"
    - path: "tests/integration/load/TaxonomyMigrationTest.php"
      provides: "Regression coverage for compiled mapping.sites locale-key-to-site-handle fallback"
      contains: "['nl' => 'default', 'en' => 'enUs']"
    - path: "src/console/MigrateController.php"
      provides: "Transform __report warning merge into main MigrationReport and live relation/taxonomy blocking decision"
      contains: "__report"
    - path: "tests/unit/console/MigrateControllerFailureExitTest.php"
      provides: "Regression coverage proving transform sentinel warnings reach migration report output and relation/taxonomy failures block live success"
      contains: "Transform:"
    - path: ".planning/rehearsal/v1.0/cqm/README.md"
      provides: "Executable CQM runbook command sequence matching current CLI"
      contains: "php craft kunstmaan-migrator/migrate"
  key_links:
    - from: "src/load/TaxonomyMigrationService.php"
      to: "Craft::$app->sites->getSiteByHandle"
      via: "mapping.sites value-to-Craft-handle extraction"
      pattern: "getSiteByHandle\\(\\$siteHandle\\)"
    - from: "src/console/MigrateController.php"
      to: "src/load/MigrationReport.php"
      via: "__report sentinel warning merge using MigrationReport::warn and relation/taxonomy failure recording"
      pattern: "Transform: .*Handler 'relation' failed|recordFailure"
    - from: ".planning/rehearsal/v1.0/cqm/README.md"
      to: "src/console/MigrateController.php"
      via: "documented default dry-run command and --live write mode"
      pattern: "kunstmaan-migrator/migrate\\n.*kunstmaan-migrator/migrate --live"
---

<objective>
Close the three Phase 10 verification gaps without broadening Phase 10 scope.

Purpose: Phase 10 already delivered the restored CQM proof and generic migration hardening, but verification found three remaining gaps that can still make the phase non-executable or semi-silent: taxonomy locale fallback can resolve legacy locale keys instead of Craft handles, transform handler warnings can be discarded before the main MigrationReport, and the CQM runbook contains an unsupported dry-run option.

Output:
- Taxonomy fallback site-handle bug fixed with regression coverage.
- Transform sentinel warnings merged into the main MigrationReport with live relation/taxonomy failure blocking semantics documented in code/tests.
- CQM runbook command sequence corrected to the actual CLI: default `migrate` is dry-run, `migrate --live` is write mode.

Out of scope:
- Do not rerun the full restored CQM rehearsal as part of this gap plan unless targeted tests unexpectedly cannot prove the fix.
- Do not change taxonomy migration mode defaults, relation architecture, verify count semantics, Matrix fallback behavior, sparse-locale fallback behavior, PageBuilder ownership validation, or CQM external artifacts.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-CONTEXT.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-RESEARCH.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-PATTERNS.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-VERIFICATION.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-REVIEW.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-01-SUMMARY.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-02-SUMMARY.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-03-SUMMARY.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-04-SUMMARY.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-SUMMARY.md

<interfaces>
Key existing contracts the executor should use directly:

From `src/load/MigrationReport.php`:
```php
final class MigrationReport
{
    public array $counts = [];
    public array $warnings = [];
    public array $failures = [];

    public function incr(string $bucket, int $by = 1): void;
    public function warn(string $message): void;
    public function recordFailure(string|int $legacyId, ?string $slug, ?string $handler, Throwable $e): void;
    public function hasFailures(): bool;
    public function failureCount(): int;
}
```

From `src/transform/TransformService.php`:
```php
try {
    $fieldValues[$targetHandle] = $handler->resolve($legacyValue, $ctx, $opts);
} catch (Throwable $e) {
    $report['warnings'][] = "Handler '{$handlerId}' failed on {$targetHandle}: " . $e->getMessage();
}
```

From `src/console/MigrateController.php` current transform sentinel handling:
```php
foreach ($plugin->transformService->run(...) as $payload) {
    if (isset($payload['__report'])) {
        continue; // sentinel - counters available via the Transform run report
    }
    // writes transformed payloads
}
```

From `src/load/TaxonomyMigrationService.php` current failing fallback branch:
```php
$sites = (array) ($mapping['sites'] ?? []);
foreach ($sites as $siteHandle => $_siteCfg) {
    $site = Craft::$app->sites->getSiteByHandle((string) $siteHandle);
    // ...
    $report->warn(sprintf(
        'fallback: taxonomy locale values for %s id=%d site=%s use default-language values',
        $this->fqcnToSlug($fqcn),
        $legacyId,
        (string) $siteHandle,
    ));
}
```

Decision for this gap closure:
- Per D-08 and D-10, taxonomy/relation handler failures must not be silent.
- Live relation/taxonomy handler failures should block a clean live migration outcome before load proceeds, because saving pages after a relation/taxonomy resolver exception can persist page-owned content with missing relation fields.
- Dry-run transform handler failures remain operator-visible warnings so operators can inspect the scope without writes.
</interfaces>
</context>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---:|---|---:|---|---|
| GOAL | Phase 10 | Clean generic migration hardening without silent loss of page-owned relations/content | 10-05 | COVERED | Gap plan closes verifier-identified silent/semi-silent relation and runbook issues only. |
| REQ | PH10-04 | Taxonomy-dependent relation resolution fixed by stage ordering or deferred relation tokens | 10-05 | COVERED | Normal resolver exists from 10-03; this plan closes fallback handle mapping and transform failure visibility/blocking gaps. |
| REQ | PH10-08 | Rehearsal instructions include restore/rerun and inspection gates | 10-05 | COVERED | This plan fixes unsupported dry-run command and documents `--live` write mode. |
| RESEARCH | WR-01 / Review | Taxonomy fallback must resolve Craft handles from mapping values | 10-05 | COVERED | Task 1. |
| RESEARCH | WR-02 / Review | Transform sentinel warnings must reach MigrationReport | 10-05 | COVERED | Task 2. |
| CONTEXT | D-05 | Missing taxonomy locale values fall back to default-language values | 10-05 | COVERED | Task 1 fixes compiled mapping shape that currently skips fallback saves. |
| CONTEXT | D-08 | Fallback values must be visible in migration report/log output | 10-05 | COVERED | Task 1 preserves report warning; Task 2 merges transform warnings. |
| CONTEXT | D-10 | Page-owned referenced content surfaces migrate or are explicitly dropped/out-of-scope | 10-05 | COVERED | Task 2 blocks clean live outcome for relation/taxonomy handler failures. |
| CONTEXT | D-11 | Closing proof is restored-backup full workflow | 10-05 | COVERED | Task 3 keeps runbook executable; this gap plan does not rerun full rehearsal unless targeted verification cannot prove the fix. |
| CONTEXT | Deferred | Broad relation mode architecture | 10-05 | EXCLUDED | Deferred in 10-CONTEXT.md; not planned here. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve taxonomy fallback Craft handles from mapping.sites values</name>

  <files>
src/load/TaxonomyMigrationService.php,
tests/integration/load/TaxonomyMigrationTest.php
  </files>

  <read_first>
    - `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-VERIFICATION.md` lines covering gap "Missing taxonomy locale values use default-language values and are visibly reported."
    - `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-REVIEW.md` WR-01.
    - `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-03-SUMMARY.md` taxonomy fallback/reporting decisions.
    - `src/load/TaxonomyMigrationService.php` around `applyGedmoTranslations(...)`, especially the `$translations === []` fallback branch.
    - `tests/integration/load/TaxonomyMigrationTest.php` existing taxonomy fallback and resolver tests.
  </read_first>

  <behavior>
    - Test 1: A compiled mapping shape `['sites' => ['nl' => 'default', 'en' => 'enUs']]` resolves fallback Craft handles as `default` and `enUs`, not `nl` and `en`.
    - Test 2: Existing array-shaped compatibility remains intact: `['en' => ['siteHandle' => 'enUs']]` resolves `enUs`.
    - Test 3: Array-shaped mappings without `siteHandle` fall back to the legacy locale key to preserve older test/config shape compatibility.
  </behavior>

  <action>
    Implement the taxonomy fallback fix per D-05 and D-08.

    In `src/load/TaxonomyMigrationService.php`:
    - Add a small private helper, for example `private function siteHandleFromMappingSite(string $legacyLocale, mixed $siteCfg): string`, that returns:
      - `(string) ($siteCfg['siteHandle'] ?? $legacyLocale)` when `$siteCfg` is an array.
      - `(string) $siteCfg` when `$siteCfg` is scalar/string-like.
      - `''` only when the resolved handle is empty after casting.
    - In the `$translations === []` branch of `applyGedmoTranslations(...)`, iterate as `foreach ($sites as $legacyLocale => $siteCfg)`, compute `$siteHandle` with the helper, skip empty handles, and call `Craft::$app->sites->getSiteByHandle($siteHandle)`.
    - Keep the primary-site skip by comparing `$site->id === $primarySite->id`.
    - Update the fallback warning to report the resolved Craft site handle and preserve the source legacy locale for traceability, for example `site=%s locale=%s`.
    - Do not change taxonomy resolver mode, state-row semantics, dry-run behavior, or full unreferenced taxonomy import behavior.

    In `tests/integration/load/TaxonomyMigrationTest.php`:
    - Add regression coverage that can run without a full Craft application bootstrap by testing the helper via reflection or by extracting a pure mapping-shape test seam already accepted in this test file.
    - Include the exact compiled-shape mapping required by verification: `['nl' => 'default', 'en' => 'enUs']`.
    - Include the array-shaped compatibility case so older mapping/test shapes keep working.
  </action>

  <acceptance_criteria>
    - `grep -n "foreach (\\$sites as \\$legacyLocale => \\$siteCfg)" src/load/TaxonomyMigrationService.php` returns a match in the empty-translation fallback branch.
    - `grep -n "getSiteByHandle(\\$siteHandle)" src/load/TaxonomyMigrationService.php` returns a match.
    - `grep -n "'nl' => 'default'.*'en' => 'enUs'\\|'en' => 'enUs'" tests/integration/load/TaxonomyMigrationTest.php` returns evidence of the compiled mapping shape.
    - The warning text still contains `fallback: taxonomy locale values` and now includes the resolved Craft site handle rather than the legacy locale key alone.
  </acceptance_criteria>

  <verify>
    <automated>vendor/bin/phpunit tests/integration/load/TaxonomyMigrationTest.php --filter 'Compiled|Locale|Fallback|MappingSite|SiteHandle' --testdox</automated>
    <automated>php -l src/load/TaxonomyMigrationService.php</automated>
  </verify>

  <done>
    Taxonomy locale fallback for compiled mapping shape `['nl' => 'default', 'en' => 'enUs']` resolves Craft site handles from mapping values, keeps array-shaped compatibility, and preserves operator-visible fallback reporting.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Merge transform sentinel warnings into MigrationReport and block live relation/taxonomy handler failures</name>

  <files>
src/console/MigrateController.php,
tests/unit/console/MigrateControllerFailureExitTest.php
  </files>

  <read_first>
    - `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-VERIFICATION.md` lines covering gap "Page-owned taxonomy/relation handler failures are operator-visible and cannot silently lose relation content."
    - `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-REVIEW.md` WR-02.
    - `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-CONTEXT.md` D-08, D-09, D-10.
    - `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-03-SUMMARY.md` taxonomy resolver and RelationHandler delegation decisions.
    - `src/transform/TransformService.php` handler exception/report sentinel behavior.
    - `src/console/MigrateController.php` transform loop, `writeReport(...)`, `reportExitCode(...)`, and `mergeReport(...)`.
    - `src/load/MigrationReport.php` `warn(...)`, `recordFailure(...)`, and `hasFailures(...)`.
    - `tests/unit/console/MigrateControllerFailureExitTest.php` existing reflection/source-scan style.
  </read_first>

  <behavior>
    - Test 1: A transform sentinel such as `['__report' => ['warnings' => ["Handler 'relation' failed on topicRelation: resolver failed"]]]` appends `Transform: Handler 'relation' failed on topicRelation: resolver failed` to the main `MigrationReport`.
    - Test 2: The merged warning appears in migration report output through the existing `## Warnings` rendering path, or through a direct helper assertion that the report warnings array is what `writeReport(...)` renders.
    - Test 3: In live mode, relation/taxonomy handler warnings are treated as blocking transform failures before load proceeds, so the main `MigrationReport` has failures and final `reportExitCode(...)` is non-zero.
    - Test 4: In dry-run mode, the same warning remains visible in `MigrationReport::warnings` without recording a failure.
  </behavior>

  <action>
    Implement transform sentinel merging per D-08 and D-10.

    In `src/console/MigrateController.php`:
    - Replace the current `if (isset($payload['__report'])) { continue; }` discard branch in `actionIndex()` with a call to a new helper that merges the sentinel into the main `MigrationReport`.
    - The helper should:
      - Read `(array) ($payload['__report']['warnings'] ?? [])`.
      - For each warning, call `$report->warn('Transform: ' . (string) $warning)`.
      - Increment a transform warning counter such as `$report->incr('transform.warning')` for operator-visible counts.
      - Detect relation/taxonomy handler failures using the merged warning text. Treat at least these as blocking in live mode: `Handler 'relation' failed`, `taxonomy`, and `TaxonomyMigrationService`.
    - Add an explicit decision in code comments near the helper: dry-run keeps transform handler failures as warnings; live relation/taxonomy handler failures block the clean migration outcome because page-owned relation fidelity is more important than saving entries with omitted relation fields.
    - In `actionIndex()`, accumulate a boolean such as `$hasBlockingTransformRelationFailure` during transform.
    - After the transform loop finishes and before taxonomy/load/SEO/Retour stages begin, if `$this->live === true` and the boolean is true:
      - Record a failure on the main report with `MigrationReport::recordFailure(...)` using a synthetic legacy id like `'transform'`, slug like `'transform'`, handler like `'TransformService'`, and a `RuntimeException` message that tells the operator to inspect the `Transform:` warnings in `REPORT.md`.
      - Call `writeReport($storageDir, $report, $filters, $tRunStart)` so `REPORT.md` exists with the transform warning/failure evidence.
      - Return `reportExitCode($report)` without running load, so pages are not saved with known relation/taxonomy handler omissions.
    - Do not change `TransformService` catch behavior unless a small shape change is required for tests. The minimum correct wiring is in `MigrateController`, where the sentinel is currently discarded.

    In `tests/unit/console/MigrateControllerFailureExitTest.php`:
    - Add tests around the new helper via reflection if it is private.
    - Assert the exact merged warning prefix `Transform:`.
    - Assert relation/taxonomy warning detection records or returns a blocking signal for live mode and does not record failure for dry-run mode.
    - Assert `reportExitCode(...)` returns `ExitCode::UNSPECIFIED_ERROR` when the blocking transform failure has been recorded.
  </action>

  <acceptance_criteria>
    - `grep -n "Transform:" src/console/MigrateController.php tests/unit/console/MigrateControllerFailureExitTest.php` returns matches.
    - `grep -n "__report" src/console/MigrateController.php` shows sentinel payloads are merged, not discarded with a bare `continue`.
    - `grep -n "recordFailure(.*TransformService\\|TransformService" src/console/MigrateController.php tests/unit/console/MigrateControllerFailureExitTest.php` returns matches.
    - Tests prove live relation/taxonomy handler failures produce a non-clean MigrationReport outcome before load proceeds.
    - Tests prove dry-run relation/taxonomy handler failures remain visible warnings without write-mode failure recording.
  </acceptance_criteria>

  <verify>
    <automated>vendor/bin/phpunit tests/unit/console/MigrateControllerFailureExitTest.php --filter 'Transform|Sentinel|Handler|Relation|Taxonomy' --testdox</automated>
    <automated>php -l src/console/MigrateController.php</automated>
  </verify>

  <done>
    Transform sentinel warnings are merged into the main MigrationReport/REPORT.md path. Live taxonomy/relation handler failures block a clean write-mode migration before load can persist entries with known missing page-owned relations; dry-run remains warning-visible.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct CQM runbook dry-run command and run gap-closure verification</name>

  <files>
.planning/rehearsal/v1.0/cqm/README.md
  </files>

  <read_first>
    - `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-VERIFICATION.md` lines covering gap "Restored-backup CQM rerun instructions are executable against the current CLI."
    - `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-04-SUMMARY.md` "Used actual non-interactive CQM CLI forms" decision.
    - `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-SUMMARY.md` restored-backup workflow commands.
    - `.planning/rehearsal/v1.0/cqm/README.md` restored-backup closing workflow.
    - `src/console/MigrateController.php` `options()` and dry-run/live mode behavior if needed to confirm command surface.
  </read_first>

  <action>
    Update the runbook per PH10-08 and D-11 so the documented workflow matches the current CLI.

    In `.planning/rehearsal/v1.0/cqm/README.md`:
    - Replace `php craft kunstmaan-migrator/migrate --dry-run` with `php craft kunstmaan-migrator/migrate`.
    - Add one sentence immediately near the command block stating: dry-run is the default `migrate` mode; `php craft kunstmaan-migrator/migrate --live` is the write mode.
    - Keep the restore command, doctor/analyze/map/compile/migrate/live/verify sequence, source-shape audit, inspection gates, strict acceptance bar, and mechanical gate intact.
    - Do not commit external CQM artifacts and do not rerun the full restored CQM rehearsal for this documentation fix unless targeted verification cannot prove the gap is closed.
  </action>

  <acceptance_criteria>
    - `grep -n "kunstmaan-migrator/migrate --dry-run" .planning/rehearsal/v1.0/cqm/README.md` returns no matches.
    - `grep -n "php craft kunstmaan-migrator/migrate$" .planning/rehearsal/v1.0/cqm/README.md` returns the default dry-run command in the workflow block.
    - `grep -n "dry-run is the default\\|--live is the write mode\\|write mode" .planning/rehearsal/v1.0/cqm/README.md` returns the explanatory sentence.
    - The README still includes restore, doctor, analyze, map, compile, migrate, migrate --live, verify, source-shape audit, and inspection gates.
  </acceptance_criteria>

  <verify>
    <automated>vendor/bin/phpunit tests/integration/load/TaxonomyMigrationTest.php --filter 'Compiled|Locale|Fallback|MappingSite|SiteHandle' --testdox</automated>
    <automated>vendor/bin/phpunit tests/unit/console/MigrateControllerFailureExitTest.php --filter 'Transform|Sentinel|Handler|Relation|Taxonomy' --testdox</automated>
    <automated>composer test</automated>
    <automated>grep -n "kunstmaan-migrator/migrate --dry-run" .planning/rehearsal/v1.0/cqm/README.md; test $? -eq 1</automated>
    <automated>grep -n "php craft kunstmaan-migrator/migrate" .planning/rehearsal/v1.0/cqm/README.md</automated>
  </verify>

  <done>
    The CQM runbook uses the supported dry-run/default command and documents `--live` as write mode. Targeted taxonomy, transform/report, and full composer tests pass. The executor summary maps each of the three original `10-VERIFICATION.md` gaps to code/test/doc evidence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| mapping.yaml -> Craft site lookup | Operator/compiler-produced `mapping.sites` values are used to select Craft sites for localized taxonomy fallback saves. |
| TransformService -> MigrateController | Transform-stage handler exceptions cross from a local sentinel report into the main MigrationReport and live-mode control flow. |
| Runbook -> operator shell | README commands are copied into a developer/staging shell and must match the supported CLI surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-05-01 | Tampering | `TaxonomyMigrationService::applyGedmoTranslations` | mitigate | Resolve Craft site handles only from explicit mapping values or existing array `siteHandle`; cast to string and skip empty handles; no eval/dynamic code. |
| T-10-05-02 | Repudiation | `MigrateController` transform sentinel merge | mitigate | Prefix merged warnings with `Transform:` and persist them through MigrationReport/REPORT.md so operators can audit handler failures. |
| T-10-05-03 | Information Disclosure | `REPORT.md` transform warnings | accept | Handler error messages already enter local transform report today; this plan moves them to the existing operator-only migration report. Do not add legacy content samples, SQL rows, secrets, or source payload dumps. |
| T-10-05-04 | Denial of Service | Live relation/taxonomy handler failure blocking | accept | Blocking live load on relation/taxonomy handler failure is intentional to prevent silent page-owned relation loss; dry-run remains available for diagnostics. |
| T-10-05-05 | Spoofing | CQM runbook command docs | mitigate | Document exact current CLI behavior: default `migrate` is dry-run; `--live` is write mode; `NeverProductionTrait` remains the production guard. |
</threat_model>

<verification>
Overall checks after all tasks:

```bash
vendor/bin/phpunit tests/integration/load/TaxonomyMigrationTest.php --filter 'Compiled|Locale|Fallback|MappingSite|SiteHandle' --testdox
vendor/bin/phpunit tests/unit/console/MigrateControllerFailureExitTest.php --filter 'Transform|Sentinel|Handler|Relation|Taxonomy' --testdox
php -l src/load/TaxonomyMigrationService.php
php -l src/console/MigrateController.php
composer test
grep -n "kunstmaan-migrator/migrate --dry-run" .planning/rehearsal/v1.0/cqm/README.md; test $? -eq 1
grep -n "php craft kunstmaan-migrator/migrate" .planning/rehearsal/v1.0/cqm/README.md
```

Final acceptance:
- Review `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-VERIFICATION.md` gaps 1-3 and confirm each is closed by this plan's evidence:
  1. Taxonomy fallback uses mapping values as Craft site handles and has compiled-shape regression coverage.
  2. Transform sentinel warnings reach the main MigrationReport/REPORT.md path and live relation/taxonomy handler failures block clean write-mode success.
  3. The CQM runbook no longer documents unsupported `migrate --dry-run` and explains default dry-run plus `--live` write mode.
- If the GSD verifier command is available in the execution environment, rerun Phase 10 verification after implementation; otherwise include the gap-to-evidence table in `10-05-SUMMARY.md`.
</verification>

<success_criteria>
- PH10-04 returns to satisfied for taxonomy-dependent relation resolution because normal resolver behavior remains intact, taxonomy locale fallback is correct for compiled mapping shapes, and resolver/handler failures cannot be discarded.
- PH10-08 returns to satisfied for executable CQM runbook instructions because the documented dry-run command matches the current CLI.
- `composer test` passes after targeted regression tests.
- No full restored CQM rehearsal is run for this gap plan unless targeted verification cannot prove the code/doc fixes.
- No CQM-specific production logic, page IDs, block handles, class-name conditionals, or external CQM content artifacts are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-05-SUMMARY.md` with:
- Commands run and results.
- A three-row gap-closure evidence table keyed to `10-VERIFICATION.md` gaps.
- The live relation/taxonomy handler failure decision and rationale.
- Confirmation that the full restored CQM rehearsal was not rerun unless targeted verification required it.
</output>
