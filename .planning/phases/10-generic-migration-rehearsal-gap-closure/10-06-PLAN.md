---
phase: 10-generic-migration-rehearsal-gap-closure
plan: 10-06
type: execute
wave: 6
depends_on: ["10-05"]
files_modified:
  - src/finalize/CkeditorRewriterService.php
  - src/finalize/FinalizeWalker.php
  - src/console/MigrateController.php
  - src/audit/PageRootedSurfaceDiscovery.php
  - src/audit/PageRootedCoverageAuditor.php
  - tests/unit/finalize/CkeditorRewriterServiceTest.php
  - tests/unit/audit/PageRootedSurfaceDiscoveryTest.php
  - tests/unit/audit/PageRootedCoverageAuditorTest.php
  - tests/unit/console/MigrateControllerFailureExitTest.php
  - .planning/rehearsal/v1.0/cqm/README.md
autonomous: true
gap_closure: true
requirements: [PH10-04, PH10-07, PH10-08]
user_setup: []
must_haves:
  truths:
    - "A live finalize pass reports unresolved CKEditor token causes by token family and reason, not only a single aggregate count."
    - "Resolved `[NT...]`, `%5BNT...%5D`, `[M...]`, and `%5BM...%5D` tokens are replaced by Craft ref tokens without retaining the legacy literal plus unresolved marker."
    - "Live migration/finalize cannot be release-clean when `finalize.unresolvable` is nonzero; REPORT.md contains actionable unresolved diagnostics when unresolved tokens remain."
    - "Page-rooted coverage warning/unsupported rows represent real page-owned evidence, not synthetic absence rows created solely because scanner or relation metadata input was unavailable."
    - "Global taxonomy/dataProvider proposals are not repeated across every page root unless page-owned mapping/field/relation evidence connects that proposal to the page."
    - "Explicit migrated, dropped, and out_of_scope rows remain visible, including operator-curated drops and accepted exclusions."
  artifacts:
    - path: "src/finalize/CkeditorRewriterService.php"
      provides: "Broadened NT cache warming, media/NT unresolved diagnostics, and token rewrite guarantees"
      contains: "unresolved"
    - path: "src/finalize/FinalizeWalker.php"
      provides: "Finalize pass aggregate counts plus unresolved diagnostics returned to migration orchestration"
      contains: "diagnostics"
    - path: "src/console/MigrateController.php"
      provides: "Blocking live release gate for nonzero finalize unresolved count and detailed REPORT.md rows"
      contains: "finalize.unresolvable"
    - path: "src/audit/PageRootedSurfaceDiscovery.php"
      provides: "Evidence-based Page-rooted discovery without blocking synthetic absence rows"
      contains: "not-discovered"
    - path: "tests/unit/finalize/CkeditorRewriterServiceTest.php"
      provides: "Pure PHPUnit coverage for raw/encoded token rewrite and NT/media diagnostics seams"
      contains: "NT"
    - path: "tests/unit/audit/PageRootedSurfaceDiscoveryTest.php"
      provides: "Regression coverage that no-input service/relation metadata does not emit blocking warning/unsupported placeholders"
      contains: "not-discovered"
    - path: "tests/unit/audit/PageRootedCoverageAuditorTest.php"
      provides: "Regression coverage preserving migrated/dropped/out_of_scope classifications after discovery cleanup"
      contains: "out_of_scope"
    - path: ".planning/rehearsal/v1.0/cqm/README.md"
      provides: "Runbook gate requiring zero unresolved finalize tokens and zero blocking Page-rooted coverage rows"
      contains: "finalize.unresolvable"
  key_links:
    - from: "src/finalize/CkeditorRewriterService.php"
      to: "src/finalize/FinalizeWalker.php"
      via: "rewriter exposes per-pass unresolved diagnostics consumed after each CKEditor field rewrite"
      pattern: "unresolved.*diagnostic"
    - from: "src/finalize/FinalizeWalker.php"
      to: "src/console/MigrateController.php"
      via: "walk() returns unresolved diagnostic rows alongside processed/rewritten/unresolvable counts"
      pattern: "finalize\\.unresolvable"
    - from: "src/console/MigrateController.php"
      to: "REPORT.md"
      via: "MigrationReport warning/failure rows make unresolved finalize causes release-blocking in live mode"
      pattern: "Finalize.*unresolved"
    - from: "src/audit/PageRootedSurfaceDiscovery.php"
      to: "src/audit/PageRootedCoverageAuditor.php"
      via: "discovery emits categoryHint values based on structural evidence; auditor renders them deterministically"
      pattern: "categoryHint"
    - from: ".planning/rehearsal/v1.0/cqm/README.md"
      to: "/Users/macbook25/Sites/cqm-craft-website/storage/migration/REPORT.md"
      via: "operator verification requires `finalize.unresolvable = 0` and no raw unresolved token grep hits before release"
      pattern: "finalize\\.unresolvable.*0"
---

<objective>
Close the Phase 10 release-owner gate by making unresolved finalize tokens and Page-rooted warning/unsupported coverage rows blocking until they are generically fixed or truthfully reclassified.

Purpose: Phase 10 implementation and gap closure reached zero entry/stage failures, but release-owner review rejected two remaining visible risks as acceptable-for-release: unresolved finalize tokens in transformed/saved CKEditor content, and Page-rooted coverage rows classified as warning/unsupported where the row exists only because generic scanner/relation metadata was absent. This plan fixes as much as possible generically without adding CQM-specific IDs, classes, handles, or page names.

Output:
- Broadened CKEditor NT/media placeholder resolution with diagnostics that explain unresolved causes.
- Live migration/finalize release gate for nonzero unresolved finalize count.
- Page-rooted coverage discovery cleanup so blocking rows require page-owned structural evidence.
- Regression tests for finalize and coverage behavior.
- CQM runbook/verification expectations requiring zero unresolved finalize tokens and zero blocking Page-rooted warning/unsupported rows while allowing explicit accepted out_of_scope/dropped rows.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-CONTEXT.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-RESEARCH.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-PATTERNS.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-VERIFICATION.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-05-SUMMARY.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-05-REVIEW-FIX.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-05-REVIEW-RECHECK.md
@.planning/phases/10-generic-migration-rehearsal-gap-closure/10-SUMMARY.md
@.planning/rehearsal/v1.0/cqm/README.md

External current CQM artifacts to inspect when available:
- `/Users/macbook25/Sites/cqm-craft-website/storage/migration/REPORT.md`
- `/Users/macbook25/Sites/cqm-craft-website/storage/migration/PAGE-ROOTED-COVERAGE.md`
- `/Users/macbook25/Sites/cqm-craft-website/storage/migration/transformed/`

Release-owner decision for this plan:
- Treat both unresolved finalize tokens and Page-rooted warning/unsupported coverage rows as blocking for now.
- Fix generically across Lameco Kunstmaan sites.
- Reclassify only when the row has no structural evidence or scanner input; do not hide genuine page-owned missing content.

<interfaces>
Key existing contracts the executor should use directly:

From `src/finalize/CkeditorRewriterService.php`:
```php
public const KUMA_MEDIA_PLACEHOLDER_REGEX = '~(?:\[|%5B)M(\d+)(?:\]|%5D)~i';
public const KUMA_NT_PLACEHOLDER_REGEX = '~(?:\[|%5B)NT(\d+)(?:\]|%5D)~i';

public ?MigrationStateService $migrationState = null;
public ?LegacyDbService $legacyDb = null;
public ?object $assetResolver = null;

public function rewrite(?string $html, int $siteId, array $entryUrlToId = []): string;
public function seedUrlIdCache(array $map): void;
public function seedKumaMediaIdCache(array $map): void;
public function seedNtToEntryCache(array $map): void;
```

Current unresolved behavior:
```php
private function rewriteMediaPlaceholders(string $html, int $siteId): string
{
    return preg_replace_callback(
        self::KUMA_MEDIA_PLACEHOLDER_REGEX,
        function ($m) use ($siteId) {
            $kumaMediaId = (int) $m[1];
            $craftAssetId = $this->resolveKumaMediaId($kumaMediaId);
            if ($craftAssetId !== null) {
                return '{asset:' . $craftAssetId . '@' . $siteId . ':url}';
            }
            return $m[0] . $this->unresolvedMarker('kuma_media:' . $kumaMediaId);
        },
        $html,
    ) ?? $html;
}
```

Current NT cache limitation:
```php
$refNodeRows = $this->legacyDb->queryAll(
    'SELECT DISTINCT t.node_id, v.ref_id, v.ref_entity_name'
    . ' FROM kuma_node_translations t'
    . ' JOIN kuma_node_versions v ON v.id = t.public_node_version_id'
    . ' WHERE t.public_node_version_id IS NOT NULL',
);
```

From `src/finalize/FinalizeWalker.php`:
```php
/**
 * @return array{processed: int, rewritten: int, unresolvable: int}
 */
public function walk(MigrationFilters $filters, ?callable $onProgress = null): array
{
    // Counts unresolvable when rewritten HTML contains '<!-- MIGRATION:UNRESOLVED'
}
```

From `src/console/MigrateController.php` full migration finalize block:
```php
$finalizeCounts = $plugin->finalizeWalker->walk($filters, $finalizeProgress);
$report->incr('finalize.processed', (int) $finalizeCounts['processed']);
$report->incr('finalize.rewritten', (int) $finalizeCounts['rewritten']);
$report->incr('finalize.unresolvable', (int) $finalizeCounts['unresolvable']);
```

From `src/audit/PageRootedSurfaceDiscovery.php` current synthetic absence rows:
```php
// relationRows(): currently emits many_to_one/many_to_many/one_to_many unsupported rows
// when no relation metadata exists.
$rows[] = $this->row($pageFqcn, $missingType, self::CATEGORY_UNSUPPORTED, 'Doctrine relation metadata', $missingType . ':not-discovered', [
    'reason' => 'No relation metadata provided for this relation shape.',
]);

// metadataRows(): currently emits asset and ckeditor_ref warning rows when scanner input is empty.
$rows[] = $this->row($pageFqcn, $surfaceType, $missingCategory, $sourceService, $surfaceType . ':not-discovered', [
    'reason' => 'No structural discovery input provided for this surface.',
]);

// taxonomyAndDataProviderRows(): currently repeats every taxonomy/dataProvider proposal for every page root.
foreach ((array) ($mapping['proposals'] ?? []) as $proposal) {
    if (((string) ($proposal['kind'] ?? '')) !== $kind) {
        continue;
    }
    $rows[] = $this->row($pageFqcn, 'taxonomy_dataprovider', $category, ...);
}
```
</interfaces>
</context>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---:|---|---:|---|---|
| GOAL | Phase 10 | Clean generic migration rehearsal closure without silent loss of page-owned content | 10-06 | COVERED | This plan closes the release-owner blocking review for unresolved finalize tokens and Page-rooted blocking classifications. |
| REQ | PH10-04 | Taxonomy/relation resolution must not silently lose page-owned references | 10-06 | COVERED | Finalize NT resolution is broadened and unresolved NT/media references block live release cleanliness. |
| REQ | PH10-07 | Regression tests cover known generic failure classes without proprietary content | 10-06 | COVERED | Finalize and Page-rooted coverage unit tests use generic fixtures and pure seams. |
| REQ | PH10-08 | Rehearsal instructions include inspection gates for unresolved CKEditor markers and coverage | 10-06 | COVERED | Runbook requires zero unresolved finalize tokens and zero blocking coverage rows, allowing explicit accepted out_of_scope/dropped rows. |
| CONTEXT | D-08 | Fallback/synthesized behavior must be visible in migration report/log output | 10-06 | COVERED | Unresolved finalize diagnostics are reported with token family/reason instead of aggregate count only. |
| CONTEXT | D-09 | Clean live rehearsal requires zero entry/stage failures | 10-06 | COVERED | Nonzero live finalize unresolved count is made blocking for release cleanliness. |
| CONTEXT | D-10 | Every page-owned referenced surface must migrate or be explicitly out-of-scope/dropped | 10-06 | COVERED | Coverage discovery no longer marks generic absence as warning/unsupported, but preserves blocking rows where page-owned evidence exists. |
| CONTEXT | D-11 | Closing proof is restored-backup full workflow | 10-06 | COVERED | Summary must record before/after external CQM counts and whether the full CQM rerun was required. |
| RELEASE_OWNER | ROD-10-06 | Both unresolved finalize tokens and Page-rooted warning/unsupported rows are blocking for now | 10-06 | COVERED | All tasks implement this decision directly. |
| RESEARCH | Current CQM artifacts | `REPORT.md` has `failed=0` but `finalize.unresolvable=435`; transformed JSON still contains unresolved markers/tokens | 10-06 | COVERED | Task 1/2 add fixes, diagnostics, and a live blocking gate; verification includes external grep/count checks when available. |
| RESEARCH | Current coverage artifacts | `PAGE-ROOTED-COVERAGE.md` has synthetic warning/unsupported rows from no scanner/relation metadata and global taxonomy repetition | 10-06 | COVERED | Task 3 removes/downgrades synthetic absence rows and scopes taxonomy/dataProvider rows to page-owned evidence. |
| CONTEXT | Deferred | Full generic relation `mode: embed | promote` architecture | 10-06 | EXCLUDED | Not needed for the release-owner gate; do not implement relation architecture expansion. |
| CONTEXT | Deferred | Full orphan media import | 10-06 | EXCLUDED | Media placeholder resolution may use existing state/resolver seams only; do not add orphan media sync tooling. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Broaden finalize token resolution and expose unresolved diagnostics</name>

  <files>
src/finalize/CkeditorRewriterService.php,
src/finalize/FinalizeWalker.php,
tests/unit/finalize/CkeditorRewriterServiceTest.php
  </files>

  <read_first>
    - `src/finalize/CkeditorRewriterService.php` complete file.
    - `src/finalize/FinalizeWalker.php` complete file.
    - `tests/unit/finalize/CkeditorRewriterServiceTest.php` existing seed-cache tests.
    - `src/load/MigrationStateService.php` public `all(...)` row shape and meta decoding conventions.
    - `src/db/LegacyDbService.php` query helper signatures.
    - External CQM `REPORT.md` counts if available: `/Users/macbook25/Sites/cqm-craft-website/storage/migration/REPORT.md`.
  </read_first>

  <behavior>
    - Test 1: Raw `[NT80]` and encoded `%5BNT80%5D` are fully replaced by `{entry:<id>@<siteId>:url}` when `seedNtToEntryCache([80 => <id>])` is present; the output contains neither the legacy token nor `MIGRATION:UNRESOLVED`.
    - Test 2: Raw `[M482]` and encoded `%5BM482%5D` are fully replaced by `{asset:<id>@<siteId>:url}` when `seedKumaMediaIdCache([482 => <id>])` is present; the output contains neither the legacy token nor `MIGRATION:UNRESOLVED`.
    - Test 3: Unresolved NT/media tokens produce diagnostics containing token family (`nt` or `media`), legacy id, token literal, site id, and reason.
    - Test 4: NT cache warming can map a `kuma_node_translations.id` through state rows using both `sourceKey` and `meta.kumaNodeId` paths without Craft bootstrap by exercising a pure helper/test seam with fake state and legacy rows.
    - Test 5: Diagnostics are reset or consumed per rewrite/finalize field so stale unresolved rows from prior rewrites do not leak into later results.
  </behavior>

  <action>
    Implement diagnostics and generic NT-resolution hardening per ROD-10-06, D-08, and D-10.

    In `src/finalize/CkeditorRewriterService.php`:
    - Add a private diagnostics buffer such as `private array $unresolvedDiagnostics = [];`.
    - Add public/internal test seam `public function consumeUnresolvedDiagnostics(): array` that returns diagnostics and clears the buffer.
    - When `[M...]` or `%5BM...%5D` cannot resolve, append a diagnostic row before returning the legacy literal plus marker. Include keys: `tokenFamily` (`media`), `legacyId`, `siteId`, `token`, `source` (`kuma_media:<id>`), and `reason`.
    - When `[NT...]` or `%5BNT...%5D` cannot resolve, append the same shape with `tokenFamily` (`nt`) and `source` (`kuma_node_translation:<id>`).
    - Do not append unresolved diagnostics when a token resolves. For resolved tokens, return only the Craft ref token and never retain the original literal or marker.
    - Broaden `warmNtCache()` generically:
      - Keep current public-node-version path.
      - Add a schema-adaptive node-version load that inspects available legacy columns through `LegacyDbService` and uses every safe bridge available, including translation `node_id`, translation version-id columns when present, node-version `node_id` when present, and `kuma_node_versions.ref_id/ref_entity_name`.
      - Continue to map state rows by `source/sourceKey -> targetId` and by decoded `meta.kumaNodeId -> targetId`.
      - Preserve the current fallback where `sourceKey` may already equal `kuma_nodes.id`.
      - Record reason-specific cache-miss metadata where possible: no NT row, no node id, no node-version ref, no matching state row, or no entry target id.
      - Wrap optional legacy schema queries defensively as the existing code does, logging `Craft::warning(...)` instead of crashing the entire cache warm when a candidate column/table is unavailable.
    - Keep production logic generic: no CQM classes, IDs, handles, page names, or literal token IDs.

    In `src/finalize/FinalizeWalker.php`:
    - Extend `walk()` return shape to include diagnostics, for example `array{processed:int, rewritten:int, unresolvable:int, unresolvedDiagnostics:list<array<string,mixed>>}`.
    - After each field rewrite, call `$this->rewriter->consumeUnresolvedDiagnostics()` and attach entry/site/field context to each diagnostic row: Craft entry id, site id, field handle, and a stable source such as `FinalizeWalker`.
    - Preserve the existing `unresolvable` count behavior: increment when rewritten HTML contains `<!-- MIGRATION:UNRESOLVED`.
    - Do not include field body content or sample values in diagnostics.

    In `tests/unit/finalize/CkeditorRewriterServiceTest.php`:
    - Add RED tests for raw and encoded NT/media replacement removing originals when caches are seeded.
    - Add RED tests for unresolved diagnostics and consume/reset behavior.
    - Add a fake-state/fake-legacy test seam for the pure NT mapping logic. If direct `warmNtCache()` is not safe to call without Craft bootstrap, extract a private pure helper that accepts legacy rows/state rows and returns `ntId => targetId` plus reason maps, then invoke it via reflection.
  </action>

  <acceptance_criteria>
    - Resolved raw and encoded NT/media tokens leave no `[NT`, `%5BNT`, `[M`, `%5BM`, or `MIGRATION:UNRESOLVED` substring in the rewritten output.
    - Unresolved token diagnostics include token family, legacy id, token literal, reason, site id, and no content body values.
    - `FinalizeWalker::walk()` returns diagnostic rows alongside counts.
    - NT cache warming is broader than only `t.public_node_version_id -> v.id`; code contains a schema-adaptive path or helper that can consume all safe node-version/translation/state bridges.
    - No production code contains CQM-specific IDs/classes/handles.
  </acceptance_criteria>

  <verify>
    <automated>vendor/bin/phpunit tests/unit/finalize/CkeditorRewriterServiceTest.php --filter 'Nt|NodeTranslation|KumaMedia|Placeholder|Unresolved|Diagnostic|Encoded' --testdox</automated>
    <automated>php -l src/finalize/CkeditorRewriterService.php</automated>
    <automated>php -l src/finalize/FinalizeWalker.php</automated>
  </verify>

  <done>
    CKEditor rewrite has generic reasoned diagnostics, resolved tokens are fully replaced, and NT resolution has regression coverage for state/meta/version fallback paths without requiring Craft bootstrap.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make nonzero finalize unresolved counts release-blocking and documented</name>

  <files>
src/console/MigrateController.php,
tests/unit/console/MigrateControllerFailureExitTest.php,
.planning/rehearsal/v1.0/cqm/README.md
  </files>

  <read_first>
    - `src/console/MigrateController.php` full migration finalize block, standalone `actionFinalize()`, `writeReport(...)`, `renderReport(...)`, `reportExitCode(...)`, and `MigrationReport` usage.
    - `tests/unit/console/MigrateControllerFailureExitTest.php` existing report/failure/marker tests from plan 10-05.
    - `.planning/rehearsal/v1.0/cqm/README.md` current inspection gates.
    - External CQM `/Users/macbook25/Sites/cqm-craft-website/storage/migration/REPORT.md` if available.
  </read_first>

  <behavior>
    - Test 1: When live full migration receives finalize counts with `unresolvable > 0`, `MigrationReport` contains `finalize.unresolvable`, actionable finalize unresolved warning/failure text, and the command exits nonzero through the existing report exit path.
    - Test 2: When live finalize counts have `unresolvable = 0`, finalize does not create a blocking failure.
    - Test 3: Standalone `migrate/finalize --live` returns nonzero when unresolved diagnostics/counts remain and prints the same blocking semantics as full migration.
    - Test 4: Rendered REPORT.md includes a bounded diagnostics section/table for unresolved finalize tokens when diagnostics are present, without content bodies or sample values.
  </behavior>

  <action>
    Implement the release gate per ROD-10-06, D-09, D-10, and PH10-08.

    In `src/console/MigrateController.php`:
    - After the full migration finalize pass increments `finalize.processed`, `finalize.rewritten`, and `finalize.unresolvable`, check `(int) $finalizeCounts['unresolvable']`.
    - If nonzero:
      - Add an operator-visible report warning stating that live finalize left unresolved CKEditor references and release is blocked until they resolve or are explicitly classified outside the Page-rooted contract.
      - Record a blocking report failure using the existing `MigrationReport` failure path or the existing report-exit mechanism. The failure handler/message must include `finalize.unresolvable=<count>`.
      - Attach up to a bounded number of diagnostic rows from `FinalizeWalker` to report rendering. Use a stable cap such as 100 rows in REPORT.md while keeping aggregate counts complete.
      - Do not expose content bodies, sample values, proprietary text, or raw HTML in diagnostics.
    - Apply equivalent blocking behavior to standalone `actionFinalize()`:
      - It already requires `--live` for writes.
      - Return nonzero when unresolved count is nonzero.
      - Print a clear `FAIL finalize unresolved` message with the count.
      - If standalone finalize does not currently write `REPORT.md`, either wire it through existing report rendering with a finalize-only report or print diagnostics to console and return nonzero; full migration REPORT.md must still be detailed.
    - Preserve dry-run behavior: dry-run finalize is skipped and should not report a false unresolved failure because no finalize write pass ran.
    - Keep existing entry/stage failure semantics; this gate is additive and should not hide real load/finalize exceptions.

    In `tests/unit/console/MigrateControllerFailureExitTest.php`:
    - Add tests using existing controller test seams/reflection patterns to prove nonzero `finalize.unresolvable` produces a blocking exit/failure.
    - Add rendered report assertions for diagnostics headings/rows and absence of body/sample keys.
    - Add zero-unresolved regression assertion.

    In `.planning/rehearsal/v1.0/cqm/README.md`:
    - Update inspection gates to require:
      - `REPORT.md` contains `finalize.unresolvable | 0`.
      - No `MIGRATION:UNRESOLVED` remains in transformed/saved migration artifacts after live finalize.
      - No raw `[NT...]` or `[M...]` tokens remain in transformed CKEditor content where a target exists.
      - Nonzero unresolved finalize diagnostics block release until fixed or explicitly accepted outside Page-rooted release scope by the release owner.
    - Document that explicit `out_of_scope` and `dropped` coverage rows remain acceptable when intentionally classified.
  </action>

  <acceptance_criteria>
    - Live full migration cannot return release-clean success with `finalize.unresolvable > 0`.
    - Standalone live finalize cannot return success with `unresolvable > 0`.
    - REPORT.md rendering includes aggregate unresolved count plus bounded reasoned diagnostics.
    - README contains zero-unresolved and zero-blocking-coverage release gates.
    - No dry-run path falsely fails on finalize unresolved counts because finalize is not executed in dry-run.
  </acceptance_criteria>

  <verify>
    <automated>vendor/bin/phpunit tests/unit/console/MigrateControllerFailureExitTest.php --filter 'Finalize|Unresolvable|Unresolved|Report|Blocking' --testdox</automated>
    <automated>php -l src/console/MigrateController.php</automated>
    <automated>grep -n "finalize.unresolvable" .planning/rehearsal/v1.0/cqm/README.md</automated>
  </verify>

  <done>
    Nonzero unresolved finalize output is a blocking release condition in code and runbook, with actionable diagnostics rather than a silent aggregate count.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reclassify Page-rooted coverage absence rows using evidence-based discovery</name>

  <files>
src/audit/PageRootedSurfaceDiscovery.php,
src/audit/PageRootedCoverageAuditor.php,
tests/unit/audit/PageRootedSurfaceDiscoveryTest.php,
tests/unit/audit/PageRootedCoverageAuditorTest.php,
.planning/rehearsal/v1.0/cqm/README.md
  </files>

  <read_first>
    - `src/audit/PageRootedSurfaceDiscovery.php` complete file.
    - `src/audit/PageRootedCoverageAuditor.php` complete file.
    - `tests/unit/audit/PageRootedSurfaceDiscoveryTest.php` existing expected surface types and no-input test.
    - `tests/unit/audit/PageRootedCoverageAuditorTest.php` current synthetic warning expectations.
    - `src/console/CompileController.php` page-rooted coverage write path around discovery/auditor invocation.
    - External `/Users/macbook25/Sites/cqm-craft-website/storage/migration/PAGE-ROOTED-COVERAGE.md` if available.
  </read_first>

  <behavior>
    - Test 1: When `serviceMetadata` has no `assets` scanner input, discovery does not emit an `asset:not-discovered` warning row. It either emits no asset row or emits a non-blocking `out_of_scope`/`no_input` row.
    - Test 2: When `serviceMetadata` has no `ckeditorRefs` scanner input, discovery does not emit a `ckeditor_ref:not-discovered` warning row. It either emits no CKEditor row or emits a non-blocking `out_of_scope`/`no_input` row.
    - Test 3: When relation metadata input is absent for a page, discovery does not emit synthetic `many_to_one:not-discovered`, `many_to_many:not-discovered`, or `one_to_many:not-discovered` unsupported rows.
    - Test 4: When actual relation metadata exists, discovered `ManyToOne`, `ManyToMany`, and `OneToMany` rows still appear and keep their existing migrated/unsupported semantics.
    - Test 5: Taxonomy/dataProvider rows are associated only with pages having page-owned evidence through mapped fields, relation metadata, page structure, or explicit mapping linkage. Global proposals without page-owned evidence are not repeated for every page root.
    - Test 6: Implicit-content warnings are emitted only for pages with structural source evidence of implicit content needing migration and no mapping. Pages with no implicit-content source evidence get non-blocking no-input/out_of_scope treatment.
    - Test 7: Accepted migrated rows, operator-curated dropped rows, and explicit out_of_scope rows remain rendered and deterministic.
  </behavior>

  <action>
    Implement evidence-based Page-rooted coverage cleanup per ROD-10-06 and D-10.

    In `src/audit/PageRootedSurfaceDiscovery.php`:
    - Change `serviceRows(...)` / `metadataRows(...)` behavior for `asset` and `ckeditor_ref`:
      - If the relevant scanner key is absent from `serviceMetadata`, treat that as no scanner input, not page-owned warning evidence.
      - Do not emit warning rows solely because scanner input was absent.
      - If a row is emitted for scanner absence, its `categoryHint` must be `out_of_scope` and `sourceIdentifier` should be a non-blocking `*:no-input`/`*:scanner-not-run` style identifier; it must not be `warning`.
      - If scanner metadata rows are present, keep emitting migrated/evidence rows as before.
    - Change `relationRows(...)`:
      - Remove synthetic `many_to_one`, `many_to_many`, and `one_to_many` unsupported rows when no relation metadata exists.
      - Only emit `many_to_*` rows for actual relation metadata rows.
      - If a no-input relation row is needed for operator visibility, emit at most one non-blocking `relation_metadata:no-input` out_of_scope row per page; do not emit one unsupported row per relation shape.
    - Change `taxonomyAndDataProviderRows(...)`:
      - Do not loop every global taxonomy/dataProvider proposal into every page.
      - Add a helper that determines page-owned evidence for a taxonomy/dataProvider proposal. Accept generic evidence only: mapped field specs referencing the proposal FQCN/source table/state source, relation metadata targeting the proposal FQCN, pageStructure references, or explicit proposal parent/page linkage.
      - Emit page rows only when evidence connects the proposal to the page.
      - For proposals with no page-owned evidence anywhere, emit either no page-rooted row or one global non-blocking out_of_scope/informational row that is not repeated across every page. If a global row is emitted, use a stable non-page identifier such as `__global__` and keep renderer output deterministic.
    - Change implicit-content discovery:
      - Keep migrated implicit-content rows for configured `__implicit_content__|<Page>|<context>` mappings.
      - Emit a blocking warning only when pageStructure/mapping has structural evidence that the page has implicit content/pagepart source rows requiring migration and no mapping exists.
      - For pages with no implicit-content source evidence, do not emit the current warning `No implicit content page-part mapping is configured.`; either emit no row or emit non-blocking out_of_scope/no_input.
    - Preserve true migrated, dropped, unsupported, warning, and out_of_scope rows when they are backed by actual page-owned evidence.
    - Do not remove operator-curated drops or accepted exclusions.

    In `src/audit/PageRootedCoverageAuditor.php`:
    - Keep classifier/render pure and deterministic.
    - Adjust only if needed to support a non-page global informational row or new no-input identifiers without converting them back to warnings.
    - Preserve allowed categories: `migrated`, `dropped`, `out_of_scope`, `unsupported`, `warning`.

    In tests:
    - Update `PageRootedSurfaceDiscoveryTest::testMissingOptionalServiceInputsCreateExplicitClassifications()` so it expects no blocking warning/unsupported rows from absent scanner/relation input.
    - Add coverage for actual metadata rows still producing evidence rows.
    - Add taxonomy/dataProvider multi-page fixture proving global proposals are not repeated across every page.
    - Update `PageRootedCoverageAuditorTest` expectations that currently assert `ckeditor_ref:not-discovered` is warning; it must become non-blocking or absent when no scanner input exists.
    - Add regression assertions that dropped/out_of_scope rows remain visible.

    In `.planning/rehearsal/v1.0/cqm/README.md`:
    - Add Page-rooted coverage release gate:
      - Zero `warning` or `unsupported` rows unless each row has real page-owned evidence and an explicit release-owner disposition.
      - `out_of_scope` and `dropped` rows are acceptable when intentional and documented.
      - Scanner absence/no metadata rows are informational and must not block unless page-owned structural evidence exists.
  </action>

  <acceptance_criteria>
    - No-input `asset:not-discovered` and `ckeditor_ref:not-discovered` rows no longer produce blocking warnings.
    - Missing relation metadata no longer produces synthetic unsupported `many_to_*:not-discovered` rows for every page.
    - Taxonomy/dataProvider proposals without page-owned evidence are not repeated across all page roots.
    - Implicit-content warnings require actual structural source evidence.
    - Real discovered relation, asset, CKEditor, taxonomy/dataProvider, migrated, dropped, and out_of_scope rows remain visible and deterministic.
  </acceptance_criteria>

  <verify>
    <automated>vendor/bin/phpunit tests/unit/audit/PageRootedSurfaceDiscoveryTest.php tests/unit/audit/PageRootedCoverageAuditorTest.php --filter 'PageRooted|Missing|Service|Relation|Taxonomy|DataProvider|Implicit|Coverage|Dropped|OutOfScope' --testdox</automated>
    <automated>php -l src/audit/PageRootedSurfaceDiscovery.php</automated>
    <automated>php -l src/audit/PageRootedCoverageAuditor.php</automated>
    <automated>grep -n "Page-rooted coverage" .planning/rehearsal/v1.0/cqm/README.md</automated>
  </verify>

  <done>
    Page-rooted coverage blocking classifications now require structural page-owned evidence, while intentional migrated/dropped/out_of_scope classifications remain visible.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Legacy DB -> migration plugin | Untrusted/variable legacy schema and content drive NT/media cache warming and diagnostics. |
| Transformed CKEditor HTML -> Craft save pipeline | Legacy tokens and unresolved markers can enter Craft field values during finalize. |
| Migration artifacts -> operator reports | REPORT.md and PAGE-ROOTED-COVERAGE.md communicate release decisions and must not leak content bodies or hide blocking evidence. |
| Mapping/pageStructure/service metadata -> coverage classifier | Structural metadata can be absent, partial, or stale; absence must not be misrepresented as page-owned missing content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-06-01 | Tampering | `CkeditorRewriterService::warmNtCache()` | mitigate | Use schema-adaptive read-only legacy queries, validate numeric IDs before mapping, and preserve existing defensive `try/catch` warnings for optional legacy columns. |
| T-10-06-02 | Information Disclosure | Finalize diagnostics / REPORT.md | mitigate | Diagnostics must include token family, numeric ids, field handle, entry id, site id, and reason only; never include CKEditor body HTML, samples, proprietary text, or raw field content. |
| T-10-06-03 | Repudiation | Release gate for unresolved finalize tokens | mitigate | Record `finalize.unresolvable=<count>` in REPORT.md and command exit path so a non-clean release state is auditable. |
| T-10-06-04 | Denial of Service | Rendering many unresolved diagnostics | mitigate | Bound report diagnostic rows to a stable cap while preserving aggregate counts. Avoid scanning content repeatedly beyond the existing finalize pass. |
| T-10-06-05 | Information Disclosure | Page-rooted coverage metadata | mitigate | Preserve existing structural-only behavior; do not emit samples, body, content, or values in discovery/auditor rows. |
| T-10-06-06 | Tampering | Coverage reclassification | mitigate | Reclassify only no-input/no-evidence rows as non-blocking. Keep real page-owned discovered evidence as warning/unsupported/migrated/dropped/out_of_scope according to mapping and structural metadata. |
| T-10-06-07 | Elevation of Privilege | Console migration commands | accept | No new auth/network surface is introduced; commands remain existing local Craft console commands behind current NeverProductionTrait gates. |
</threat_model>

<verification>
Run these commands after implementing all tasks:

```bash
vendor/bin/phpunit tests/unit/finalize/CkeditorRewriterServiceTest.php --filter 'Nt|NodeTranslation|KumaMedia|Placeholder|Unresolved|Diagnostic|Encoded' --testdox
vendor/bin/phpunit tests/unit/console/MigrateControllerFailureExitTest.php --filter 'Finalize|Unresolvable|Unresolved|Report|Blocking' --testdox
vendor/bin/phpunit tests/unit/audit/PageRootedSurfaceDiscoveryTest.php tests/unit/audit/PageRootedCoverageAuditorTest.php --filter 'PageRooted|Missing|Service|Relation|Taxonomy|DataProvider|Implicit|Coverage|Dropped|OutOfScope' --testdox
php -l src/finalize/CkeditorRewriterService.php
php -l src/finalize/FinalizeWalker.php
php -l src/console/MigrateController.php
php -l src/audit/PageRootedSurfaceDiscovery.php
php -l src/audit/PageRootedCoverageAuditor.php
composer test
```

If the external CQM artifacts are available, run these read-only checks and record before/after counts in `10-06-SUMMARY.md`:

```bash
grep -n "finalize.unresolvable" /Users/macbook25/Sites/cqm-craft-website/storage/migration/REPORT.md
grep -n "Total failed\|_No per-entry failures_" /Users/macbook25/Sites/cqm-craft-website/storage/migration/REPORT.md
grep -RIl "MIGRATION:UNRESOLVED" /Users/macbook25/Sites/cqm-craft-website/storage/migration/transformed 2>/dev/null | wc -l
grep -Roh "\[NT[0-9]\+\]" /Users/macbook25/Sites/cqm-craft-website/storage/migration/transformed 2>/dev/null | wc -l
grep -Roh "\[M[0-9]\+\]" /Users/macbook25/Sites/cqm-craft-website/storage/migration/transformed 2>/dev/null | wc -l
grep -n "warning\|unsupported\|asset:not-discovered\|ckeditor_ref:not-discovered\|many_to_.*:not-discovered\|No implicit content page-part mapping is configured" /Users/macbook25/Sites/cqm-craft-website/storage/migration/PAGE-ROOTED-COVERAGE.md
```

If code changes affect compile output semantics, rerun compile/page-rooted coverage in the target Craft project before final acceptance and compare:
- `asset:not-discovered` warning rows
- `ckeditor_ref:not-discovered` warning rows
- `many_to_one:not-discovered`, `many_to_many:not-discovered`, `one_to_many:not-discovered` unsupported rows
- repeated taxonomy/dataProvider rows across all page roots
- implicit_content warnings without source evidence

Do not require a full restored CQM rerun unless targeted tests plus compile/report regeneration cannot prove the release-owner gate. If a full rerun is performed, record the exact command sequence and final counts in `10-06-SUMMARY.md`.
</verification>

<success_criteria>
Plan 10-06 is complete when:

- [ ] Resolved raw and URL-encoded `[NT...]` and `[M...]` placeholders are replaced without retaining legacy token literals or unresolved markers.
- [ ] Unresolved finalize diagnostics distinguish NT vs media causes and include reasoned, structural metadata only.
- [ ] `finalize.unresolvable > 0` blocks live release cleanliness in full migration and standalone live finalize paths.
- [ ] REPORT.md includes actionable unresolved finalize diagnostics when unresolved tokens remain.
- [ ] Page-rooted coverage no longer emits blocking warning/unsupported rows solely because asset/CKEditor scanner input or relation metadata is absent.
- [ ] Taxonomy/dataProvider proposals are not repeated across every page root without page-owned evidence.
- [ ] Implicit-content warnings are emitted only when structural source evidence shows implicit content needing migration and no mapping exists.
- [ ] Migrated, dropped, and explicit out_of_scope rows remain visible and deterministic.
- [ ] CQM runbook states the release gate: zero unresolved finalize tokens and zero blocking Page-rooted warning/unsupported rows, while allowing explicit accepted out_of_scope/dropped rows.
- [ ] Targeted PHPUnit, PHP lint, and `composer test` pass.
- [ ] `10-06-SUMMARY.md` records before/after CQM counts and states whether a full CQM rerun was required.
</success_criteria>

<output>
After completion, create `.planning/phases/10-generic-migration-rehearsal-gap-closure/10-06-SUMMARY.md`.

The summary must include:
- Before counts from current CQM artifacts when available:
  - `finalize.unresolvable = 435`
  - transformed JSON `MIGRATION:UNRESOLVED` file/count baseline
  - transformed JSON `[NT...]` file/count baseline
  - transformed JSON `[M...]` file/count baseline
  - Page-rooted coverage warning/unsupported baseline by collapsed finding group
- After counts from targeted tests and any regenerated CQM artifacts.
- Whether the full restored CQM workflow was rerun.
- If unresolved tokens remain, the diagnostic reason groups and the next generic fix path.
- If blocking Page-rooted coverage rows remain, each row group with evidence and release-owner disposition needed.
- Confirmation that no CQM-specific production logic was introduced.
</output>
