---
phase: 08-taxonomies-and-proposers
plan: 17
type: execute
wave: 5
depends_on: [08-11, 08-12, 08-15]
files_modified:
  - .planning/phases/08-taxonomies-and-proposers/08-RECONCILIATION.md
  - CHANGELOG.md
  - README.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "08-RECONCILIATION.md exists and documents every v1 TaxonomyMigrationService rule with explicit disposition: kept-verbatim / reshaped-for-single-mapping.yaml / reshaped-for-atomic-always-on / reshaped-for-D-09 / dropped"
    - "CHANGELOG.md has a 'Known omissions in v1.0' section under the v1.0 entry listing FormBundle, SearchBundle, MenuBundle, users/roles/ACLs, kuma_translations, media folder hierarchy, asset metadata, slug history, drafts/non-public node versions"
    - "README.md Quickstart section cross-links to the CHANGELOG known-omissions section"
    - "PROJECT.md Out of Scope table cross-links to the CHANGELOG known-omissions section"
    - "REQUIREMENTS.md gains TAX-01..TAX-10 + PROP-01..PROP-06 + DOC-01..DOC-02 IDs"
  artifacts:
    - path: ".planning/phases/08-taxonomies-and-proposers/08-RECONCILIATION.md"
      provides: "Phase 8 verbatim-port reconciliation doc"
      contains: "Phase 8 — Reconciliation"
    - path: "CHANGELOG.md"
      provides: "Known omissions in v1.0 section"
      contains: "Known omissions in v1.0"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TAX + PROP + DOC requirement IDs"
      contains: "TAX-01"
  key_links:
    - from: "README.md Quickstart"
      to: "CHANGELOG.md Known omissions in v1.0"
      via: "markdown anchor link"
      pattern: "known-omissions"
    - from: ".planning/PROJECT.md Out of Scope"
      to: "CHANGELOG.md Known omissions in v1.0"
      via: "markdown anchor link"
      pattern: "known-omissions"
---

<objective>
Final documentation pass: write Phase 8's RECONCILIATION.md (D-08 mandate), append "Known omissions in v1.0" to CHANGELOG.md (ROADMAP success criterion 8), cross-link from README and PROJECT.md, and codify TAX/PROP/DOC requirement IDs in REQUIREMENTS.md.

Purpose: DOC-01 + DOC-02 closure. v1.0 tag-readiness depends on operators being able to find what's in / out of scope without reading the source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-taxonomies-and-proposers/08-CONTEXT.md
@.planning/phases/08-taxonomies-and-proposers/08-PATTERNS.md
@CHANGELOG.md
@README.md

<interfaces>
<!-- Prior phase RECONCILIATION.md examples in .planning/phases/04-adapters-verify-settings/04-12-... and .planning/phases/02.1-source-introspection/02.1-09-...; mirror their shape. -->

<!-- ROADMAP success criterion 8: "Known omissions in v1.0 section listing Kunstmaan surfaces this migrator deliberately does NOT cover: FormBundle, SearchBundle, MenuBundle, user accounts / roles / ACLs, `kuma_translations` (i18n string catalog), media folder hierarchy, asset metadata (alt text / focal point), slug history (Retour-style mining beyond `kuma_redirects`)." -->

<!-- CONTEXT deferred ideas: drafts / non-public node versions are explicitly out of scope forever (NOT deferred). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 08-RECONCILIATION.md</name>
  <files>.planning/phases/08-taxonomies-and-proposers/08-RECONCILIATION.md</files>
  <read_first>
    - ~/Sites/craft-kunstmaan-migrator/src/bridge/load/TaxonomyMigrationService.php (the v1 source — all 443 LOC)
    - src/load/TaxonomyMigrationService.php (Plan 11 — the v2 port)
    - .planning/phases/04.1-polish-recovery-and-env-defaults/04.1-07-recovery-commands-PLAN.md (RECONCILIATION reference example — find the latest 04.1 RECONCILIATION.md it references and mirror its table shape)
    - .planning/phases/02.1-source-introspection/02.1-09-reconciliation-PLAN.md (another RECONCILIATION example — mirror shape)
    - .planning/phases/08-taxonomies-and-proposers/08-CONTEXT.md (D-08 verbatim-port mandate)
    - .planning/phases/08-taxonomies-and-proposers/08-PATTERNS.md (5 reshape points enumerated)
  </read_first>
  <behavior>
    - Doc has H1 "Phase 8 — Reconciliation" + brief context paragraph naming D-08 verbatim-port mandate.
    - Rule-by-rule disposition table covering every v1 TaxonomyMigrationService rule (the 443-LOC walkthrough). Each row: (v1 rule, v2 disposition, line range in v2 file, rationale).
    - Five reshape entries called out explicitly (PATTERNS.md gotcha section): single mapping.yaml, atomic-always-on, D-09 empty-table fallback, taxonomies block compiler-emitted shape, detection-inside-service short-circuit.
    - Note that `relation:deferred` finalize-pass (Phase 4.1 / REC-02) stays unimplemented (D-03 confirms taxonomies-before-pages avoids that case).
    - Note that `extTranslationsFor()` was restored on v2 LegacyDbService (Plan 04 / SRC-11 ??=).
  </behavior>
  <action>
    Create `.planning/phases/08-taxonomies-and-proposers/08-RECONCILIATION.md`:

    ```markdown
    # Phase 8 — Reconciliation

    **Phase:** 08-taxonomies-and-proposers
    **Reconciliation date:** 2026-04-XX (fill in)
    **Verbatim-port reference:** `~/Sites/craft-kunstmaan-migrator/src/bridge/load/TaxonomyMigrationService.php` (443 LOC)
    **v2 implementation:** `src/load/TaxonomyMigrationService.php` (Plan 08-11)

    Per Phase 4 / D-54 verbatim-port discipline (re-asserted in Phase 8 / D-08), this document records every v1 rule and its v2 disposition.

    ## Reshape decisions

    | # | v1 Rule | v2 Disposition | Rationale |
    |---|---------|----------------|-----------|
    | 1 | Three mapping files (`mapping.yaml` + `.draft` + `mapping-drops-{ts}.yaml`) read via `MappingLoader` | **Reshape: single mapping.yaml.** v2 uses `MappingFile->load()`. | PROJECT.md ground rule — single mapping.yaml with per-row `status:`. |
    | 2 | Per-row `Craft::$app->db->transaction` (atomic save) | **Kept verbatim.** No `--atomic` flag check (v1's flag is dropped). | PROJECT.md ground rule — atomic-always-on. |
    | 3 | `taxonomies:` block raw-edited by operator | **Reshape: compiler-emitted.** Block now produced by `MappingCompiler::compileTaxonomies()` (Plan 09) from `kind=taxonomy` rows + same-`sourceTable` `kind=column` rows. Operator-edited block still wins (skip-existing per MAP-04). | D-07 mandate — single mapping.yaml + per-row status drives compile. |
    | 4 | `extTranslationsFor()` returns rows for non-empty Gedmo Translatable installs | **Kept verbatim.** Restored on v2 `LegacyDbService` (Plan 04). | D-08 verbatim-port. |
    | 5 | Empty `ext_translations` → exception OR silent monolingual write | **Reshape: explicit D-09 fallback.** When `extTranslationsFor()` returns `[]`, copy source-locale row across every site in `mapping.sites`. WARN line via doctor 11th check (Plan 14). | D-09 mandate — pragmatic monolingual default. |
    | 6 | Source-table regex whitelist (`preg_match('/^[a-z0-9_]+$/', $sourceTable)`) | **Kept verbatim.** | SQL injection defense — preserved verbatim. |
    | 7 | Site-agnostic state rows (`siteId=null` on `migrationState->record`) | **Kept verbatim.** | Taxonomies are language-neutral; state record matches v1. |
    | 8 | Taxonomy entries enabled for ALL sites globally (overrides Section's `enabledByDefault=false`) | **Kept verbatim.** | Language-neutral entry behavior preserved. |
    | 9 | `kunstmaanSourceId` field NOT touched (taxonomy entries don't carry it) | **Kept verbatim.** | State table is the only identity record. |
    | 10 | `service.migrateAll()` short-circuits when no taxonomies block | **Reshape: detection-inside-service WARN line.** Mirrors `SeoMigrationService` empty-mapping warn (Phase 4 / D-56). | Phase 4 / D-56 pattern preserved across all load services. |
    | 11 | Per-locale Gedmo overlay (skip primary site; normalize Craft language to base locale; `propagateChanges=false`) | **Kept verbatim.** | Locale fan-out logic preserved exactly. |
    | 12 | `--atomic` CLI flag (v1) | **Dropped.** Atomic-always-on. | PROJECT.md ground rule. |
    | 13 | v1's `MappingLoader` injection | **Replaced** with v2's `MappingFile`. Plan 08-11 wires `mappingFile` instead of `mappingLoader`. | Single mapping.yaml. |

    ## Open / deferred items

    - **`relation:deferred` finalize-pass** (Phase 4.1 / REC-02): stays unimplemented. D-03 (taxonomies-before-pages run order) avoids the deferred-relation case entirely. RelationHandler resolves category FKs via the existing state table.
    - **`--no-taxonomies` CLI flag**: deliberately not added. D-04 + Phase 2 / D-12 three-flag cap (`--entities`, `--locales`, `--since`) preserved. Operator's escape hatch is `taxonomies[fqcn].action: SKIP` (per-row) or omission of the row.

    ## Cross-references

    - PROJECT.md ground rules (single mapping.yaml; atomic-always-on; runtime-zero-AI; never-production gate).
    - Phase 4 / D-54 (verbatim-port discipline; RECONCILIATION.md mandate per port).
    - Phase 4.1 / SRC-20 (attributes-only DoctrineEntityParser — extended by Plan 02 to include Gedmo namespace; SRC-20 invariant preserved).
    - Phase 8 CONTEXT.md (D-01..D-14).
    ```

    Adapt the line ranges and any specific v1 rule that PATTERNS.md doesn't enumerate but that you find while reading the v1 source.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-taxonomies-and-proposers/08-RECONCILIATION.md && grep -c "Phase 8 — Reconciliation" .planning/phases/08-taxonomies-and-proposers/08-RECONCILIATION.md | grep -v '^#' | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/08-taxonomies-and-proposers/08-RECONCILIATION.md` succeeds
    - `grep -c "Phase 8 — Reconciliation" .planning/phases/08-taxonomies-and-proposers/08-RECONCILIATION.md` returns 1
    - `grep -c "Reshape\|Kept verbatim\|Dropped" .planning/phases/08-taxonomies-and-proposers/08-RECONCILIATION.md` returns at least 5
    - `grep -c "D-09" .planning/phases/08-taxonomies-and-proposers/08-RECONCILIATION.md` returns at least 1
    - `grep -c "single mapping.yaml" .planning/phases/08-taxonomies-and-proposers/08-RECONCILIATION.md` returns at least 1
  </acceptance_criteria>
  <done>RECONCILIATION.md exists; reshape table complete with at least 13 rows; D-08 mandate satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG.md known-omissions section + README + PROJECT cross-links</name>
  <files>CHANGELOG.md, README.md, .planning/PROJECT.md</files>
  <read_first>
    - CHANGELOG.md (full file — already in Keep-a-Changelog format from Phase 5 / Plan 05-08; find the v1.0 entry)
    - README.md (Quickstart section — anchor target for cross-link)
    - .planning/PROJECT.md (Out of Scope section — anchor target for cross-link)
    - .planning/phases/08-taxonomies-and-proposers/08-CONTEXT.md (specifics + deferred lists)
    - .planning/ROADMAP.md (Phase 8 success criterion 8)
  </read_first>
  <behavior>
    - CHANGELOG.md gains a "### Known omissions in v1.0" sub-heading under the v1.0 entry listing: FormBundle, SearchBundle, MenuBundle, user accounts/roles/ACLs, `kuma_translations` (i18n string catalog), media folder hierarchy, asset metadata (alt text / focal point), slug history (beyond `kuma_redirects`), drafts / non-public node versions.
    - README.md Quickstart section gains a sentence + link: "_For Kunstmaan surfaces this migrator deliberately does not cover, see [Known omissions in v1.0](CHANGELOG.md#known-omissions-in-v10)._"
    - PROJECT.md Out of Scope section gains a sentence + link: "_See also [CHANGELOG.md > Known omissions in v1.0](../CHANGELOG.md#known-omissions-in-v10) for the operator-facing summary._" (or whatever the relative path resolves to from PROJECT.md location).
  </behavior>
  <action>
    Step A — CHANGELOG.md. Locate the v1.0 entry. Inside it, append:

    ```markdown
    ### Known omissions in v1.0

    The following Kunstmaan surfaces are deliberately out of scope for v1.0:

    - **FormBundle** — form schemas + submissions are not migrated.
    - **SearchBundle** — Elasticsearch / search-index integration is not migrated.
    - **MenuBundle** — menu trees are not migrated.
    - **User accounts / roles / ACLs** — operator authentication and authorization are not migrated.
    - **`kuma_translations`** — Kunstmaan's i18n string catalog (distinct from `ext_translations`, which Phase 8 DOES support for Gedmo Translatable taxonomies).
    - **Media folder hierarchy** — `kuma_media_folder` parent_id chains are dropped at migrate time; assets land flat. May become a v1.1 phase if a real project demands it.
    - **Asset metadata** — alt text, copyright, focal point. May become a v1.1 phase.
    - **Slug history** — beyond `kuma_redirects`, Kunstmaan tracks slug changes that could feed Retour as historical redirects. Deferred.
    - **Drafts / non-public node versions** — `streamLiveNodes` filters `online=1 AND public_node_version_id`; drafts are explicitly skipped by design (carryover from v1). Permanently out of scope.

    Operators needing any of these should treat this migrator as a starting point and write project-specific extensions.
    ```

    Step B — README.md. Locate the Quickstart section. Append (or insert near the start of the section):

    ```markdown
    > For Kunstmaan surfaces this migrator deliberately does NOT cover (FormBundle, SearchBundle, MenuBundle, user accounts, media folder hierarchy, slug history beyond `kuma_redirects`, drafts), see [Known omissions in v1.0](CHANGELOG.md#known-omissions-in-v10).
    ```

    Step C — PROJECT.md. Locate the Out of Scope section. Append:

    ```markdown
    > See also [CHANGELOG.md > Known omissions in v1.0](../../CHANGELOG.md#known-omissions-in-v10) for the operator-facing summary.
    ```

    (Adjust the relative path to match PROJECT.md's actual location: it sits at `.planning/PROJECT.md`, so the link to repo-root `CHANGELOG.md` is `../CHANGELOG.md`. Confirm before writing.)
  </action>
  <verify>
    <automated>grep -c "Known omissions in v1.0" CHANGELOG.md | grep -v '^#' | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "FormBundle" CHANGELOG.md | grep -v '^#' | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "known-omissions-in-v10" README.md | grep -v '^#' | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "known-omissions-in-v10" .planning/PROJECT.md | grep -v '^#' | awk '{ if ($1 >= 1) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Known omissions in v1.0" CHANGELOG.md` returns at least 1
    - `grep -c "FormBundle\|SearchBundle\|MenuBundle\|kuma_translations" CHANGELOG.md` returns at least 4
    - `grep -c "known-omissions-in-v10" README.md` returns at least 1
    - `grep -c "known-omissions-in-v10" .planning/PROJECT.md` returns at least 1
  </acceptance_criteria>
  <done>CHANGELOG known-omissions section + cross-links from README and PROJECT in place.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md TAX/PROP/DOC ID codification</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — note existing requirement-block conventions: ID, description, acceptance, traceability)
    - .planning/ROADMAP.md (Phase 8 success criteria 1-8 — the source for ID derivation)
    - .planning/phases/08-taxonomies-and-proposers/08-CONTEXT.md (D-01..D-14 — informs each ID's acceptance)
  </read_first>
  <behavior>
    - REQUIREMENTS.md gains a new "### Taxonomies (TAX)" subsection with TAX-01..TAX-10.
    - REQUIREMENTS.md gains a new "### AI Proposer Coverage (PROP)" subsection with PROP-01..PROP-06.
    - REQUIREMENTS.md gains a new "### Phase 8 Documentation (DOC)" subsection with DOC-01..DOC-02.
    - Each requirement: ID, brief description, acceptance criterion, decision/plan reference.
  </behavior>
  <action>
    Mirror the existing requirement-section format (read the file to see the convention — the existing sections like `### ETL pipeline (ETL)` show the block shape).

    Add:

    ```markdown
    ### Taxonomies (TAX)

    - **TAX-01** — `mapping.taxonomies` block accepted by MappingFile / MappingAuditor / MappingCompiler. Identity tuple `(kind, fqcn)`. Field-level mapping inferred from same-sourceTable `kind=column` rows. (Plans 08-01, 08-09; D-07.)
    - **TAX-02** — Analyze AI proposer emits taxonomy candidates via `proposeNonPageEntities` for non-Page Doctrine entities. (Plan 08-05; D-05.)
    - **TAX-03** — `--entities=` filter scoping auto-includes taxonomies referenced by allowed FQCNs (relation-graph reachability); no `--taxonomies=` flag added. (Plan 08-10; D-04.)
    - **TAX-04** — DoctrineEntityParser captures Gedmo Translatable as a per-property attribute scan; SRC-20 attributes-only invariant preserved. (Plan 08-02; D-10 signal #1.)
    - **TAX-05** — KnowledgeBase exposes `renderTaxonomiesMarkdown()` mirroring `renderPagePartsMarkdown()`. (Plan 08-03.)
    - **TAX-06** — `LegacyDbService::extTranslationsFor()` restored on v2 (verbatim port from v1); `KunstmaanCoreTables::EXT_TRANSLATIONS = 'ext_translations'` constant added. (Plan 08-04; D-08.)
    - **TAX-07** — `TaxonomyMigrationService` ported verbatim from v1 (443 LOC) with five documented reshape points. (Plan 08-11; D-08.)
    - **TAX-08** — `TaxonomyMigrationService` wired into Plugin DI + MigrateController; runs BEFORE pages in actionIndex; `actionTaxonomies` resume sub-action exists. (Plan 08-12; D-03.)
    - **TAX-09** — Doctor 11th check for `ext_translations` presence (WARN-only when empty per D-09; never FAIL). (Plan 08-14; D-09.)
    - **TAX-10** — Integration test (`TaxonomyMigrationTest.php`) drives one taxonomy entity through analyze → compile → load and asserts a Craft entry exists; unit tests for `MappingCompiler::compileTaxonomies()`. (Plans 08-15, 08-16.)

    ### AI Proposer Coverage (PROP)

    - **PROP-01** — `LlmClassifier::proposeNonPageEntities()` emits kind=taxonomy rows (high → accepted, medium/low → needs-review) and kind=column drop rows (`reason=not-taxonomy-likely-supporting`) per the Phase 2 / D-02 confidence-tier ladder. (Plan 08-05; D-05, D-06.)
    - **PROP-02** — `LlmClassifier::proposeLayoutBlocks()` emits kind=nodeClass partial-update rows for `headerBlock` / `bodyWrapBlock` / `bodyColumn` slots, gated by Matrix-catalog signal heuristic. (Plan 08-06; D-11, D-12.)
    - **PROP-03** — `LlmClassifier::proposeDataProviders()` emits kind=dataProvider rows for orphan page-parts (no `kuma_page_part_refs` row + sourceTable not joined to `kuma_node_versions`). (Plan 08-07; D-13.)
    - **PROP-04** — `Settings::proposeLayout` + `Settings::proposeProviders` (default true) + CLI flags `--no-layout` / `--no-providers` + CP `_settings.twig` "AI" H2 group. Mirrors Phase 4.1 / ADP-04 ladder. (Plans 08-08, 08-13; D-14.)
    - **PROP-05** — `MappingCompiler` folds layout-block proposals into nodeClasses (skip-existing per slot) and emits top-level `mapping.dataProviders`. `_compileReport` surfaces `taxonomiesEmitted`, `layoutBlocksEmitted`, `dataProvidersEmitted`. (Plan 08-09.)
    - **PROP-06** — Unit tests (`MappingCompilerLayoutBlocksTest`, `MappingCompilerDataProvidersTest`) pin compile invariants. (Plan 08-16.)

    ### Phase 8 Documentation (DOC)

    - **DOC-01** — `08-RECONCILIATION.md` documents every v1 `TaxonomyMigrationService` rule with explicit v2 disposition (kept-verbatim / reshaped / dropped). (Plan 08-17; D-08 mandate.)
    - **DOC-02** — `CHANGELOG.md` "Known omissions in v1.0" section lists Kunstmaan surfaces deliberately out of scope (FormBundle, SearchBundle, MenuBundle, user accounts/roles/ACLs, `kuma_translations`, media folder hierarchy, asset metadata, slug history, drafts). README Quickstart and PROJECT.md Out of Scope cross-link the section. (Plan 08-17; ROADMAP success criterion 8.)
    ```

    Insert these blocks AFTER the existing v1 Requirements sections (before "## v2 Requirements (deferred)" or wherever the existing convention places them).

    Also update the Phase 8 row in ROADMAP.md's requirements column from `TAX-01..0N, PROP-01..0N` to the concrete `TAX-01..10, PROP-01..06, DOC-01..02` range.
  </action>
  <verify>
    <automated>grep -c "^- \\*\\*TAX-01\\*\\*" .planning/REQUIREMENTS.md | grep -v '^#' | grep -q '^1$' && grep -c "^- \\*\\*PROP-01\\*\\*" .planning/REQUIREMENTS.md | grep -v '^#' | grep -q '^1$' && grep -c "^- \\*\\*DOC-01\\*\\*" .planning/REQUIREMENTS.md | grep -v '^#' | grep -q '^1$' && grep -c "TAX-10" .planning/REQUIREMENTS.md | grep -v '^#' | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "PROP-06" .planning/REQUIREMENTS.md | grep -v '^#' | awk '{ if ($1 >= 1) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TAX-01" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "TAX-10" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "PROP-01" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "PROP-06" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "DOC-01" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "DOC-02" .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c "Taxonomies (TAX)" .planning/REQUIREMENTS.md` returns at least 1
  </acceptance_criteria>
  <done>All TAX/PROP/DOC IDs codified in REQUIREMENTS.md.</done>
</task>

</tasks>

<verification>
- All four files have the new content.
- All cross-links resolve (manual spot-check or markdown-link-checker if available).
</verification>

<success_criteria>
After this plan ships:
- DOC-01 (RECONCILIATION) + DOC-02 (CHANGELOG omissions + cross-links) closed.
- Phase 8 requirement IDs codified.
- v1.0 tag-readiness from a docs perspective is reached.
</success_criteria>

<output>
After completion, create `.planning/phases/08-taxonomies-and-proposers/08-17-SUMMARY.md` documenting:
- File list + line counts.
- Confirmation: all four artifact files updated.
- Confirmation: REQUIREMENTS.md ID range matches plan-frontmatter cross-references.
</output>
