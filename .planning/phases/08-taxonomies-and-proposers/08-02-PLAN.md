---
phase: 08-taxonomies-and-proposers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/source/DoctrineEntityParser.php
autonomous: true
requirements: [TAX-04]
must_haves:
  truths:
    - "DoctrineEntityParser captures Gedmo Translatable as a per-property flag (signal #1 of D-10's union)"
    - "Parser remains attributes-only — no annotation parser revival (Phase 4.1 / SRC-20 invariant preserved)"
    - "Each parsed property exposes a boolean flag indicating Gedmo Translatable presence"
  artifacts:
    - path: "src/source/DoctrineEntityParser.php"
      provides: "Gedmo\\Mapping\\Annotation\\* attribute scan + per-property isGedmoTranslatable flag"
      contains: "Gedmo"
  key_links:
    - from: "src/source/DoctrineEntityParser.php"
      to: "DoctrineColumnInfo (or analogous VO)"
      via: "isGedmoTranslatable flag on property metadata"
      pattern: "isGedmoTranslatable\\|gedmoTranslatable"
---

<objective>
Extend `DoctrineEntityParser` to scan the `Gedmo\Mapping\Annotation\*` attribute namespace alongside the existing `Doctrine\ORM\Mapping\*` scan, capturing `#[Gedmo\Translatable]` (and the equivalent short-class hit through the use-map) on a per-property basis. Each parsed property gains a `bool $isGedmoTranslatable` flag (or analogous field on the property metadata VO).

Purpose: D-10 mandates the translatable-field auto-detection signal as the union of (1) source attribute and (2) runtime `ext_translations` row. This plan lands signal #1. Plan 11 (TaxonomyMigrationService) will consume both the static flag and the runtime signal.

Output:
- DoctrineEntityParser parses `#[Gedmo\Translatable]` PHP 8 attribute hits.
- Each property metadata exposes the Gedmo-translatable flag.
- No annotation parsing revival (SRC-20 / Phase 4.1 invariant preserved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-taxonomies-and-proposers/08-CONTEXT.md
@.planning/phases/08-taxonomies-and-proposers/08-PATTERNS.md
@.planning/phases/04.1-polish-recovery-and-env-defaults/04.1-06-attributes-only-parser-PLAN.md
@src/source/DoctrineEntityParser.php

<interfaces>
<!-- Patterns extracted by 08-PATTERNS.md and CONTEXT — executor should NOT re-explore. -->

D-10 union signal:
1. Source attribute: `#[Gedmo\Translatable]` and equivalent short-class hits via use-map.
2. Runtime signal: actual rows in ext_translations (consumed by TaxonomyMigrationService — Plan 11).

SRC-20 / Phase 4.1 invariant: Parser is ATTRIBUTES-ONLY. Do NOT reintroduce annotation parsing. Gedmo support is added as a second attribute-namespace scan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Gedmo namespace + use-map resolution to DoctrineEntityParser</name>
  <files>src/source/DoctrineEntityParser.php</files>
  <read_first>
    - src/source/DoctrineEntityParser.php (full file — current Doctrine\ORM\Mapping\* attribute scan + use-map resolver)
    - .planning/phases/08-taxonomies-and-proposers/08-PATTERNS.md (section: "src/source/DoctrineEntityParser.php — extend Gedmo namespace scan")
    - .planning/phases/08-taxonomies-and-proposers/08-CONTEXT.md (D-10: union of source attribute + runtime ext_translations signal)
    - .planning/phases/04.1-polish-recovery-and-env-defaults/04.1-06-attributes-only-parser-PLAN.md (SRC-20 attributes-only invariant — must NOT be reverted)
    - tests/unit/source/DoctrineEntityParserTest.php (if exists — current attribute coverage)
  </read_first>
  <behavior>
    - Parser detects `#[Gedmo\Translatable]` PHP 8 attribute on properties.
    - Parser detects `#[Translatable]` short-class hit when the file's use-map includes `use Gedmo\Mapping\Annotation\Translatable;` (or `use Gedmo\Mapping\Annotation as Gedmo;`).
    - Parser does NOT detect docblock annotations like `@Gedmo\Translatable()` (SRC-20 invariant).
    - Each parsed property metadata exposes a boolean flag (e.g. `isGedmoTranslatable` or `gedmoTranslatable`) indicating whether the attribute was present.
    - Properties without the attribute have the flag set to false.
    - Existing attribute parsing (ORM\Column, ORM\ManyToOne, ORM\JoinTable, etc.) continues to work unchanged.
  </behavior>
  <action>
    Locate the existing attribute namespace scan in DoctrineEntityParser. Add `Gedmo\Mapping\Annotation\*` as a recognized namespace alongside `Doctrine\ORM\Mapping\*`.

    Implementation steps:
    1. In the use-map resolver, add Gedmo recognition: when the file has `use Gedmo\Mapping\Annotation\Translatable;` (or any other Gedmo annotation), the short-class `Translatable` resolves to the FQCN `Gedmo\Mapping\Annotation\Translatable`.
    2. In the property-attribute walk loop, when the attribute FQCN matches `Gedmo\Mapping\Annotation\Translatable`, set the property metadata flag.
    3. Decide where the flag lives:
       - If the parser already has a column-info VO (`DoctrineColumnInfo` or similar from Phase 02.1), add `public bool $isGedmoTranslatable = false;` to the VO.
       - If the parser stores property metadata in a plain array, add an `'isGedmoTranslatable' => bool` key.
    4. The existing Doctrine\ORM scan and any returned VO/array shape MUST stay backward-compatible — only ADD the flag, don't reshape.

    Concrete pattern in the property-attribute loop (sketch — adapt to the actual VO/array shape in this file):
    ```php
    foreach ($reflectionProperty->getAttributes() as $attr) {
        $attrName = $attr->getName();
        // existing ORM\Column / ORM\ManyToOne / ORM\JoinColumn / etc. handling stays unchanged
        if ($attrName === 'Gedmo\\Mapping\\Annotation\\Translatable') {
            $isGedmoTranslatable = true;
        }
    }
    ```

    The use-map resolver already handles short-class → FQCN resolution for ORM attributes; reuse that same path so that `#[Translatable]` (with `use Gedmo\Mapping\Annotation\Translatable;` in scope) resolves to the same FQCN.

    Do NOT add any docblock-parsing code. Do NOT bring back doctrine/annotations or any annotation-reader dependency. SRC-20 invariant.
  </action>
  <verify>
    <automated>composer phpstan && grep -c "Gedmo" src/source/DoctrineEntityParser.php | grep -v '^#' | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && grep -c "isGedmoTranslatable\|gedmoTranslatable" src/source/DoctrineEntityParser.php | grep -v '^#' | awk '{ if ($1 >= 1) exit 0; else exit 1 }' && ! grep -E "doctrine/annotations|AnnotationReader" src/source/DoctrineEntityParser.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Gedmo" src/source/DoctrineEntityParser.php` returns at least 1
    - `grep -c "isGedmoTranslatable\|gedmoTranslatable" src/source/DoctrineEntityParser.php` returns at least 1
    - `grep -c "doctrine/annotations\|AnnotationReader" src/source/DoctrineEntityParser.php` returns 0 (SRC-20 invariant — annotation parser stays dead)
    - `composer phpstan` exits 0
  </acceptance_criteria>
  <done>Parser captures #[Gedmo\Translatable] as a per-property flag; SRC-20 invariant intact; phpstan green.</done>
</task>

</tasks>

<verification>
- `composer phpstan` exits 0.
- Gedmo namespace recognition added without reviving annotation parsing.
</verification>

<success_criteria>
After this plan ships:
- D-10 signal #1 (source attribute) is captured.
- Plan 11 can consume the flag during taxonomy migration to seed the per-locale Gedmo overlay.
- The parser remains attributes-only (SRC-20 invariant).
</success_criteria>

<output>
After completion, create `.planning/phases/08-taxonomies-and-proposers/08-02-SUMMARY.md` documenting:
- Where the Gedmo namespace scan was inserted (line numbers).
- Where the `isGedmoTranslatable` flag lives on the property metadata.
- Confirmation that the SRC-20 attributes-only invariant is preserved (no doctrine/annotations dependency added).
</output>
