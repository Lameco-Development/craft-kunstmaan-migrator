---
phase: 08-taxonomies-and-proposers
plan: 13
type: execute
wave: 4
depends_on: [08-08]
files_modified:
  - src/templates/_settings.twig
autonomous: true
requirements: [PROP-04]
must_haves:
  truths:
    - "_settings.twig exposes a new 'AI' H2 group between Mapping and Fallback"
    - "AI group surfaces lightswitchField for proposeLayout (D-14)"
    - "AI group surfaces lightswitchField for proposeProviders (D-14)"
    - "Each lightswitch carries the operator-explanatory instructions text noting --no-layout / --no-providers CLI bypass"
    - "Existing Connectivity / Mapping / Fallback groups left untouched"
  artifacts:
    - path: "src/templates/_settings.twig"
      provides: "AI H2 group with proposeLayout + proposeProviders lightswitches"
      contains: "proposeLayout"
  key_links:
    - from: "src/templates/_settings.twig"
      to: "src/models/Settings.php (proposeLayout / proposeProviders booleans from Plan 08)"
      via: "Twig settings.proposeLayout / settings.proposeProviders bindings"
      pattern: "settings.proposeLayout"
---

<objective>
Surface the two new Settings booleans in the CP `_settings.twig` AI H2 group. D-14 mandates the CP surface (Phase 4 / D-62 grouped-section convention).

Purpose: PROP-04 ladder closure — operator can toggle the proposers via CP, in addition to the CLI flags from Plan 08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-taxonomies-and-proposers/08-CONTEXT.md
@.planning/phases/08-taxonomies-and-proposers/08-PATTERNS.md
@src/templates/_settings.twig

<interfaces>
<!-- Existing H2 groups (lines 19, 75, 120): Connectivity / Mapping / Fallback. -->
<!-- D-62 grouped-section convention from Phase 4. -->
<!-- D-14 says new "AI" H2 group between Mapping and Fallback. -->

<!-- Standard lightswitch idiom (Craft 5):
{{ forms.lightswitchField({
    label: '...'|t('kunstmaan-migrator'),
    id: 'proposeLayout',
    name: 'proposeLayout',
    on: settings.proposeLayout,
    instructions: '...'|t('kunstmaan-migrator'),
}) }}
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AI H2 group with two lightswitches</name>
  <files>src/templates/_settings.twig</files>
  <read_first>
    - src/templates/_settings.twig (full file — note Connectivity / Mapping / Fallback H2 groups at ~19, ~75, ~120; check existing field shape conventions; confirm forms.lightswitchField is in scope)
    - .planning/phases/08-taxonomies-and-proposers/08-PATTERNS.md (section: "src/templates/_settings.twig — surface 2 new fields under 'AI' H2 group")
    - .planning/phases/04-adapters-verify-settings/04-CONTEXT.md (D-62 grouped-section convention)
  </read_first>
  <behavior>
    - Twig file has a new `<h2>{{ 'AI'|t('kunstmaan-migrator') }}</h2>` block between the Mapping and Fallback groups.
    - Below the H2: forms.lightswitchField for proposeLayout binding to settings.proposeLayout.
    - Below that: forms.lightswitchField for proposeProviders binding to settings.proposeProviders.
    - Each lightswitch has a clear instructions string mentioning the CLI counterpart (--no-layout / --no-providers).
    - Existing groups untouched.
  </behavior>
  <action>
    Locate the Mapping H2 group block. After it ends (and before the Fallback H2 block begins), insert:

    ```twig
    <h2>{{ 'AI'|t('kunstmaan-migrator') }}</h2>

    {{ forms.lightswitchField({
        label: 'Propose page-builder layout (header / body-wrap / body-column)'|t('kunstmaan-migrator'),
        id: 'proposeLayout',
        name: 'proposeLayout',
        on: settings.proposeLayout,
        instructions: 'When ON, analyze proposes Matrix layout slots (headerBlock / bodyWrapBlock / bodyColumn) for each accepted nodeClass. Heuristic-gated by Matrix catalog. CLI --no-layout bypasses per-run.'|t('kunstmaan-migrator'),
    }) }}

    {{ forms.lightswitchField({
        label: 'Propose dataProviders (orphan page-parts)'|t('kunstmaan-migrator'),
        id: 'proposeProviders',
        name: 'proposeProviders',
        on: settings.proposeProviders,
        instructions: 'When ON, analyze proposes mapping.dataProviders rows for orphan page-parts (no kuma_page_part_refs row + sourceTable not joined to kuma_node_versions). CLI --no-providers bypasses per-run.'|t('kunstmaan-migrator'),
    }) }}
    ```

    Do NOT modify Connectivity / Mapping / Fallback groups. Do NOT relocate seoEnabled / retourEnabled (per PATTERNS gotcha — those stay where they are; relocation is "out of Phase 8 scope").

    If `forms` macro import isn't already in scope in this template, do not add a new import — Craft's CP `_layouts/cp` parent template makes `forms` available globally. Confirm by checking the rest of the template for `forms.textField` / `forms.passwordField` / `forms.editableTableField` usage.
  </action>
  <verify>
    <automated>grep -c "<h2>{{ 'AI'|t" src/templates/_settings.twig | grep -v '^#' | grep -q '^1$' && grep -c "settings.proposeLayout" src/templates/_settings.twig | grep -v '^#' | grep -q '^1$' && grep -c "settings.proposeProviders" src/templates/_settings.twig | grep -v '^#' | grep -q '^1$' && grep -c "forms.lightswitchField" src/templates/_settings.twig | grep -v '^#' | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && grep -c "name: 'proposeLayout'" src/templates/_settings.twig | grep -v '^#' | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<h2>{{ 'AI'" src/templates/_settings.twig` returns 1
    - `grep -c "settings.proposeLayout" src/templates/_settings.twig` returns 1
    - `grep -c "settings.proposeProviders" src/templates/_settings.twig` returns 1
    - `grep -c "forms.lightswitchField" src/templates/_settings.twig` returns at least 2
    - `grep -c "--no-layout" src/templates/_settings.twig` returns at least 1 (instructions copy)
    - `grep -c "--no-providers" src/templates/_settings.twig` returns at least 1
  </acceptance_criteria>
  <done>AI H2 group present with both lightswitches; instruction text references CLI counterparts.</done>
</task>

</tasks>

<verification>
- The Twig file is syntactically valid (open the file in a Twig parser if available; otherwise rely on smoke check via the CP).
- The new fields bind to the Plan 08 Settings properties.
</verification>

<success_criteria>
After this plan ships:
- PROP-04 CP surface complete.
- Operator can toggle proposers via CP without editing settings JSON or restarting.
</success_criteria>

<output>
After completion, create `.planning/phases/08-taxonomies-and-proposers/08-13-SUMMARY.md` documenting:
- The new H2 group's line range.
- Confirmation that no existing groups were modified.
</output>
