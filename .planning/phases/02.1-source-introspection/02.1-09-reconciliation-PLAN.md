---
phase: 02.1-source-introspection
plan: 09
type: execute
wave: 5
depends_on: ['02.1-08']
files_modified:
  - .planning/phases/02.1-source-introspection/RECONCILIATION.md
  - src/mapping/CoverageAuditor.php
  - src/mapping/MappingAuditor.php
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SRC-01, SRC-02, SRC-03, SRC-04, SRC-05, SRC-06, SRC-07, SRC-08, SRC-09, SRC-10, SRC-11, SRC-12, SRC-13, SRC-14, SRC-15, SRC-16, SRC-17]
must_haves:
  truths:
    - "RECONCILIATION.md exists at .planning/phases/02.1-source-introspection/RECONCILIATION.md and contains rule-by-rule disposition tables for both v1↔v2 reconcile pairs (CoverageAuditor 482→102 LOC; MappingValidator 647→172 LOC)."
    - "Every rule from v1's CoverageAuditor.php is classified as ported / dropped intentionally (with rationale) / dropped accidentally (with v2 patch reference)."
    - "Every rule from v1's MappingValidator.php is classified the same way; the 13 rules from PATTERNS section 13 are the starting checklist."
    - "All 'dropped accidentally' entries have corresponding patches landed into v2's CoverageAuditor.php / MappingAuditor.php."
    - "REQUIREMENTS.md grows a `### Source introspection (SRC)` section codifying SRC-01..SRC-17 derived from the 17 ROADMAP success criteria."
    - "ROADMAP.md Phase 02.1 success criteria 1 + 3 are patched per D-31 (drop greenfield-fallback wording)."
    - "Traceability table in REQUIREMENTS.md maps SRC-01..SRC-17 to phase 02.1."
  artifacts:
    - path: ".planning/phases/02.1-source-introspection/RECONCILIATION.md"
      provides: "Canonical rule-by-rule disposition artifact for v1↔v2 reconciliation."
      min_lines: 100
    - path: ".planning/REQUIREMENTS.md"
      provides: "SRC-01..SRC-17 codified."
      contains: "SRC-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Traceability table mapping SRC IDs to phase 02.1"
      pattern: "SRC-01"
---

<objective>
Close Phase 02.1 with the reconciliation artifact (D-37/D-38/D-39), patch any v2 rules surfaced as "dropped accidentally" in `MappingAuditor` / `CoverageAuditor` against their v1 originals, codify SRC-01..SRC-17 in REQUIREMENTS.md (currently the ROADMAP success criteria are de-facto requirements; this plan formalizes them), and apply the D-31 ROADMAP patches (drop greenfield-fallback wording from criteria 1 + 3).

This is a docs-heavy plan with limited code edits — the code edits land ONLY for `dropped accidentally` rules from PATTERNS sections 13 + 14. If reconciliation surfaces no new accidentally-dropped rules beyond what PATTERNS already flags, Tasks 2 + 3 may be smaller than expected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02.1-source-introspection/02.1-CONTEXT.md
@.planning/phases/02.1-source-introspection/02.1-PATTERNS.md
@.planning/phases/02-schema-mapping-filters/02-05-coverage-audit-doctor-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author RECONCILIATION.md with rule-by-rule diff for both v1↔v2 audit pairs</name>
  <files>.planning/phases/02.1-source-introspection/RECONCILIATION.md</files>
  <read_first>
    - ~/Sites/craft-kunstmaan-migrator/src/bridge/schema/knowledgebase/CoverageAuditor.php (full 482 LOC — read in entirety; every distinct rule, threshold, constant, scoping helper is in scope)
    - ~/Sites/craft-kunstmaan-migrator/src/bridge/mapping/MappingValidator.php (full 647 LOC — read in entirety; the 13 rules from PATTERNS section 13 are a starting checklist but the file may contain finer-grained rules)
    - src/mapping/CoverageAuditor.php (current Plan 02-05 + Plan 08 extensions — 102 LOC + page-part extension)
    - src/mapping/MappingAuditor.php (current Plan 02-05 + Plan 08 extensions — 172 LOC + 4th finding kind)
    - .planning/phases/02.1-source-introspection/02.1-PATTERNS.md (sections 13 + 14 — already-identified accidentally-dropped rules)
    - .planning/phases/02.1-source-introspection/02.1-CONTEXT.md (D-37/D-38/D-39 — methodology)
  </read_first>
  <action>
    Create `.planning/phases/02.1-source-introspection/RECONCILIATION.md` with two main sections:

    **A. CoverageAuditor reconciliation (v1 482 LOC vs v2 102 + page-part extension):**

    Format: a markdown table with columns `Rule (v1 file:line)`, `Description`, `Disposition`, `v2 location / rationale`. Enumerate every distinct rule in v1's CoverageAuditor.php — read the file end-to-end to identify each:
    - The fillRate threshold (v1 line 95).
    - The empty-table guard (v1 line 84).
    - `reachableTables()` scoping (v1 KnowledgeBase line 862, used at v1 CoverageAuditor line 66).
    - `buildCoveredSet`, `collectJoinAliasFields`, `collectFromFieldsBlock`, `deepWalk`, `isColumnReferenceKey`, `collectInlineDrops`, `fetchSamples`, `isSafeIdent` (v1 lines 115-460 cover these scoping helpers).
    - Any other rules surfaced by reading the file.

    Disposition values: `ported` (cite v2 file:line), `dropped intentionally` (cite reason — usually "v2 architectural decision: single mapping.yaml, runtime-zero-AI, CLI-only, etc."), `dropped accidentally` (Task 2 patches into v2).

    **B. MappingValidator reconciliation (v1 647 LOC vs v2 172 + Plan 08 extensions):**

    Same format. Start from PATTERNS section 13's 13-rule checklist:
    - Rules 1-3 (top-level keys, runtime.neverProduction, runtime.sourceIdField): dropped intentionally per v2 architecture.
    - Rule 4 (nodeClasses): dropped intentionally — pagePart rows live in proposals[].
    - Rule 5 (pageParts SKIP/required): patched into v2 via D-34 (Plan 06) + Plan 08.
    - Rules 6-11 (dataProviders / sites / taxonomies / cross-refs): out of scope or dropped intentionally.
    - Rule 7 (CANONICAL_HANDLERS cross-ref) — already in v2 lines 35-38; reconcile the 3-entry delta (v1 had 9 entries, v2 has 12 — verify by reading both).
    - Rule 12 (relation table info_schema cross-ref): possibly accidentally dropped — Task 3 may patch.
    - Rule 13 (drop-shape rationale length ≥ 10 chars): possibly accidentally dropped — Task 3 may patch.

    Then read the rest of v1's MappingValidator.php (lines 50-647) for any rules not in the 13-rule checklist. Add them to the table.

    **C. Summary section** at the end of RECONCILIATION.md:
    - Counts: ported / dropped intentionally / dropped accidentally per pair.
    - List of "dropped accidentally" entries with a one-line note describing the v2 patch (Task 2 + Task 3 land them).
    - LOC delta projection: v2 fresh-write was 102 + 172 = 274 LOC; v1 was 482 + 647 = 1129 LOC; post-reconciliation v2 expected to land at 800-1000 LOC per CONTEXT specifics.

    **Citations:** Every entry MUST cite v1 file:line. Use the absolute path `~/Sites/craft-kunstmaan-migrator/src/...:NN`. Every "ported" entry MUST cite v2 file:line.

    Length target: 100-300 lines depending on rule count surfaced. Don't pad.
  </action>
  <verify>
    <automated>test -f .planning/phases/02.1-source-introspection/RECONCILIATION.md &amp;&amp; wc -l .planning/phases/02.1-source-introspection/RECONCILIATION.md &amp;&amp; grep -c "v1 file:line\\|dropped accidentally\\|dropped intentionally\\|ported" .planning/phases/02.1-source-introspection/RECONCILIATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/02.1-source-introspection/RECONCILIATION.md`.
    - `wc -l` reports ≥ 100 lines.
    - `grep -c "## CoverageAuditor reconciliation\\|## MappingValidator reconciliation\\|## MappingAuditor reconciliation" RECONCILIATION.md` ≥ 2 (both main sections present).
    - `grep -c "ported\\|dropped intentionally\\|dropped accidentally" RECONCILIATION.md` ≥ 10 (rule count threshold; if v1 has fewer than 10 distinct rules per file the threshold drops).
    - `grep -c "craft-kunstmaan-migrator/src/" RECONCILIATION.md` ≥ 5 (v1 citations present).
    - `grep -c "src/mapping/CoverageAuditor.php\\|src/mapping/MappingAuditor.php" RECONCILIATION.md` ≥ 2 (v2 citations present).
    - Summary section enumerates accidentally-dropped rules; Tasks 2/3 cite this list.
  </acceptance_criteria>
  <done>RECONCILIATION.md authored. Methodology per D-37 applied. List of accidentally-dropped rules drives Tasks 2 + 3.</done>
</task>

<task type="auto">
  <name>Task 2: Apply 'dropped accidentally' patches to CoverageAuditor + MappingAuditor</name>
  <files>src/mapping/CoverageAuditor.php, src/mapping/MappingAuditor.php</files>
  <read_first>
    - .planning/phases/02.1-source-introspection/RECONCILIATION.md (Task 1 output — drives this task's scope)
    - src/mapping/CoverageAuditor.php (current — Plan 02-05 + Plan 08 extension; needs the empty-table guard from PATTERNS section 14, possibly reachableTables scoping)
    - src/mapping/MappingAuditor.php (current — Plan 02-05 + Plan 08 extension; needs rule 12 (relation table cross-ref) and rule 13 (drop-shape rationale length) from PATTERNS section 13)
  </read_first>
  <action>
    For each "dropped accidentally" entry in RECONCILIATION.md, apply the corresponding v2 patch. Likely patches based on PATTERNS sections 13 + 14:

    **A. CoverageAuditor — empty-table guard (PATTERNS section 14 #5):**
    Add an early `continue` at the top of the per-table iteration:
    ```php
    $rowCount = (int) ($schemaDump['tables'][$table] ?? 0);
    if ($rowCount === 0) { continue; }
    ```
    Place BEFORE the per-column fillRate walk. Preserves the v1 behavior of not flagging columns in empty tables (no data → no coverage gap).

    **B. CoverageAuditor — reachableTables scoping (PATTERNS section 14 #1):**
    Only patch if RECONCILIATION.md classifies it as accidentally dropped. If kept (pure-function v2 keeps explicit STRUCTURAL_IGNORE list as the alternative), document the rationale in RECONCILIATION.md and SKIP this patch.

    **C. MappingAuditor — drop-shape rationale length (PATTERNS section 13 rule 13):**
    Add a check inside the existing audit() walk:
    ```php
    if (($row['status'] ?? '') === 'dropped' && strlen((string) ($row['rationale'] ?? '')) < 10) {
        $findings[] = [
            'table' => $row['table'] ?? '', 'column' => $row['column'] ?? '',
            'targetEntryType' => $row['targetEntryType'] ?? '',
            'targetHandle' => $row['targetHandle'] ?? '',
            'kind' => 'drop-rationale-missing',
            'detail' => 'dropped row has rationale shorter than 10 chars (v1 MappingValidator rule 13)',
        ];
    }
    ```

    **D. MappingAuditor — relation table info_schema cross-ref (PATTERNS section 13 rule 12):**
    This rule overlaps with Plan 5/8 drift detection. If RECONCILIATION.md classifies it as accidentally dropped, decide whether to wire it into MappingAuditor (specific cross-ref check on relation tables in mapping.yaml) or leave it covered by drift detection. If drift detection covers it, document in RECONCILIATION.md and skip the patch.

    **E. CANONICAL_HANDLERS reconcile (PATTERNS section 13 rule 7):**
    v2 has 12 entries; v1 had 9. Verify the delta is intentional (3 added entries v1 didn't have) and document in RECONCILIATION.md. Likely no patch needed.

    For each patch landed, update the v2 file with a comment like `// Patched in Phase 02.1 / Plan 09 from v1 MappingValidator.php:NNN per RECONCILIATION.md.` (preserves traceability without polluting the main flow).
  </action>
  <verify>
    <automated>php -l src/mapping/CoverageAuditor.php &amp;&amp; php -l src/mapping/MappingAuditor.php &amp;&amp; composer test 2>&1 | grep -E "OK|FAILURES"</automated>
  </verify>
  <acceptance_criteria>
    - `php -l` clean for both files.
    - `composer test` exits 0.
    - Every "dropped accidentally" entry from RECONCILIATION.md has a corresponding patch in v2 OR a documented rationale for not patching.
    - `grep -c "Patched in Phase 02.1 / Plan 09" src/mapping/CoverageAuditor.php src/mapping/MappingAuditor.php` ≥ 1 (at least one patch landed; comment in either file).
    - LOC delta visible: `wc -l` of both files should grow modestly from Plan 08's totals (10-50 lines combined depending on patch count).
  </acceptance_criteria>
  <done>Accidentally-dropped rules patched into v2. Traceability comments link patches to RECONCILIATION.md.</done>
</task>

<task type="auto">
  <name>Task 3: Codify SRC-01..SRC-17 in REQUIREMENTS.md + apply D-31 ROADMAP patches</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full ~129 lines — note the `### Foundation (FND)` / `### Connectivity (CONN)` / etc. section structure to mirror; the Traceability table at lines 113-129)
    - .planning/ROADMAP.md (full — Phase 02.1 section starting around line 63; success criteria 1 and 3 contain the greenfield-fallback wording per D-31 + CONTEXT specifics; the Plans line is `**Plans:** TBD` and the Plans list is empty)
    - .planning/phases/02.1-source-introspection/02.1-CONTEXT.md (D-31 — patches ROADMAP success criteria 1 + 3)
  </read_first>
  <action>
    Two doc edits.

    **A. .planning/REQUIREMENTS.md — add `### Source introspection (SRC)` section:**

    Insert AFTER the `### Locale handling (LOC)` section, BEFORE the `### ETL pipeline (ETL)` section. Codify SRC-01..SRC-17 from the 17 ROADMAP Phase 02.1 success criteria. Map verbatim (each ROADMAP criterion N becomes SRC-NN). Tag each with the phase number `(Phase 02.1)` and the plan(s) that satisfy it (e.g. SRC-10 → Plan 01; SRC-11 / SRC-12 → Plan 02; SRC-13 / SRC-14 → Plan 03; SRC-05 / SRC-06 / SRC-07 → Plan 04; SRC-01 / SRC-02 / SRC-03 / SRC-04 → Plan 05; SRC-08 / SRC-09 → Plan 07; SRC-15 → Plan 08; SRC-16 / SRC-17 → Plan 09 reconciliation).

    Format consistent with existing FND-*/CONN-*/MAP-*/etc. requirements:
    ```markdown
    - [ ] **SRC-01**: KunstmaanSourceScanner reads KUNSTMAAN_SOURCE_PATH (env or Settings::kunstmaanSourcePath); scans Entity/**/*.php (project) + vendor/kunstmaan/*/Entity/**/*.php (vendor). _(Phase 02.1 / Plan 05.)_
    - [ ] **SRC-02**: Discovered tables: every #[ORM\Table(name: '…')] (and @ORM\Table legacy syntax); every #[ORM\JoinTable(name: '…')] for M2M; vendor base-class tables resolved via extends chain. _(Phase 02.1 / Plan 02 + Plan 05.)_
    ```
    ... and so on for SRC-03..SRC-17 per the 17 ROADMAP criteria.

    Add to the Traceability table (lines 113-129):
    ```markdown
    | SRC-01 … SRC-17 | 02.1 |
    ```

    **B. .planning/ROADMAP.md — apply D-31 patches:**

    Phase 02.1 success criteria 1 + 3 currently contain greenfield-fallback wording. Per D-31:

    1. Criterion 1: Drop the trailing clause `". Degrades gracefully when path unset (greenfield mode → current `kuma_*` LIKE behavior)."` Replace with: `KUNSTMAAN_SOURCE_PATH is required for analyze; doctor's 5th check FAILs (not WARNs) when unset.`
    2. Criterion 3: Drop the clause `"; falls back to `LIKE 'kuma_%'` (or `Settings::legacyDbTablePrefix`) otherwise."` Replace with: `(Required path; greenfield-fallback dropped per D-31 — KUNSTMAAN_SOURCE_PATH is the new floor.)`

    Also update the `**Plans:** TBD` line for Phase 02.1 to `**Plans:** 9 plans` and populate the Plans list:
    ```markdown
    Plans:
    - [ ] 02.1-01-source-path-infra-PLAN.md — Settings field + Resolver + Doctor 5th check + flag declarations (SRC-10)
    - [ ] 02.1-02-doctrine-port-PLAN.md — DoctrineEntityParser + DetailTableResolver + 3 VOs (SRC-11, SRC-12)
    - [ ] 02.1-03-discovery-port-PLAN.md — BodyScan + MediaFk + Topological + KnowledgeBase partial port (SRC-13, SRC-14)
    - [ ] 02.1-04-page-structure-scanner-PLAN.md — KunstmaanPageStructureScanner + nikic/php-parser (SRC-05, SRC-06, SRC-07)
    - [ ] 02.1-05-source-scanner-orchestrator-PLAN.md — KunstmaanSourceScanner + Plugin::config registration (SRC-01..SRC-04)
    - [ ] 02.1-06-mapping-extension-PLAN.md — MappingFile row-kind + MapController two-mode walker (SRC-05, SRC-06)
    - [ ] 02.1-07-analyze-wiring-PLAN.md — AnalyzeController 11-step + heuristic 1.5 + LlmClassifier KB (SRC-08, SRC-09)
    - [ ] 02.1-08-audit-extension-PLAN.md — BlockAvailabilityValidator + Drift section (SRC-15)
    - [ ] 02.1-09-reconciliation-PLAN.md — RECONCILIATION.md + REQUIREMENTS.md + ROADMAP patches (SRC-16, SRC-17)
    ```
  </action>
  <verify>
    <automated>grep -c "SRC-01\\|SRC-17" .planning/REQUIREMENTS.md &amp;&amp; grep -c "Source introspection (SRC)" .planning/REQUIREMENTS.md &amp;&amp; grep -F -- "greenfield mode" .planning/ROADMAP.md | wc -l &amp;&amp; grep -c "9 plans" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Source introspection (SRC)" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "**SRC-01**" .planning/REQUIREMENTS.md` returns 1.
    - `grep -c "**SRC-17**" .planning/REQUIREMENTS.md` returns 1.
    - For each `SRC-NN` from 01-17: at least one occurrence in REQUIREMENTS.md.
    - `grep -c "SRC-01 … SRC-17" .planning/REQUIREMENTS.md` ≥ 1 (Traceability table entry).
    - `grep -c -F "Degrades gracefully when path unset" .planning/ROADMAP.md` returns 0 (greenfield wording dropped from criterion 1).
    - `grep -c -F "falls back to \`LIKE 'kuma_%'\`" .planning/ROADMAP.md` returns 0 (criterion 3 wording dropped).
    - `grep -c -F "KUNSTMAAN_SOURCE_PATH is required" .planning/ROADMAP.md` ≥ 1 (D-31 replacement wording).
    - `grep -c -F "**Plans:** 9 plans" .planning/ROADMAP.md` returns 1 (Phase 02.1 plans count finalized).
    - Plans list under Phase 02.1 contains 9 entries: `grep -c "02.1-0[1-9]-.*-PLAN.md" .planning/ROADMAP.md` returns 9.
  </acceptance_criteria>
  <done>SRC-01..SRC-17 codified in REQUIREMENTS.md. ROADMAP greenfield-fallback wording patched per D-31. Traceability complete. Phase 02.1 fully documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — docs-and-patches plan; v2 patches inherit threat surface from Plan 08) | |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-09-01 | Tampering | reconciliation-driven patches to MappingAuditor/CoverageAuditor | mitigate | Each patch lands a comment `// Patched in Phase 02.1 / Plan 09 from v1 ...` linking to RECONCILIATION.md. The patches are additive (new finding kinds, new early-exit guards) — no behavioral changes to existing rules. `composer test` exit 0 acts as the regression gate. |
| T-02.1-09-02 | Repudiation | RECONCILIATION.md as the audit trail | mitigate | RECONCILIATION.md is committed to git (commit_docs); future operators / Phase 5 verifier can rerun the rule-by-rule diff against the v1 originals to verify no regressions. Phase 02.1 verifier checks RECONCILIATION.md is present + every "dropped accidentally" entry has a matching v2 patch (D-39). |
</threat_model>

<verification>
- RECONCILIATION.md exists with both pair tables + summary section.
- Every "dropped accidentally" rule has a v2 patch landed OR a documented rationale.
- REQUIREMENTS.md contains SRC-01..SRC-17 with Phase 02.1 traceability.
- ROADMAP.md Phase 02.1 success criteria 1 + 3 patched per D-31; Plans list populated with 9 entries.
- `composer test` exits 0 (PluginBootstrapTest D-21 invariant intact through all reconciliation patches).
</verification>

<success_criteria>
SRC-16 (CoverageAuditor reconciliation) and SRC-17 (MappingValidator reconciliation) substantially complete. SRC-01..SRC-15 codified for verifier traceability. ROADMAP cleaned up per D-31. Phase 02.1 ready for verification handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-source-introspection/02.1-09-SUMMARY.md`. The phase verifier will read RECONCILIATION.md + the 9 SUMMARYs as the canonical phase-completion artifact set.
</output>
