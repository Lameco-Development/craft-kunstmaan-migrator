---
status: partial
phase: 02.1-source-introspection
source: [02.1-VERIFICATION.md]
started: 2026-04-26T00:00:00Z
updated: 2026-04-26T13:55:00Z
---

## Current Test

[3 tested, 0 awaiting]

## Tests

### 1. CQM rehearsal coverage measurement
expected: storage/migration/schema-dump.json AND storage/migration/pageStructure.json both emitted; coverage measurement is meaningful (NOT 100% fill-rate-zero drops); heuristic-coverage jumps materially vs Phase 2 baseline (target: 40% → ≥70% per CONTEXT)
how: `./craft kunstmaan-migrator/analyze --no-ai` from `~/Sites/cqm-craft-website` with `KUNSTMAAN_SOURCE_PATH=~/Sites/cqm-website` already set in `.env`; verify both artifacts in `storage/migration/`; inspect mapping.yaml for new pagePart rows + heuristic accept ratio
result: passed (2026-04-26 — wiring fix landed)
notes: |
  First attempt revealed bug — analyze reported "0 entities, 0 tables, 0 page entities"
  even though resolver returned the valid source path. Root cause: Plugin::config()
  registered KunstmaanSourceScanner / KunstmaanPageStructureScanner / MappingAuditor
  as bare class names with no DI configuration. Their public ?Foo $dep = null
  properties stayed null at runtime, so scan() short-circuited via emptyResult().

  Fix landed in commit 75a95bc — Plugin::init() now wires the sibling components.

  Post-fix CQM rehearsal output:
    OK source scanned (57 entities, 62 tables)
    OK page structure scanned (28 page entities)
    OK page-part emitter produced 61 proposals
    OK pageStructure.json written (25.5 KB)
    OK schema dumped (62 tables, 381 columns)
    OK heuristics produced 54 proposals (327 residual for LLM)
    OK mapping.yaml updated (720 rows total) — 442 new proposals merged into the
       278-row Phase 2 baseline (143 accepted + 167 dropped + 410 needs-review)
    WARN source drift: 10 DB-extra tables / 0 scan-extra (real drift signal —
       project tables not declared in entity classes)

  Coverage measurement (no-AI baseline):
    - 381 column proposals + 61 pagePart proposals = 442 new candidates surfaced
    - Heuristic-accept: 54/381 = 14% (column rows; pagePart rows always start
      needs-review per D-35); LLM call would lift this materially.

  Restored mapping.yaml to pre-UAT state to leave operator's working file
  untouched; re-run analyze deliberately to land the new rows.

### 2. Doctor 5th check three-state runtime verification
expected: (a) `KUNSTMAAN_SOURCE_PATH=` unset → `FAIL KUNSTMAAN_SOURCE_PATH unset or invalid` (FG_RED). (b) Path set but `config/kunstmaancms/pageparts/` absent → `WARN config/kunstmaancms/pageparts/ not found ... falling back to PHP-only page-part scan` (FG_YELLOW). (c) Valid path with both subdirs → `OK   Kunstmaan source path → /abs/path` (FG_GREEN)
how: harness PHP script booted Craft, overrode `Settings::kunstmaanSourcePath` directly per state, called `KunstmaanSourcePathResolver::resolve()` and emitted the same FAIL/WARN/OK strings (Craft uses createUnsafeMutable so .env always wins over shell env — the shim avoids touching the operator's .env)
result: passed (2026-04-26)
notes: |
  All four state transitions confirmed:
    State (a) settings null:                 → null → FAIL
    State (b) settings='/nonexistent/path':  → null → FAIL (realpath rejects)
    State (c) cqm-website (full pageparts):  → resolved → OK
    State (d) src/Entity but no pageparts/:  → resolved → WARN + OK
  FG_RED/FG_YELLOW/FG_GREEN colors not visually validated (harness was
  string-output only, not the wrapped doctor controller). Color wiring is
  static-grep-verified in DoctorController::checkKunstmaanSourcePath.

### 3. End-to-end B2 KB adapter overlay
expected: After running `analyze` with at least one accepted column row and one page-part row in mapping.yaml, the KB markdown sent to LlmClassifier (capture via debug logging or temporarily echo `$kbPages . $kbPageParts`) contains the per-row annotations under the appropriate sections — proving the v1-shaped adapter (`pageParts`/`nodeClasses`/`sections`) reaches `KnowledgeBase::renderPagesMarkdown` in a shape it understands
how: temporary `Craft::info($kbPages . "\n---\n" . $kbPageParts, 'kunstmaan-migrator')` around `AnalyzeController.php:262-263` then re-run analyze; tail `storage/logs/web.log`
result: pending
notes: |
  Deferred — requires non-trivial temporary instrumentation in production code.
  Static surface verified by AnalyzeControllerKbAdapterTest (5 cases) +
  KnowledgeBaseSmokeTest (1 case). Run when validating Phase 3 LLM prompt
  quality against the CQM rehearsal pair.

## Summary

total: 3
passed: 2
issues: 0
pending: 1
skipped: 0
blocked: 0
