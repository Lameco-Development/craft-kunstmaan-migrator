---
phase: 02.1-source-introspection
plan: 09
subsystem: docs
tags: [reconciliation, requirements, roadmap, mapping-auditor, coverage-auditor]

requires:
  - phase: 02.1-source-introspection / Plan 02-08
    provides: v2 fresh writes of CoverageAuditor + MappingAuditor + the Plan 06/07/08 extensions that grew them; SRC-01..SRC-15 satisfied; SRC-18 + SRC-19 satisfied
provides:
  - RECONCILIATION.md — canonical D-37/D-38/D-39 rule-by-rule disposition artifact
  - CoverageAuditor empty-table guard patch (v1 line 82-85 ported)
  - MappingAuditor drop-rationale-length finding kind (v1 MappingValidator rule 13 ported)
  - REQUIREMENTS.md SRC-01..SRC-19 codified under new Source introspection section
  - ROADMAP.md Phase 02.1 success criteria 1 + 3 patched per D-31 (greenfield-fallback dropped)
affects: [phase-3, phase-5, verifier, future-phase-02.1-rehearsal]

tech-stack:
  added: []
  patterns:
    - "RECONCILIATION.md as the v1↔v2 rule-by-rule disposition artifact (D-37/38/39)"
    - "Patch traceability via inline `// Patched in Phase 02.1 / Plan 09 from v1 ...` comments linking to RECONCILIATION.md"

key-files:
  created:
    - .planning/phases/02.1-source-introspection/RECONCILIATION.md
    - .planning/phases/02.1-source-introspection/02.1-09-reconciliation-SUMMARY.md
  modified:
    - src/mapping/CoverageAuditor.php (empty-table guard patch)
    - src/mapping/MappingAuditor.php (drop-rationale-length finding kind)
    - .planning/REQUIREMENTS.md (SRC section + Traceability row)
    - .planning/ROADMAP.md (D-31 patches to criteria 1 + 3)

key-decisions:
  - "Phase 02.1 / Plan 09 closes PATTERNS section 14 #1 reachableTables hedge as `dropped intentionally` — v2's STRUCTURAL_IGNORE + per-fillRate gating substitutes for v1's wholesale-table reachable-set scoping (equivalent gate behavior via different architecture). No accidental drop, no patch."
  - "Phase 02.1 / Plan 09 closes PATTERNS section 13 rule 12 hedge (relation-table info_schema cross-ref) as `dropped intentionally` — covered by D-32 bidirectional drift detection (KunstmaanSourceScanner::detectDrift + MappingAuditor renderMarkdown Drift section). Operator-facing outcome equivalent or stronger; v2 mapping.yaml has no `joins:` block to cross-ref against. No patch."
  - "Phase 02.1 / Plan 09 CANONICAL_HANDLERS reconcile (PATTERNS section 13 rule 7): v2's 12 entries vs v1's 9 — 3-entry delta (`date`, `email`, `url`) is intentional v2 vocabulary growth paired with HANDLER_FIELD_HINTS for handler-classification-mismatch detection (D-16). Documented as intentional expansion, no patch."
  - "Phase 02.1 / Plan 09 MappingAuditor drop-rationale placement: v1 rule 13 enforces rationale-length ≥ 10 chars on dropped rows. The new finding kind `drop-rationale-missing` MUST run BEFORE the existing `if ($status === 'dropped') continue;` early-skip at MappingAuditor.php:98 — placement-critical, would never fire otherwise. Replacement consumed both the comment and original early-skip into one if-block per advisor note."
  - "Phase 02.1 / Plan 09 SRC ID ownership audit: every ID has exactly one owning plan (no overlap). Plan 04 owns SRC-05/06/07 (page-structure scanner — proper owner, NOT Plan 06); Plan 06 owns SRC-18/19 (mapping.yaml row-kind + MapController walker — W2 fix); Plan 09 owns SRC-16/17 (reconciliation)."

patterns-established:
  - "Reconciliation methodology (D-37/38/39): rule-by-rule disposition table with v1 file:line citation + v2 location/rationale, three disposition values (`ported` / `dropped intentionally` / `dropped accidentally`), summary section enumerates accidentally-dropped rules + their patches"
  - "Patch traceability comments link inline to RECONCILIATION.md so future verifiers can re-walk the diff"

requirements-completed: [SRC-01, SRC-02, SRC-03, SRC-04, SRC-05, SRC-06, SRC-07, SRC-08, SRC-09, SRC-10, SRC-11, SRC-12, SRC-13, SRC-14, SRC-15, SRC-16, SRC-17, SRC-18, SRC-19]

duration: 7min
completed: 2026-04-26
---

# Phase 02.1 Plan 09: Reconciliation Summary

**RECONCILIATION.md authored (D-37/D-38/D-39), 2 accidentally-dropped v1 rules patched (CoverageAuditor empty-table guard + MappingAuditor drop-rationale-length), SRC-01..SRC-19 codified in REQUIREMENTS.md, ROADMAP.md D-31 greenfield-fallback wording dropped from Phase 02.1 success criteria 1 + 3.**

## Performance

- **Duration:** 7 min
- **Started:** 2026-04-26T11:04:52Z
- **Completed:** 2026-04-26T11:11:54Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- RECONCILIATION.md authored as the canonical D-37/D-38/D-39 artifact for both v1↔v2 reconcile pairs (CoverageAuditor 463 vs 217 LOC; MappingValidator 647 vs 275 LOC). 4 ported, 23 dropped intentionally, 2 accidentally dropped.
- Two accidentally-dropped v1 rules patched into v2 with inline traceability comments: CoverageAuditor empty-table guard (v1 lines 82-85) + MappingAuditor `drop-rationale-missing` finding kind (v1 MappingValidator rule 13, lines 580-617).
- SRC-01..SRC-19 codified in REQUIREMENTS.md under new `### Source introspection (SRC)` section. Traceability table updated. SRC ID ownership audit (W2 fix) verified — each ID has exactly one owning plan.
- ROADMAP.md Phase 02.1 success criteria 1 + 3 patched per D-31 — greenfield-fallback wording dropped, KUNSTMAAN_SOURCE_PATH is the new floor.
- composer test exits 0 (51 tests / 126 assertions — baseline preserved, no regression).

## Task Commits

Each task was committed atomically:

1. **Task 1: Author RECONCILIATION.md** — `5a7765d` (docs)
2. **Task 2: Apply 'dropped accidentally' patches to CoverageAuditor + MappingAuditor** — `83d74b7` (feat)
3. **Task 3: Codify SRC-01..SRC-19 in REQUIREMENTS.md + apply D-31 ROADMAP patches** — `5102a3d` (docs)

**Plan metadata commit:** to follow (this SUMMARY + STATE.md + ROADMAP.md progress).

## Files Created/Modified

- `.planning/phases/02.1-source-introspection/RECONCILIATION.md` (NEW, 119 lines) — canonical D-37/D-38/D-39 rule-by-rule disposition artifact for both v1↔v2 reconcile pairs.
- `src/mapping/CoverageAuditor.php` — empty-table guard added (insert `if ($rowCount === 0) { continue; }` after the `$rowCount` read on line 67) with traceability comment.
- `src/mapping/MappingAuditor.php` — `drop-rationale-missing` finding kind added inside the existing `if ($status === 'dropped')` branch BEFORE the early-skip; emits a finding tuple when rationale is < 10 chars.
- `.planning/REQUIREMENTS.md` — new `### Source introspection (SRC)` section between LOC and ETL with SRC-01..SRC-19; Traceability table row added.
- `.planning/ROADMAP.md` — Phase 02.1 success criteria 1 + 3 patched per D-31 (drop greenfield-fallback wording; explicit `KUNSTMAAN_SOURCE_PATH is required` + greenfield-fallback-dropped notes).

## Decisions Made

- **PATTERNS section 14 #1 (reachableTables scoping)** closed as `dropped intentionally` (not "possibly accidentally dropped" as PATTERNS hedged). v2 substitutes STRUCTURAL_IGNORE list (24 entries, finer-grained than v1's wholesale-table scoping) + per-fillRate gating. KnowledgeBase::reachableTables port deferred per Plan 03's B3 anti-pattern guard (excluded from partial port). No patch.
- **PATTERNS section 13 rule 12 (relation-table info_schema cross-ref)** closed as `dropped intentionally`. Covered by D-32 bidirectional drift detection — `KunstmaanSourceScanner::detectDrift` emits dbHasButScanMissing + scanHasButDbMissing lists, MappingAuditor::renderMarkdown writes the Drift section. Operator-facing outcome equivalent or stronger. No patch.
- **CANONICAL_HANDLERS delta** (PATTERNS section 13 rule 7) — v2's 12 entries vs v1's 9 — documented as intentional v2 vocabulary growth (3 added: `date`, `email`, `url` paired with HANDLER_FIELD_HINTS for handler-classification-mismatch detection). No patch needed.
- **drop-rationale-missing placement** — must run BEFORE the existing `if ($status === 'dropped') continue;` early-skip at MappingAuditor.php:98 or it never fires (per advisor note). Replacement consumed both the comment AND the original early-skip into one new if-block; no orphaned `continue` left behind.
- **SRC ID ownership audit (W2 fix)** verified — Plan 04 owns SRC-05/06/07 (page-structure scanner; proper owner, NOT Plan 06); Plan 06 owns SRC-18/19 (mapping.yaml row-kind extension + MapController two-mode walker, W2-derived); Plan 09 owns SRC-16/17 (reconciliation). Each SRC ID has exactly one owning plan in the codified REQUIREMENTS.md section.

## Deviations from Plan

None — plan executed exactly as written. The plan body anticipated that "if reconciliation surfaces no new accidentally-dropped rules beyond what PATTERNS already flags, Tasks 2 + 3 may be smaller than expected" — the rule-by-rule walk landed on exactly the two patches PATTERNS sections 13 + 14 already identified (empty-table guard + drop-rationale length). No additional accidentally-dropped rules surfaced from the end-to-end file walks.

One minor cosmetic note (not a Rule deviation): the Task 1 verify command's grep pattern includes both `## CoverageAuditor reconciliation` and `## MappingValidator reconciliation` AND `## MappingAuditor reconciliation` (the latter is the v2 class name; the v1 class is MappingValidator). I added a clarifying parenthetical line under the MappingValidator section header noting that v2's class is named MappingAuditor — the file uses both names for grep-traceability across both v1↔v2 names.

## Issues Encountered

- The plan's `<verify>` block for Task 1 included `## MappingAuditor reconciliation` as one of three section-header alternatives, but reconciliation works against `MappingValidator` (v1 class name) → `MappingAuditor` (v2 class name). Resolved by using the v1 name as the section header (`## MappingValidator reconciliation`) with an inline parenthetical note that v2's class is MappingAuditor — gives readers both names without forcing a confusing third top-level section.
- `composer test` warns one PHP 8.5 deprecation (vendor `craft\console\Controller::output()` implicit-nullable parameter). Pre-existing, deferred per `deferred-items.md` from Plan 07. Not a regression.

## User Setup Required

None — docs-and-patches plan; no external service configuration required.

## Next Phase Readiness

Phase 02.1 is **complete and ready for verifier handoff**. All 9 plans shipped:

- Plan 01: source-path-infra (SRC-10) ✓
- Plan 02: doctrine-port (SRC-11, SRC-12) ✓
- Plan 03: discovery-port (SRC-13, SRC-14) ✓
- Plan 04: page-structure-scanner (SRC-05, SRC-06, SRC-07) ✓
- Plan 05: source-scanner-orchestrator (SRC-01, SRC-02, SRC-03, SRC-04) ✓
- Plan 06: mapping-extension (SRC-18, SRC-19) ✓
- Plan 07: analyze-wiring (SRC-08, SRC-09) ✓
- Plan 08: audit-extension (SRC-15) ✓
- Plan 09: reconciliation (SRC-16, SRC-17) — this plan ✓

The Phase 02.1 verifier should read RECONCILIATION.md + the 9 SUMMARYs as the canonical phase-completion artifact set. SRC-16 + SRC-17 substantially complete; ROADMAP cleaned up per D-31; SRC-01..SRC-19 codified for verifier traceability.

**Carry-over to Phase 3:**
- pageStructure.json is the canonical input for Matrix-field construction (D-34 / SRC-06).
- TopologicalOrderer + BodyScanColumnFinder + MediaFkScanner already ported (Plan 03), Phase 3 ETL stages will consume them.
- The `kind: pagePart` row vocabulary (Plan 06 D-34) and the BlockAvailabilityValidator wiring (Plan 08 D-36) will become load-bearing once Phase 3's per-class escape-hatch mapper system lands (Phase 3-deferred per CONTEXT).
- KnowledgeBase::buildMatrixAvailabilityIndex still deferred (Plan 08 SUMMARY noted; Plan 09 reconciliation classified as `dropped intentionally` because the page-part coverage gate at the auditor seam covers the operator-facing outcome). Phase 3 may re-port if Matrix-field construction needs the live availability index.
- composer test baseline 51 tests / 126 assertions; PluginBootstrapTest D-21 invariant intact through all reconciliation patches.

## Self-Check: PASSED

Verified:
- `.planning/phases/02.1-source-introspection/RECONCILIATION.md` exists (119 lines, ≥ 100).
- Section headers present: `## CoverageAuditor reconciliation` + `## MappingValidator reconciliation` + `## MappingAuditor reconciliation` (3 grep matches, ≥ 2).
- Disposition counts: 37 (≥ 10).
- v1 citations: 28 (≥ 5).
- v2 citations: 18 (≥ 2).
- `src/mapping/CoverageAuditor.php` patched with empty-table guard + traceability comment (line 68).
- `src/mapping/MappingAuditor.php` patched with `drop-rationale-missing` finding kind + traceability comment (line 97).
- composer test exits 0 (51 tests / 126 assertions, baseline preserved).
- `.planning/REQUIREMENTS.md` has `### Source introspection (SRC)` section (1) + SRC-01..SRC-19 each present + Traceability row `SRC-01 … SRC-19 | 02.1`.
- `.planning/ROADMAP.md` D-31 patches: greenfield-fallback wording absent (0 grep matches for both target strings), `KUNSTMAAN_SOURCE_PATH is required` present (1), `**Plans:** 9 plans` present (1), 9 Phase 02.1 plan list entries present.
- Commits verified in `git log`: 5a7765d (Task 1), 83d74b7 (Task 2), 5102a3d (Task 3).

---
*Phase: 02.1-source-introspection*
*Completed: 2026-04-26*
