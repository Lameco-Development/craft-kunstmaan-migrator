---
phase: 02.1-source-introspection
fixed_at: 2026-04-27T00:00:00Z
review_path: .planning/phases/02.1-source-introspection/02.1-REVIEW.md
iteration: 1
findings_in_scope: 5
fixed: 1
skipped: 4
status: partial
---

# Phase 02.1: Code Review Fix Report

**Fixed at:** 2026-04-27T00:00:00Z
**Source review:** .planning/phases/02.1-source-introspection/02.1-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 5 (1 warning + 4 info, `fix_scope: all`)
- Fixed: 1
- Skipped: 4 (3 already fixed in current source, 1 deferred as cosmetic-only with locked-test constraint)

Test gate: `composer test` passes (331 tests, 891 assertions) after IN-04 fix.

## Fixed Issues

### IN-04: `KunstmaanPageStructureScanner::scanPageEntity` swallows resolver failures

**Files modified:** `src/source/KunstmaanPageStructureScanner.php`
**Commit:** c93983b
**Applied fix:** Both `try/catch` blocks (the YAML-context allowedPagePartClasses fill loop at lines 132-148 and the per-Page-entity resolve at lines 199-215) previously caught `InvalidArgumentException` silently — operators had no signal when the 4-tier `DetailTableResolver` legitimately could not map a Page entity or page-part class. Added `Craft::info` (not warning, per reviewer guidance — these are expected misses, not failures) at both sites with the FQCN and exception message. Behaviorally a no-op (still falls through to `$table = ''` / `$tableName = ''`); just adds an observability signal so operators can grep storage logs for unresolved entities. Logic is straightforward (add a log call before existing assignment) so no human verification flag is needed.

## Skipped Issues

### WR-01: `MapController` entity filter silently excludes every `kind: pagePart` row

**File:** `src/console/MapController.php:662-675`
**Reason:** Already fixed in current source. `MapController::matchesEntitiesFilter` at lines 674-709 now branches on `$kind = (string) ($row['kind'] ?? 'column')`; when `$kind === 'pagePart'` it matches against `parentPageClass` basename (handling both fully-namespaced and unnamespaced values), and falls through to the original `kuma_<snake>` table-prefix logic for column rows. The method is also `public static` (per reviewer's testability note) so it can be exercised directly. Verified call sites at `:120` (`applyEntitiesFilter`) and `:137` (interactive walk-order) both call `self::matchesEntitiesFilter`. No code change needed.
**Original issue:** PagePart rows produced by `MappingFile::buildPagePartRow()` carry `sourceTable`/`parentPageClass` instead of `table`, so the old `str_starts_with($table, 'kuma_<snake>')` filter dropped every pagePart row when `--entities=` was set.

### IN-01: `KunstmaanCoreTables` missing `declare(strict_types=1);`

**File:** `src/source/KunstmaanCoreTables.php:1-3`
**Reason:** Already fixed in current source. Line 3 reads `declare(strict_types=1);` directly after the `<?php` tag, matching every other file under `src/source/`. No code change needed.
**Original issue:** Reviewer noted this file was the only one in `src/source/` omitting the strict-types declaration.

### IN-02: `KnowledgeBase` interpolates table/column/join-table names directly into SQL

**File:** `src/source/KnowledgeBase.php` (multiple sites listed in REVIEW.md)
**Reason:** Skipped intentionally — reviewer's recommendation is explicitly framed as "Fix (defense in depth, optional)" and the threat model documented in `CLAUDE.md` (dev/staging only via `NeverProductionTrait`, operator-controlled inputs, verbatim port from v1) accepts the current trust boundary. Routing every helper-method write through a shared `isSafeIdent()` guard touches ~11 SQL sites across `KnowledgeBase` and would invite drift from the v1 verbatim-port contract documented at the top of the class. Defer until a phase explicitly tasks identifier-guard hardening.
**Original issue:** Backticked table/column/join-table names are concatenated directly into SQL strings without going through `isSafeIdent()` at the call sites.

### IN-03: `Plugin::config()` component-line alignment is inconsistent

**File:** `src/Plugin.php:80-88` (component declarations span lines 124-170)
**Reason:** Skipped — `tests/integration/PluginBootstrapTest.php:48` asserts a literal substring `"'legacyDbService' => LegacyDbService::class"` (single space around `=>`). That line is fixed at single-space alignment by an explicit invariant test, while `kunstmaanPageStructureScanner` at 29 chars forces any unified column to ~32 chars. So a single consistent column for the entire components array is structurally impossible without breaking the locked test. The reviewer flagged this as cosmetic-only ("Optional reformatting … for diff cleanliness") with no behavioural impact, and the existing local groupings (Phase 1/2 block, Phase 02.1 block, Phase 3+ block) are each internally aligned. Defer until either the lock-test is loosened or a wholesale config-formatting pass is scheduled.
**Original issue:** Phase 02.1 lines (135-145) use a wider `=>` arrow column than the existing Phase 1/2 block (125-134).

---

_Fixed: 2026-04-27T00:00:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
