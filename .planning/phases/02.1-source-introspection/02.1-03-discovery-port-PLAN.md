---
phase: 02.1-source-introspection
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/source/BodyScanColumnFinder.php
  - src/source/MediaFkScanner.php
  - src/source/TopologicalOrderer.php
  - src/source/KnowledgeBase.php
  - tests/source/KnowledgeBaseSmokeTest.php
autonomous: true
requirements: [SRC-13, SRC-14]
must_haves:
  truths:
    - "BodyScanColumnFinder discovers TEXT/LONGTEXT columns matching body/intro/summary/description/content regex against information_schema.COLUMNS, scoped by legacyTablePrefix and KunstmaanCoreTables exclusions."
    - "MediaFkScanner discovers BIGINT FK columns referencing kuma_media.id via the (image|icon|logo|banner|video|file|media|preview).*_?id$|^image$|^video$ regex against information_schema."
    - "TopologicalOrderer produces a depth-first parent-first ordering for kuma_nodes with cycle detection (returns PHP_INT_MAX for cycle members) and orphan detection (parent not in set → PHP_INT_MAX)."
    - "KnowledgeBase exposes renderPagesMarkdown() and renderPagePartsMarkdown() (string output) — replaces the current `'', ''` placeholders in LlmClassifier::batchPropose."
    - "KnowledgeBase ports loadAllColumns() (v1 line 774) + the CORE_TABLES private const (v1 line 67) verbatim — both are transitively reachable from the two render methods (renderPagesMarkdown calls $this->loadAllColumns at v1 line 394; renderPagePartsMarkdown calls it at v1 line 119)."
    - "Transitive-walk port rule: do a recursive call-graph walk from the two public render methods; port every reachable private method and const verbatim. Failure to walk transitively is the dropped-helpers anti-pattern that B3 corrected."
  artifacts:
    - path: "src/source/BodyScanColumnFinder.php"
      provides: "TEXT body-column discovery."
      min_lines: 130
    - path: "src/source/MediaFkScanner.php"
      provides: "kuma_media.id FK discovery."
      min_lines: 130
    - path: "src/source/TopologicalOrderer.php"
      provides: "Parent-first depth orderer + cycle/orphan detection."
      min_lines: 120
    - path: "src/source/KnowledgeBase.php"
      provides: "Markdown KB generator (two methods only) — with all transitively reachable private helpers ported verbatim."
      min_lines: 400
    - path: "tests/source/KnowledgeBaseSmokeTest.php"
      provides: "Smoke test asserting renderPagesMarkdown + renderPagePartsMarkdown construct without throwing on a mocked DB."
      min_lines: 40
  key_links:
    - from: "src/source/BodyScanColumnFinder.php"
      to: "src/db/LegacyDbService.php"
      via: "public ?LegacyDbService $legacyDb property"
      pattern: "LegacyDbService"
    - from: "src/source/MediaFkScanner.php"
      to: "src/db/LegacyDbService.php"
      via: "public ?LegacyDbService $legacyDb property"
      pattern: "LegacyDbService"
    - from: "src/source/KnowledgeBase.php"
      to: "src/source/DoctrineEntityParser.php"
      via: "public ?DoctrineEntityParser $entityParser property"
      pattern: "DoctrineEntityParser"
---

<objective>
Verbatim ports of the four discovery utilities from v1 — three small (BodyScanColumnFinder, MediaFkScanner, TopologicalOrderer) and one partial (KnowledgeBase: only `renderPagesMarkdown` + `renderPagePartsMarkdown` of the ~2200 LOC v1 class — with every transitively-reachable private helper). All under `src/source/`. Plus a smoke test that constructs KnowledgeBase against a mock DB and calls both render methods to catch any missed transitive helper before Plan 5 wires it. No callers wired here — Plan 5 (KunstmaanSourceScanner orchestrator) and Plan 6 (LlmClassifier KB integration) consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.1-source-introspection/02.1-CONTEXT.md
@.planning/phases/02.1-source-introspection/02.1-PATTERNS.md

<interfaces>
From src/db/LegacyDbService.php (queryAll bound-parameter API consumed by BodyScan + MediaFk):
```php
public function queryAll(string $sql, array $params = []): array;
```

KnowledgeBase public surface (post-port):
```php
public ?LegacyDbService $legacyDb = null;
public ?DoctrineEntityParser $entityParser = null;
public string $legacyTablePrefix = 'lameco_websitebundle_';
public const SAMPLE_MAX_LEN = 40;
public const MAX_SAMPLES = 3;
public function renderPagesMarkdown(?array $mapping, \DateTimeInterface $now): string;
public function renderPagePartsMarkdown(?array $mapping, \DateTimeInterface $now): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port BodyScanColumnFinder + MediaFkScanner (~280 LOC combined)</name>
  <files>src/source/BodyScanColumnFinder.php, src/source/MediaFkScanner.php</files>
  <read_first>
    - ~/Sites/craft-kunstmaan-migrator/src/kunstmaan/schema/BodyScanColumnFinder.php (full file, ~147 LOC — body regex at line 83 is load-bearing; cache+override pattern at lines 53-129)
    - ~/Sites/craft-kunstmaan-migrator/src/kunstmaan/schema/MediaFkScanner.php (full file, ~150 LOC — media regex at line 87 is load-bearing)
    - .planning/phases/02.1-source-introspection/02.1-PATTERNS.md (sections 6 + 7)
    - src/source/KunstmaanCoreTables.php (Plan 02 Task 1 output — used for $coreTables exclusion; references the 7 ported consts by name)
    - src/db/LegacyDbService.php (lines 33-48 — queryAll bound-parameter API)
  </read_first>
  <action>
    Verbatim port for both files with identical reshape pattern.

    For each (BodyScanColumnFinder, MediaFkScanner):

    1. Copy v1 file `~/Sites/craft-kunstmaan-migrator/src/kunstmaan/schema/{ClassName}.php` to `src/source/{ClassName}.php`.
    2. **Reshape A — namespace flatten:** namespace → `lameco\kunstmaanmigrator\source;`.
    3. **Reshape B — `use` for LegacyDbService:** `use lameco\kunstmaanmigrator\kunstmaan\db\LegacyDbService;` → `use lameco\kunstmaanmigrator\db\LegacyDbService;`.
    4. **Reshape C — drop v1's `KunstmaanCoreTables` import** (vendor-injected variant) and replace with `use lameco\kunstmaanmigrator\source\KunstmaanCoreTables;`. Wherever v1 references `KunstmaanCoreTables::NODES` etc., these constants resolve directly against the v2 sibling const class created in Plan 02 (which now exposes all 7 consts: NODES plural, NODE_TRANSLATIONS, NODE_VERSIONS, PAGE_PART_REFS, MEDIA, SEO, REDIRECTS).

    **Preserve byte-for-byte:**
    - **BodyScanColumnFinder** body-column regex literal: `'/(content|intro|summary|body|description)/i'` (v1 line 83).
    - **MediaFkScanner** media-FK regex literal: `'/(image|icon|logo|banner|video|file|media|preview).*_?id$|^image$|^video$/i'` (v1 line 87).
    - The cache + override pattern (singleton-per-request `?array $cache = null`, `$includes` / `$excludes` developer-authored override pairs, `||`-keyed dedup).
    - Bound-parameter SQL for information_schema queries — `:db`, `:prefix`, etc. NEVER concatenate.
    - `final class X extends Component` modifier and class-level docblock.
    - Public field declarations: `public ?LegacyDbService $legacyDb = null;`, `public string $legacyTablePrefix = 'lameco_websitebundle_';`, `public array $includes = [];`, `public array $excludes = [];`.
    - The `find()` (or whatever the v1 public accessor is named) public method signature.

    **DO NOT** add return types where v1 omits them. Preserve every Craft::warning() call. No code-execution primitives.
  </action>
  <verify>
    <automated>php -l src/source/BodyScanColumnFinder.php &amp;&amp; php -l src/source/MediaFkScanner.php &amp;&amp; composer test 2>&1 | grep -E "OK|FAILURES"</automated>
  </verify>
  <acceptance_criteria>
    - `php -l` reports "No syntax errors" for both files.
    - `composer test` exits 0.
    - `grep -c "namespace lameco\\\\kunstmaanmigrator\\\\source;" src/source/BodyScanColumnFinder.php` returns 1.
    - `grep -c "namespace lameco\\\\kunstmaanmigrator\\\\source;" src/source/MediaFkScanner.php` returns 1.
    - `grep -c "final class BodyScanColumnFinder extends Component" src/source/BodyScanColumnFinder.php` returns 1.
    - `grep -c "final class MediaFkScanner extends Component" src/source/MediaFkScanner.php` returns 1.
    - `grep -F "(content|intro|summary|body|description)" src/source/BodyScanColumnFinder.php | wc -l` ≥ 1 (body regex preserved).
    - `grep -F "(image|icon|logo|banner|video|file|media|preview)" src/source/MediaFkScanner.php | wc -l` ≥ 1 (media regex preserved).
    - `grep -c "use lameco\\\\kunstmaanmigrator\\\\db\\\\LegacyDbService;" src/source/BodyScanColumnFinder.php` returns 1.
    - `grep -c "use lameco\\\\kunstmaanmigrator\\\\db\\\\LegacyDbService;" src/source/MediaFkScanner.php` returns 1.
    - `grep -c "':db' =>" src/source/BodyScanColumnFinder.php` ≥ 1 (bound parameter).
    - `grep -c "':db' =>" src/source/MediaFkScanner.php` ≥ 1.
    - `wc -l src/source/BodyScanColumnFinder.php` ≥ 130.
    - `wc -l src/source/MediaFkScanner.php` ≥ 130.
  </acceptance_criteria>
  <done>BodyScan + MediaFk ported. Regexes preserved verbatim. SQL bound-parameter safety preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Port TopologicalOrderer with $warnings out-parameter reshape</name>
  <files>src/source/TopologicalOrderer.php</files>
  <read_first>
    - ~/Sites/craft-kunstmaan-migrator/src/kunstmaan/schema/TopologicalOrderer.php (full file, ~135 LOC — cycle + orphan detection at lines 56-109 is load-bearing)
    - .planning/phases/02.1-source-introspection/02.1-PATTERNS.md (section 8 — TopologicalOrderer port instructions, including the MigrationReport replacement)
  </read_first>
  <action>
    Verbatim port with three reshape edits.

    1. Copy v1 file `~/Sites/craft-kunstmaan-migrator/src/kunstmaan/schema/TopologicalOrderer.php` to `src/source/TopologicalOrderer.php`.
    2. **Reshape A — namespace flatten:** namespace → `lameco\kunstmaanmigrator\source;`.
    3. **Reshape B — drop `MigrationReport` import** (v1 line 5 or thereabouts) and replace the parameter pattern. v1's signature has `?MigrationReport $report = null` and calls `$report->warn(...)`. Replace with the simpler out-parameter:
       - Method signature: `public function order(array $rows, array &$warnings = []): array` (or whatever the v1 public method is named — preserve that name; only change the `$report` parameter to `array &$warnings`).
       - `$report->warn(sprintf('Cycle detected in kuma_nodes at id=%d', $id))` → `$warnings[] = sprintf('Cycle detected in kuma_nodes at id=%d', $id);`.
       - All other `$report->warn(...)` calls (orphan detection, depth-guard, etc.) follow the same translation.
    4. **Reshape C — add `declare(strict_types=1);`** if v1 omits it (PATTERNS section 8 calls this out as a v2 convention to add).

    **Preserve byte-for-byte:**
    - The closure-based `$computeDepth` recursive function with `$visiting`, `$depth`, `$byId` state arrays (v1 lines 56-109).
    - The `MAX_DEPTH_GUARD` constant.
    - The `PHP_INT_MAX` sentinel for cycle/orphan members.
    - The `kuma_parent_id` column reference literal.
    - The depth-first-search `usort` ordering at the end (parent-first).
  </action>
  <verify>
    <automated>php -l src/source/TopologicalOrderer.php &amp;&amp; composer test 2>&1 | grep -E "OK|FAILURES"</automated>
  </verify>
  <acceptance_criteria>
    - `php -l src/source/TopologicalOrderer.php` reports "No syntax errors".
    - `composer test` exits 0.
    - `grep -c "namespace lameco\\\\kunstmaanmigrator\\\\source;" src/source/TopologicalOrderer.php` returns 1.
    - `grep -c "declare(strict_types=1);" src/source/TopologicalOrderer.php` returns 1.
    - `grep -c "MigrationReport" src/source/TopologicalOrderer.php` returns 0 (replaced).
    - `grep -c "array &\$warnings" src/source/TopologicalOrderer.php` ≥ 1 (replacement signature).
    - `grep -c "PHP_INT_MAX" src/source/TopologicalOrderer.php` ≥ 2 (cycle + orphan sentinels).
    - `grep -c "kuma_parent_id" src/source/TopologicalOrderer.php` ≥ 1.
    - `grep -c "MAX_DEPTH_GUARD" src/source/TopologicalOrderer.php` ≥ 1.
    - `grep -c "Cycle detected" src/source/TopologicalOrderer.php` ≥ 1.
    - `wc -l src/source/TopologicalOrderer.php` ≥ 120.
  </acceptance_criteria>
  <done>TopologicalOrderer ported. Cycle + orphan detection preserved. v2 strict-types convention applied. MigrationReport replaced with out-parameter.</done>
</task>

<task type="auto">
  <name>Task 3: Port KnowledgeBase (renderPagesMarkdown + renderPagePartsMarkdown + transitively-reachable helpers, including loadAllColumns + CORE_TABLES) — partial port (~400-800 LOC) + smoke test</name>
  <files>src/source/KnowledgeBase.php, tests/source/KnowledgeBaseSmokeTest.php</files>
  <read_first>
    - ~/Sites/craft-kunstmaan-migrator/src/kunstmaan/schema/knowledgebase/KnowledgeBase.php — read these sections specifically:
      - Class header lines 17-72 (public fields, consts SAMPLE_MAX_LEN/MAX_SAMPLES, IDENT_RX const, **CORE_TABLES private const at line 67** — load-bearing for loadAllColumns)
      - **`loadAllColumns()` body at line 774** (~38 LOC — the SELECT TABLE_NAME, COLUMN_NAME, DATA_TYPE, IS_NULLABLE FROM information_schema.COLUMNS query with the prefix + CORE_TABLES IN-clause; called by both render methods)
      - `renderPagePartsMarkdown` body starting at line 111 (read until method end — likely ~200 LOC; calls $this->loadAllColumns() at line 119)
      - `renderPagesMarkdown` body starting at line 388 (read until method end — likely ~200 LOC; calls $this->loadAllColumns() at line 394)
      - **Recursive call-graph walk from both public render methods** — port every private method that's reachable. Likely transitively-reachable helpers (verify by reading):
        - `loadAllColumns()` (line 774) — both render methods call it
        - `fetchSamples()` (line 817) — sample collection
        - `formatSamples()` (line 835) — sample formatting
        - `truncateSample()` (line 846) — per-sample truncation
        - `isSafeIdent()` (line 867) — IDENT_RX validation
        - `renderTableColumns()` (line 543) — likely called by renderPagesMarkdown
        - Any other private methods invoked by the two render methods or by the helpers above (transitive closure — keep walking until fixed point)
    - .planning/phases/02.1-source-introspection/02.1-PATTERNS.md (section 9 — port scope = ONLY two methods + their transitively-reachable helpers)
    - src/source/DoctrineEntityParser.php (Plan 02 Task 2 output — KnowledgeBase composes this)
    - src/source/KunstmaanCoreTables.php (Plan 02 Task 1 output — all 7 consts available; CORE_TABLES private const references KunstmaanCoreTables::SEO / ::MEDIA / ::REDIRECTS by name)
    - src/analyze/LlmClassifier.php (lines 130-140 — read the call site that consumes the KB markdown to verify the contract: two strings, classifier truncates internally at 8000 chars)
  </read_first>
  <action>
    **Partial port — only two public methods + their transitive private helpers, walked recursively from the two render methods.**

    **Transitive-walk port rule (LOAD-BEARING — B3 fix):** Start from `renderPagesMarkdown` and `renderPagePartsMarkdown`. For each private method/const referenced (`$this->methodName(...)` or `self::CONST`), port it verbatim. Then walk into THAT method's body and port its transitive references. Repeat until you hit fixed point. The minimum reachable set includes: `loadAllColumns`, `CORE_TABLES`, `fetchSamples`, `formatSamples`, `truncateSample`, `isSafeIdent`, `IDENT_RX`, plus any others surfaced by the walk. Do NOT skip helpers because they "look unrelated" — if both render methods can reach them, they ship.

    1. Create `src/source/KnowledgeBase.php` with the v1 class header copied verbatim:
       - `<?php declare(strict_types=1);`
       - `namespace lameco\kunstmaanmigrator\source;`
       - `use Craft; use lameco\kunstmaanmigrator\db\LegacyDbService; use yii\base\Component;` and any other use statements the ported methods need.
       - Class docblock describing the partial-port scope (per PATTERNS section 9).
       - `final class KnowledgeBase extends Component`.
       - Public fields: `public ?LegacyDbService $legacyDb = null;`, `public ?DoctrineEntityParser $entityParser = null;`, `public string $legacyTablePrefix = 'lameco_websitebundle_';`.
       - Constants: `public const SAMPLE_MAX_LEN = 40;`, `public const MAX_SAMPLES = 3;`, `private const IDENT_RX = '/^[A-Za-z0-9_]+$/';`.
       - **`private const CORE_TABLES` array (verbatim from v1 line 67)** — the array literal `[KunstmaanCoreTables::SEO, KunstmaanCoreTables::MEDIA, KunstmaanCoreTables::REDIRECTS]`. This const is consumed by `loadAllColumns` at v1 line 782.

    2. Port `renderPagesMarkdown(?array $mapping, \DateTimeInterface $now): string` byte-for-byte from v1 (line 388 onward). Preserve every loop, every Markdown heading, every sample-truncation call, the `$this->loadAllColumns()` call at line 394, and the v1 mapping-array shape (`$mapping['nodeClasses']`, `$mapping['pageParts']`, `$mapping['sections']` reads at v1 lines 397-399).

    3. Port `renderPagePartsMarkdown(?array $mapping, \DateTimeInterface $now): string` byte-for-byte from v1 (line 111 onward). Same preservation rule — including the `$mapping['pageParts']` read at v1 line 118.

    4. **Port every transitively-reachable private helper.** Minimum confirmed-reachable set (do the walk to confirm; expand if more helpers surface):
       - `loadAllColumns()` (v1 line 774, ~38 LOC) — copy verbatim. Preserve the bound-parameter SQL pattern (`:db`, `:prefix`, `:ct0..ctN` for CORE_TABLES IN-clause). Preserve the `isSafeIdent` validation at v1 line 802.
       - `fetchSamples()` (v1 line 817)
       - `formatSamples()` (v1 line 835)
       - `truncateSample()` (v1 line 846)
       - `isSafeIdent()` (v1 line 867)
       - `renderTableColumns()` (v1 line 543) — verify it's called by renderPagesMarkdown; if so, port verbatim
       - Any others surfaced by the walk

    5. **DO NOT port:** `buildPages()` / `buildPageParts()` file-I/O wrappers (v1 lines 96, 376), `reachableTables()` (line 862 — public, but not called by the two render methods), `buildMatrixAvailabilityIndex()` (line 1220), `computeReachableSet()` (line 623), `walkFieldsForSubTables` / `followEntityRelations` (lines 689 / 732 — only reachable from `computeReachableSet`, not from the render methods), `renderCraftMarkdown` + `loadFields` / `loadEntryTypes` / `loadSections` / `collectLayoutFields` / `findSectionForEntryType` / `resolveMatrixBlocks` / `classify` / `fieldNotes` / `loadVolumes` / `loadGlobalSetsAndCategories` / `resolveProjectConfigDir` / `glob` / `parseYaml` / `uidFromFilename` (lines 893+, all part of v1's Craft-side KB which is out of scope for this phase). (`reachableTables` and `buildMatrixAvailabilityIndex` MAY be needed by Plan 09 reconciliation — Plan 09 will port them then. Don't port preemptively.)

    6. **Reshape:**
       - Drop `KunstmaanCoreTables` import (v1 vendor variant) and use the v2 sibling: `use lameco\kunstmaanmigrator\source\KunstmaanCoreTables;`. The CORE_TABLES private const references KunstmaanCoreTables::SEO / ::MEDIA / ::REDIRECTS — those resolve against the v2 sibling (Plan 02 ported all 7 consts).
       - Drop `KnowledgeBasePaths` import (file-path helper — irrelevant for in-memory string output).
       - Drop `MigrationReport` import if present in the ported scope; replace with silent skip or `Craft::warning(...)` (v2 idiom).
       - All SQL queries against information_schema or legacy DB MUST use bound parameters (preserve T-05.5-03-01 mitigation).

    7. The 8000-char truncation contract lives in `LlmClassifier::batchPropose` (already at line 133 — calls `wasTruncated($legacyKbMarkdown, 8000)`). KnowledgeBase emits FULL text; classifier handles the cap. Do NOT add truncation logic in KnowledgeBase.

    8. **Smoke test (NEW — caps the dropped-helpers risk):** Create `tests/source/KnowledgeBaseSmokeTest.php` (PHPUnit 11). The test:
       - Constructs `KnowledgeBase` with a stub/mock `LegacyDbService` whose `queryAll` returns small fixed arrays for the SQL patterns: `SELECT DATABASE()` → `[['DATABASE()' => 'test_db']]` (or whatever scalar shape v1's loadAllColumns expects via `queryScalar` — match the v1 call); information_schema.COLUMNS query → 1-2 fake rows; the kuma_nodes / kuma_page_part_refs queries → empty arrays.
       - Calls `$kb->renderPagesMarkdown(null, new \DateTimeImmutable('2026-01-01'))` and asserts the return is a non-empty string starting with `# Kunstmaan Pages`.
       - Calls `$kb->renderPagePartsMarkdown(null, new \DateTimeImmutable('2026-01-01'))` and asserts the return is a non-empty string starting with `# Kunstmaan Page Parts`.
       - The test passes only if every transitively-reachable private method is ported (a missing helper → fatal Error: Call to undefined method).
       - Use a minimal stub class extending LegacyDbService or a PHPUnit createMock. Whichever is simpler; the goal is "constructs and runs the render path without fatal".
  </action>
  <verify>
    <automated>php -l src/source/KnowledgeBase.php &amp;&amp; php -l tests/source/KnowledgeBaseSmokeTest.php &amp;&amp; composer test 2>&1 | grep -E "OK|FAILURES"</automated>
  </verify>
  <acceptance_criteria>
    - `php -l src/source/KnowledgeBase.php` reports "No syntax errors".
    - `php -l tests/source/KnowledgeBaseSmokeTest.php` reports "No syntax errors".
    - `composer test` exits 0 (smoke test passes — proves transitive walk is complete).
    - `grep -c "namespace lameco\\\\kunstmaanmigrator\\\\source;" src/source/KnowledgeBase.php` returns 1.
    - `grep -c "final class KnowledgeBase extends Component" src/source/KnowledgeBase.php` returns 1.
    - `grep -c "public function renderPagesMarkdown" src/source/KnowledgeBase.php` returns 1.
    - `grep -c "public function renderPagePartsMarkdown" src/source/KnowledgeBase.php` returns 1.
    - **B3 transitive-helpers assertions:**
      - `grep -c "private function loadAllColumns" src/source/KnowledgeBase.php` ≥ 1 (LOAD-BEARING — both render methods call it).
      - `grep -c "private const CORE_TABLES" src/source/KnowledgeBase.php` ≥ 1 (used by loadAllColumns).
      - `grep -c "loadAllColumns()" src/source/KnowledgeBase.php` ≥ 2 (called from both render methods).
      - `grep -c "private function fetchSamples\\|private function formatSamples\\|private function truncateSample\\|private function isSafeIdent" src/source/KnowledgeBase.php` ≥ 4 (transitive helpers).
    - `grep -c "public ?LegacyDbService \$legacyDb" src/source/KnowledgeBase.php` returns 1.
    - `grep -c "public ?DoctrineEntityParser \$entityParser" src/source/KnowledgeBase.php` returns 1.
    - `grep -c "SAMPLE_MAX_LEN" src/source/KnowledgeBase.php` ≥ 2 (const def + use site).
    - `grep -c "MAX_SAMPLES" src/source/KnowledgeBase.php` ≥ 2.
    - `grep -c "IDENT_RX" src/source/KnowledgeBase.php` ≥ 2.
    - `grep -c "buildPages\\|buildPageParts" src/source/KnowledgeBase.php` returns 0 (file-I/O wrappers explicitly excluded).
    - `grep -c "computeReachableSet\\|buildMatrixAvailabilityIndex\\|renderCraftMarkdown" src/source/KnowledgeBase.php` returns 0 (out-of-scope helpers excluded; Plan 09 may port reachableTables / buildMatrixAvailabilityIndex separately if reconciliation surfaces a gap).
    - `grep -c "use lameco\\\\kunstmaanmigrator\\\\db\\\\LegacyDbService;" src/source/KnowledgeBase.php` returns 1.
    - `grep -c "KunstmaanCoreTables" src/source/KnowledgeBase.php` ≥ 1.
    - `wc -l src/source/KnowledgeBase.php` ≥ 400.
    - **Smoke test assertions:**
      - `wc -l tests/source/KnowledgeBaseSmokeTest.php` ≥ 40.
      - `grep -c "renderPagesMarkdown" tests/source/KnowledgeBaseSmokeTest.php` ≥ 1.
      - `grep -c "renderPagePartsMarkdown" tests/source/KnowledgeBaseSmokeTest.php` ≥ 1.
      - `grep -c "# Kunstmaan Pages" tests/source/KnowledgeBaseSmokeTest.php` ≥ 1 (asserts the page-render header).
      - `grep -c "# Kunstmaan Page Parts" tests/source/KnowledgeBaseSmokeTest.php` ≥ 1 (asserts the pageparts-render header).
  </acceptance_criteria>
  <done>KnowledgeBase partial port lands with all transitively-reachable private helpers (loadAllColumns + CORE_TABLES + fetchSamples + formatSamples + truncateSample + isSafeIdent + any others surfaced by the walk). Smoke test proves both render methods execute without missing helpers. Plan 6 will wire into LlmClassifier (replacing the `'', ''` placeholders).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied legacyDbServer / legacyDbDatabase → information_schema queries via BodyScan / MediaFk / KnowledgeBase | identifier-shaped operator input crosses SQL boundary |
| legacy DB row data → KnowledgeBase sample collection → LlmClassifier prompt → outbound Anthropic API call | legacy DB content (potentially PII) flows to a third-party API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-03-01 | Tampering | BodyScanColumnFinder + MediaFkScanner SQL | mitigate | All information_schema queries use bound parameters (`:db`, `:prefix`). Identifiers are validated against `IDENT_RX` (in KnowledgeBase) before interpolation in dynamic queries. Pattern T-05.5-03-01 from v1 hardening preserved verbatim. |
| T-02.1-03-02 | Information Disclosure | KnowledgeBase sample collection | accept | Samples are truncated to `SAMPLE_MAX_LEN` (40 chars) and capped at `MAX_SAMPLES` (3 per column). The plugin is dev/staging only (NeverProductionTrait gates the analyze command — Phase 1 / D-20). PII risk acknowledged; operator's responsibility under the dev/staging-only deployment model. |
| T-02.1-03-03 | Denial of Service | TopologicalOrderer recursive depth | mitigate | `MAX_DEPTH_GUARD` constant (preserved verbatim from v1) caps recursion; cycle detection short-circuits to PHP_INT_MAX. The closure pattern is iterative with explicit `$visiting` set, not stack-blowing. |
| T-02.1-03-04 | Tampering | KnowledgeBase identifier interpolation | mitigate | The `IDENT_RX = '/^[A-Za-z0-9_]+$/'` const validates every dynamically-built table/column identifier before splicing into SQL queries. Failed validation → skip the column with a `Craft::warning`, do not interpolate. |
</threat_model>

<verification>
- `composer test` exits 0; PluginBootstrapTest D-21 invariant intact; KnowledgeBaseSmokeTest passes (proves transitive walk).
- `php -l` clean for all four ported files + smoke test.
- LOC totals: BodyScan ≥ 130, MediaFk ≥ 130, TopologicalOrderer ≥ 120, KnowledgeBase ≥ 400, KnowledgeBaseSmokeTest ≥ 40.
- Bound-parameter SQL preserved (grep for `:db` / `:prefix` returns ≥ 1 in BodyScan, MediaFk, KnowledgeBase).
- All four files namespace-flat under `lameco\kunstmaanmigrator\source`.
- Body regex literal preserved verbatim in BodyScan; media regex literal preserved verbatim in MediaFk.
- Transitively-reachable helpers ported: loadAllColumns, CORE_TABLES const, fetchSamples, formatSamples, truncateSample, isSafeIdent (and any others surfaced by the recursive call-graph walk from the two render methods).
</verification>

<success_criteria>
SRC-13 (KnowledgeBase Markdown generator ported and ready for LlmClassifier wiring) and SRC-14 (BodyScan + MediaFk + Topological ported as part of scanner output) substantially complete. Plan 5 will compose these in KunstmaanSourceScanner; Plan 6 will wire KnowledgeBase into LlmClassifier.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-source-introspection/02.1-03-SUMMARY.md`.
</output>
