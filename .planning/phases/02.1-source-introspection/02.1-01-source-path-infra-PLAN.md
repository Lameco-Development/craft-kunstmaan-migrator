---
phase: 02.1-source-introspection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Plugin.php
  - src/models/Settings.php
  - src/source/KunstmaanSourcePathResolver.php
  - src/console/DoctorController.php
  - src/console/AnalyzeController.php
  - src/console/MapController.php
autonomous: true
requirements: [SRC-10]
must_haves:
  truths:
    - "Settings declares public ?string $kunstmaanSourcePath, sourced from KUNSTMAAN_SOURCE_PATH env via ??= ladder."
    - "KunstmaanSourcePathResolver::resolve() returns absolute realpath when valid; null when unset/missing/no src/Entity/."
    - "DoctorController reports OK / FAIL for the 5th check (Kunstmaan source path); FAIL when unset/invalid; WARN when config/kunstmaancms/pageparts/ absent (PHP-only fallback)."
    - "AnalyzeController and MapController declare --source-strict CLI option (consumer wires later in Plan 5/6/8)."
    - "Plugin::config() registers 'kunstmaanSourcePathResolver' => KunstmaanSourcePathResolver::class appended below existing 10 entries; PluginBootstrapTest stays green."
  artifacts:
    - path: "src/source/KunstmaanSourcePathResolver.php"
      provides: "Yii Component resolving the Kunstmaan source path with ??= env fallback + realpath validation."
      min_lines: 30
    - path: "src/models/Settings.php"
      provides: "kunstmaanSourcePath property + EnvAttributeParserBehavior + init() + rules entries."
      contains: "public ?string $kunstmaanSourcePath"
    - path: "src/console/DoctorController.php"
      provides: "checkKunstmaanSourcePath() 5th check + actionIndex && chain."
      contains: "checkKunstmaanSourcePath"
    - path: "src/Plugin.php"
      provides: "kunstmaanSourcePathResolver component registered."
      contains: "'kunstmaanSourcePathResolver'"
  key_links:
    - from: "src/console/DoctorController.php::actionIndex"
      to: "src/source/KunstmaanSourcePathResolver.php::resolve"
      via: "Plugin::getInstance()->kunstmaanSourcePathResolver->resolve()"
      pattern: "kunstmaanSourcePathResolver->resolve"
    - from: "src/models/Settings.php::init"
      to: "App::env('KUNSTMAAN_SOURCE_PATH')"
      via: "??= idiom"
      pattern: "KUNSTMAAN_SOURCE_PATH"
---

<objective>
Land the Phase 02.1 source-path infrastructure: Settings field declaration (D-30), KunstmaanSourcePathResolver Yii Component (D-33), DoctorController 5th check (D-31), --source-strict flag declarations on AnalyzeController/MapController (D-32 — consumer wiring deferred to later plans), and Plugin::config() registration. This plan unblocks every other Phase 02.1 plan.

Purpose: every later plan reads the resolved source path. Putting the resolver, settings field, and doctor check in wave 1 means plans 2-9 can assume the path is configured. Doctor failing fast (not WARN) per D-31 is the operator-overruled-roadmap decision; greenfield-fallback is dropped.

Output: Settings + Resolver + Doctor + 2 CLI flag declarations + 1 Plugin.php component registration. Component count grows 10 → 11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-source-introspection/02.1-CONTEXT.md
@.planning/phases/02.1-source-introspection/02.1-PATTERNS.md

<interfaces>
<!-- Existing v2 components that this plan extends. Excerpted from codebase. -->

From src/db/LegacyDbService.php (lines 24-32 — Yii Component shape to mirror):
```php
final class LegacyDbService extends Component
{
    public function db(): Connection
    {
        /** @var Connection $conn */
        $conn = Craft::$app->get('legacyDb');
        return $conn;
    }
}
```

From src/models/Settings.php (lines 28-93 — declared-field-list + behaviors + init + rules pattern to extend):
```php
public ?string $anthropicApiKey      = null;

public ?string $llmModel             = null;
public ?int    $llmTimeout           = null;
public ?int    $llmInterChunkDelay   = null;
public ?string $mappingPath          = null;
// (... behaviors EnvAttributeParserBehavior::attributes list at line ~57 ...)
// (... init() ??= ladder at lines 67-93 ...)
// (... rules at lines 95-106 ...)
```

From src/console/DoctorController.php (lines 44-58 — actionIndex && chain to extend):
```php
$ok = $this->checkLegacyDb()              && $ok;
$ok = $this->checkApiKey()                && $ok;
$ok = $this->checkStorageDir()            && $ok;
$ok = $this->checkMappingFile()           && $ok;
```

From src/Plugin.php (lines 48-63 — Plugin::config() components map to extend):
```php
public static function config(): array
{
    return [
        'components' => [
            'legacyDbService' => LegacyDbService::class,      // Phase 1 (literal preserved for PluginBootstrapTest)
            'filterFactory'     => FilterFactory::class,
            // ... 8 more lines (filterFactory ... mappingAuditor) ...
        ],
    ];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings field + Resolver service + Plugin registration</name>
  <files>src/models/Settings.php, src/source/KunstmaanSourcePathResolver.php, src/Plugin.php</files>
  <read_first>
    - src/models/Settings.php (current 109 lines — see the field block at lines 28-35, EnvAttributeParserBehavior::attributes at ~line 57, init() ??= ladder at lines 67-93, rules at lines 95-106)
    - src/db/LegacyDbService.php (24-32 — final-class-extends-Component shape to mirror)
    - src/Plugin.php (full file 117 lines — note the @property-read PHPDoc block lines 27-37 and the components map lines 48-63)
    - .planning/phases/02.1-source-introspection/02.1-PATTERNS.md (sections 1, 2, 20 — Resolver/Settings/Plugin patterns)
    - .planning/phases/01-foundation-connectivity/01-CONTEXT.md (D-12, D-15, D-21 PluginBootstrapTest invariant)
  </read_first>
  <action>
    Three sub-edits. Do them in this order.

    **A. src/models/Settings.php** (per D-30 + PATTERNS section 2):
    - Add a new public property `public ?string $kunstmaanSourcePath = null;` directly after the `public ?string $mappingPath = null;` declaration (preserve the upfront-declaration block style — D-15).
    - Add `'kunstmaanSourcePath'` to the `EnvAttributeParserBehavior::attributes` array (the one that already lists 'legacyDbServer' through 'defaultSince').
    - Append `$this->kunstmaanSourcePath ??= App::env('KUNSTMAAN_SOURCE_PATH') ?: null;` to the `init()` method, after the `anthropicApiKey` line. Use `??=` (NOT `??`) and `?: null` (NOT `?? null`) per PATTERNS Pattern D.
    - Add `'kunstmaanSourcePath'` to the existing `string` rules group: change `[['anthropicApiKey', 'llmModel', 'mappingPath', 'defaultSince'], 'string']` to `[['anthropicApiKey', 'llmModel', 'mappingPath', 'defaultSince', 'kunstmaanSourcePath'], 'string']`.

    **B. src/source/KunstmaanSourcePathResolver.php** (NEW FILE per D-33 + PATTERNS section 1):
    - PSR-4 path: `src/source/KunstmaanSourcePathResolver.php`.
    - Namespace: `lameco\kunstmaanmigrator\source` (D-41 flat slot).
    - `<?php declare(strict_types=1);` header.
    - Class: `final class KunstmaanSourcePathResolver extends Component` (extends `yii\base\Component`).
    - Single public method `public function resolve(): ?string` that:
      1. Reads `(string) (Plugin::getInstance()->getSettings()->kunstmaanSourcePath ?? '')`.
      2. Returns `null` when string is empty.
      3. Calls `realpath($settingsPath)` — if `false` or not `is_dir(...)`, returns `null`.
      4. If `!is_dir($real . '/src/Entity')`, returns `null`.
      5. Returns the validated `$real` (absolute path).
    - Use statements: `Craft`, `lameco\kunstmaanmigrator\Plugin`, `yii\base\Component`.
    - Class docblock describing D-30 / D-33 contract: env-fallback + realpath + src/Entity check.

    **C. src/Plugin.php** (per PATTERNS section 20):
    - Add use statement: `use lameco\kunstmaanmigrator\source\KunstmaanSourcePathResolver;`
    - Append `'kunstmaanSourcePathResolver' => KunstmaanSourcePathResolver::class,` to the `components` array AFTER the existing 10 entries (after `mappingAuditor`). The new line is the 11th entry. Add a trailing comment: `// Phase 02.1 (Plan 01) — D-33 source-path resolver`.
    - Append `@property-read KunstmaanSourcePathResolver $kunstmaanSourcePathResolver` to the PHPDoc block (after `@property-read MappingAuditor $mappingAuditor`).
    - **DO NOT MODIFY** the existing `'legacyDbService' => LegacyDbService::class,` line — its alignment, single-space `=>`, and comment are asserted byte-for-byte by `tests/PluginBootstrapTest.php::testPluginDeclaresLegacyDbServiceComponent` (D-21 invariant).
  </action>
  <verify>
    <automated>composer test 2>&1 | grep -E "OK|FAILURES" &amp;&amp; grep -c "kunstmaanSourcePath" src/models/Settings.php &amp;&amp; grep -c "KUNSTMAAN_SOURCE_PATH" src/models/Settings.php &amp;&amp; grep -c "kunstmaanSourcePathResolver" src/Plugin.php &amp;&amp; php -l src/source/KunstmaanSourcePathResolver.php</automated>
  </verify>
  <acceptance_criteria>
    - `composer test` exits 0 (PluginBootstrapTest still green — D-21 invariant).
    - `grep -c "public ?string \$kunstmaanSourcePath" src/models/Settings.php` returns ≥ 1.
    - `grep -c "KUNSTMAAN_SOURCE_PATH" src/models/Settings.php` returns ≥ 1 (the env read line).
    - `grep -c "'kunstmaanSourcePath'" src/models/Settings.php` returns ≥ 2 (one in EnvAttributeParserBehavior::attributes, one in rules).
    - `grep -c "??= App::env('KUNSTMAAN_SOURCE_PATH')" src/models/Settings.php` returns 1.
    - `php -l src/source/KunstmaanSourcePathResolver.php` reports "No syntax errors".
    - `grep -c "final class KunstmaanSourcePathResolver extends Component" src/source/KunstmaanSourcePathResolver.php` returns 1.
    - `grep -c "namespace lameco\\\\kunstmaanmigrator\\\\source;" src/source/KunstmaanSourcePathResolver.php` returns 1.
    - `grep -c "is_dir(\$real . '/src/Entity')" src/source/KunstmaanSourcePathResolver.php` returns 1.
    - `grep -c "'kunstmaanSourcePathResolver' => KunstmaanSourcePathResolver::class" src/Plugin.php` returns 1.
    - `grep -c "'legacyDbService' => LegacyDbService::class," src/Plugin.php` returns 1 (PluginBootstrapTest line untouched).
    - `grep -c "@property-read KunstmaanSourcePathResolver \$kunstmaanSourcePathResolver" src/Plugin.php` returns 1.
  </acceptance_criteria>
  <done>Resolver lands; Plugin registers it; Settings declares the field with env fallback; PluginBootstrapTest stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Doctor 5th check + AnalyzeController/MapController --source-strict declarations</name>
  <files>src/console/DoctorController.php, src/console/AnalyzeController.php, src/console/MapController.php</files>
  <read_first>
    - src/console/DoctorController.php (full ~179 lines — note the actionIndex && chain at lines 44-58 and the existing `checkMappingFile` at lines 158-178 for shape to mirror)
    - src/console/AnalyzeController.php (lines 1-80 — the options() declaration and public property block; --noAi/--autoAcceptHigh/--auditStrict are the existing flags)
    - src/console/MapController.php (lines 1-80 — same shape; --autoAcceptHigh + --entities + --locales + --since are the existing flags)
    - .planning/phases/02.1-source-introspection/02.1-PATTERNS.md (section 18 — DoctorController 5th check, section 17 — AnalyzeController flag declaration pattern)
    - .planning/phases/02.1-source-introspection/02.1-CONTEXT.md (D-31, D-32 — strict mode opt-in)
  </read_first>
  <action>
    Three sub-edits.

    **A. src/console/DoctorController.php** (per D-31 + PATTERNS section 18):
    - Add a new private method `checkKunstmaanSourcePath(): bool` modeled byte-for-byte on the PATTERNS section 18 example. The body:
      1. `$resolver = Plugin::getInstance()->kunstmaanSourcePathResolver;`
      2. `$path = $resolver->resolve();`
      3. If `$path === null`, emit (FG_RED stderr): `"  FAIL KUNSTMAAN_SOURCE_PATH unset or invalid — analyze cannot proceed.\n       Set KUNSTMAAN_SOURCE_PATH in .env (or kunstmaanSourcePath in plugin settings).\n       Path must exist, be readable, and contain src/Entity/.\n"` and `return false;`.
      4. If `!is_dir($path . '/config/kunstmaancms/pageparts')`, emit (FG_YELLOW stdout): `"  WARN config/kunstmaancms/pageparts/ not found at {$path} — falling back to PHP-only page-part scan\n"` (per D-31 Discretion locked in this plan: YAML config absence is WARN, not FAIL).
      5. Emit (FG_GREEN stdout): `"  OK   Kunstmaan source path → {$path}\n"`. `return true;`
    - Append to the && chain in `actionIndex()`: `$ok = $this->checkKunstmaanSourcePath() && $ok;` as the LAST line of the chain (after `checkMappingFile`).

    **B. src/console/AnalyzeController.php** (per D-32 — declare --source-strict; consumer wires in Plan 5):
    - Add a new public property `public bool $sourceStrict = false;` near the existing `public bool $auditStrict = false;` declaration.
    - Add `'source-strict'` to the `options()` method's returned array (alongside 'audit-strict').
    - Add `'source-strict' => 'sourceStrict'` to `optionAliases()` if that method exists; if not, just rely on the kebab-case auto-mapping (Yii's default).
    - **DO NOT** add any consumer logic — the flag is declared-only here. Plan 5 wires `KunstmaanSourceScanner` to consume it for drift-strict elevation.

    **C. src/console/MapController.php** (per D-32 — same flag, declared-only):
    - Same edits as AnalyzeController: add `public bool $sourceStrict = false;`, add `'source-strict'` to `options()`. Consumer wires later (Plan 7's two-mode walker may consult it for page-part drift signals; out of scope here).
  </action>
  <verify>
    <automated>composer test 2>&1 | grep -E "OK|FAILURES" &amp;&amp; grep -c "checkKunstmaanSourcePath" src/console/DoctorController.php &amp;&amp; grep -c "sourceStrict" src/console/AnalyzeController.php &amp;&amp; grep -c "sourceStrict" src/console/MapController.php &amp;&amp; php -l src/console/DoctorController.php &amp;&amp; php -l src/console/AnalyzeController.php &amp;&amp; php -l src/console/MapController.php</automated>
  </verify>
  <acceptance_criteria>
    - `composer test` exits 0.
    - `php -l` reports "No syntax errors" for all three controllers.
    - `grep -c "checkKunstmaanSourcePath" src/console/DoctorController.php` returns ≥ 2 (method def + && chain call).
    - `grep -c "FAIL KUNSTMAAN_SOURCE_PATH unset or invalid" src/console/DoctorController.php` returns 1.
    - `grep -c "WARN config/kunstmaancms/pageparts" src/console/DoctorController.php` returns 1.
    - `grep -c "OK   Kunstmaan source path" src/console/DoctorController.php` returns 1.
    - `grep -c "public bool \$sourceStrict" src/console/AnalyzeController.php` returns 1.
    - `grep -c "public bool \$sourceStrict" src/console/MapController.php` returns 1.
    - `grep -c "'source-strict'" src/console/AnalyzeController.php` returns ≥ 1.
    - `grep -c "'source-strict'" src/console/MapController.php` returns ≥ 1.
  </acceptance_criteria>
  <done>Doctor reports 5 checks; --source-strict declared on both consumers; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → KUNSTMAAN_SOURCE_PATH env / Settings | operator-supplied path string crosses into filesystem reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01-01 | Tampering | KunstmaanSourcePathResolver::resolve | mitigate | `realpath()` resolves `..` segments and symlinks; explicit `is_dir($real)` and `is_dir($real . '/src/Entity')` checks reject paths that don't resolve under a real directory containing the expected layout. No string concatenation of operator input into shell commands or SQL. |
| T-02.1-01-02 | Information Disclosure | DoctorController error messages | accept | The path is operator-supplied dev-only (NeverProductionTrait still gates analyze/map; doctor itself is read-only). Echoing the configured path back to stderr/stdout is necessary for the operator to debug their misconfiguration. |
| T-02.1-01-03 | Denial of Service | resolve() called on every actionIndex | accept | `realpath()` and two `is_dir()` calls are O(1) syscalls; resolve() is cached implicitly because Yii Components are singletons per request. No re-walk of the filesystem tree. |
</threat_model>

<verification>
- `composer test` exits 0; PluginBootstrapTest's source-level reflection assertion on `'legacyDbService' => LegacyDbService::class,` stays green (D-21 invariant).
- `./craft kunstmaan-migrator/doctor` (when wired in a host) reports 5 checks; FAILs on missing path; WARNs on missing pageparts/ subdir.
- `--source-strict` flag accepted by `analyze` and `map` invocations (no-op until Plan 5/7 wires it).
</verification>

<success_criteria>
SRC-10 (doctor 5th check) satisfied. Settings declares kunstmaanSourcePath upfront. Resolver lives in `src/source/`. Component count: 10 → 11. PluginBootstrapTest invariant preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-source-introspection/02.1-01-SUMMARY.md`.
</output>
