---
phase: 02.1-source-introspection
verified: 2026-04-26T00:00:00Z
status: human_needed
score: 19/19 must-haves verified (static)
overrides_applied: 0
human_verification:
  - test: "Run analyze against the CQM rehearsal pair with KUNSTMAAN_SOURCE_PATH set"
    expected: "storage/migration/schema-dump.json AND storage/migration/pageStructure.json both emitted; coverage measurement is meaningful (NOT 100% fill-rate-zero drops); heuristic-coverage jumps materially vs Phase 2 baseline (target: 40% → ≥70% per CONTEXT)."
    why_human: "Roadmap success criterion #10 explicitly requires re-running analyze against `~/Sites/cqm-craft-website` with `KUNSTMAAN_SOURCE_PATH=~/Sites/cqm-website` and observing a meaningful coverage measurement. Static verification confirms the wiring exists; only a real CLI invocation against the operator's source tree + live legacy DB can confirm the outcome. Cannot be exercised by `composer test` (no real legacy DB or source checkout in unit test bootstrap)."
  - test: "Doctor 5th check end-to-end against three states: unset / invalid / valid path"
    expected: "(a) `KUNSTMAAN_SOURCE_PATH=` unset → `./craft kunstmaan-migrator/doctor` exits non-zero with `FAIL KUNSTMAAN_SOURCE_PATH unset or invalid`. (b) Path set but `config/kunstmaancms/pageparts/` absent → exits zero with `WARN config/kunstmaancms/pageparts/ not found ... falling back to PHP-only page-part scan`. (c) Valid path with both subdirs → exits zero with `OK   Kunstmaan source path → /abs/path`."
    why_human: "Static grep confirms the FAIL/WARN/OK strings + the && chain wiring; runtime exit-code behavior + the FG_RED/FG_YELLOW/FG_GREEN console output need a real shell invocation."
  - test: "Verify B2 KB adapter actually surfaces per-row annotations in the rendered KB markdown end-to-end"
    expected: "After running `analyze` with at least one accepted column row and one page-part row in mapping.yaml, the KB markdown that gets sent to LlmClassifier (capture via debug logging or temporarily echo `$kbPages . $kbPageParts`) contains the per-row annotations under the appropriate sections — proving the v1-shaped adapter (`pageParts`/`nodeClasses`/`sections`) reaches `KnowledgeBase::renderPagesMarkdown` in a shape it understands."
    why_human: "AnalyzeControllerKbAdapterTest proves the adapter's bucket shape; KnowledgeBaseSmokeTest proves the render methods don't crash on a stub. Neither verifies the round-trip: real proposals → adapter → KnowledgeBase → markdown that actually carries the overlay. Only an end-to-end invocation can confirm the B2 fix landed (per SRC-09)."
---

# Phase 02.1: Kunstmaan Source Introspection Verification Report

**Phase Goal:** The analyze pipeline reads the Kunstmaan source codebase as the source of truth for content schema — Doctrine entity classes, YAML page-part config, AND vendor Kunstmaan bundles. The database is treated as opaque storage; the source code tells the migrator what each table means, which polymorphic class each row instantiates, which page-part classes are allowed in which page contexts, and how to reconstruct the page → context → matrix-of-page-parts hierarchy in Craft.

**Verified:** 2026-04-26T00:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

Truths derived from the 19 SRC requirement IDs (codified in REQUIREMENTS.md by Plan 09 from the 17 ROADMAP success criteria + W2-derived SRC-18/SRC-19) plus the ROADMAP success criteria.

| #   | Truth (SRC-NN)                                                                                                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **SRC-01** KunstmaanSourceScanner reads KUNSTMAAN_SOURCE_PATH; scans Entity/**/*.php (project) + vendor/kunstmaan/*/Entity/**/*.php (vendor); greenfield-fallback dropped per D-31.                          | ✓ VERIFIED | `src/source/KunstmaanSourceScanner.php` exists (432 LOC); injects `pathResolver` + `entityParser`; `scan()` resolves path + warms entityParser. `KunstmaanSourcePathResolver::resolve()` enforces realpath + `is_dir(/src/Entity)` validation. AnalyzeController gates on `kunstmaanSourcePathResolver->resolve() === null` → `ExitCode::CONFIG`.                                                                                |
| 2   | **SRC-02** Discovered tables: every #[ORM\Table], @ORM\Table, #[ORM\JoinTable]; vendor base-class tables resolved via extends chain.                                                                         | ✓ VERIFIED | `src/source/DoctrineEntityParser.php` (366 LOC) parses ORM\Table, ORM\Column, ORM\JoinColumn + four relation kinds; `parseUseStatements()` + `resolveTargetEntity()` intact. `KunstmaanSourceScanner::scan()` unions tableName + joinTableName from each entity's relations.                                                                                                                                                   |
| 3   | **SRC-03** SchemaDumper consumes the discovered table list; WARNs (or FAILs under --source-strict) on bidirectional drift (D-32).                                                                            | ✓ VERIFIED | `SchemaDumper::dump(MigrationFilters $filters, ?array $sourceTableList = null)` (line 59). When sourceTableList is non-null, replaces the legacy `LIKE 'kuma_%'` query path. `KunstmaanSourceScanner` builds drift findings (`dbHasButScanMissing`, `scanHasButDbMissing`); MappingAuditor renders `## Drift findings` section with both sub-headings; AnalyzeController consults `$this->sourceStrict` to elevate exit code. |
| 4   | **SRC-04** M2M relations resolved end-to-end: owning + inverse cross-reference; both target classes' tables discovered; join table marked.                                                                   | ✓ VERIFIED | `KunstmaanSourceScanner::collectM2mJoins()` + `m2mJoins` key in scan() output (`['table' => …, 'owning' => …, 'inverse' => …]`). DoctrineEntityParser parses ManyToMany kind; JoinTable name extracted to relation info.                                                                                                                                                                                                       |
| 5   | **SRC-05** KunstmaanPageStructureScanner reads Entity/Pages/*.php for getPagePartAdminConfigurations() + getPageTemplates() + config/kunstmaancms/pageparts/*.yml; AST-based (nikic/php-parser).             | ✓ VERIFIED | `src/source/KunstmaanPageStructureScanner.php` (616 LOC). `composer.json` declares `"nikic/php-parser": "^5.0"` in `require`. `extractMethodReturnArray()` uses `ParserFactory` + `NodeTraverser` + `NodeVisitorAbstract`. YAML reader: `Yaml::parseFile(...)` with default flags (no PARSE_OBJECT). No `include`/`require` of source files (T-02.1-04-01 mitigation).                                                          |
| 6   | **SRC-06** Output is a structured pageStructure.json per Page entity FQCN with tableName/contexts/templates/possibleChildTypes/discriminatorMap/sourcePath.                                                  | ✓ VERIFIED | `scanPageEntity()` returns the documented shape; `AnalyzeController` calls `mappingFile->writeAtomicJson($storageDir . '/pageStructure.json', $pageStructure)` at line 173.                                                                                                                                                                                                                                                    |
| 7   | **SRC-07** Single-table-inheritance discriminators (#[ORM\InheritanceType('SINGLE_TABLE')] + #[ORM\DiscriminatorColumn] + #[ORM\DiscriminatorMap]) extracted via AST.                                        | ✓ VERIFIED | `extractDiscriminatorMap()` (line 411) walks attrGroups → attrs[] looking for `ORM\DiscriminatorMap`, `Doctrine\ORM\Mapping\DiscriminatorMap`, or `\DiscriminatorMap` suffix.                                                                                                                                                                                                                                                  |
| 8   | **SRC-08** HeuristicProposer gains entity-aware heuristic 1.5 (D-44, between zero-fill and exact-name); confidence=high → status=accepted.                                                                   | ✓ VERIFIED | `src/analyze/HeuristicProposer.php` declares `public array $entityIndex = []` + `public array $acceptedRows = []`; the autoMatch() body has heuristic 1.5 documented in comments + rationale string `"heuristic 1.5: entity %s property %s → %s.%s"`; emits `'confidence' => 'high'` so the existing D-02 ladder routes to `accepted`.                                                                                          |
| 9   | **SRC-09** LlmClassifier prompts include parent-entity class signature + property docblock + page-part contexts; KB markdown replaces `'', ''` placeholders via v1-shaped adapter (B2 fix).                  | ✓ VERIFIED | `AnalyzeController::buildKbMappingAdapter()` (public static, line 526) returns `['pageParts' => [...], 'nodeClasses' => [], 'sections' => [...]]`. Lines 262-263: `renderPagesMarkdown($adapter, $now)` and `renderPagePartsMarkdown($adapter, $now)`. No `'', ''` placeholders anywhere in AnalyzeController. AnalyzeControllerKbAdapterTest (5 tests) asserts adapter shape end-to-end.                                       |
| 10  | **SRC-10** Doctor 5th check verifies path exists + has src/Entity/; FAIL when unset/invalid; WARN when pageparts/ absent.                                                                                    | ✓ VERIFIED | `DoctorController::checkKunstmaanSourcePath()` line 190: emits `FAIL KUNSTMAAN_SOURCE_PATH unset or invalid` (FG_RED), `WARN config/kunstmaancms/pageparts/ not found` (FG_YELLOW fallback), `OK   Kunstmaan source path` (FG_GREEN). Wired into actionIndex && chain at line 60.                                                                                                                                              |
| 11  | **SRC-11** DoctrineEntityParser ported verbatim with use-map FQCN resolver intact (Foo::class / \Full\Ns\Foo::class / docblock chain); regex-only (T-02.1-02-01 mitigation).                                 | ✓ VERIFIED | 366 LOC in v2 (matches v1's LOC); `parseUseStatements()` + `resolveTargetEntity()` ported verbatim; `getByFqcn()` / `getByTable()` / `getAll()` API preserved; namespace flattened to `lameco\kunstmaanmigrator\source`. No `include`/`require` of source files.                                                                                                                                                                |
| 12  | **SRC-12** DetailTableResolver ported with 4-tier resolution (operator override → DoctrineEntityParser → information_schema prefix → all-tables suffix); bound-parameter info_schema queries.                | ✓ VERIFIED | `src/source/DetailTableResolver.php` (225 LOC). `resolve()` walks 4 tiers; `legacyTablePrefix = 'lameco_websitebundle_'` default. SQL queries use `':db'` + `':prefix'` bound parameters (T-05.5-03-01 mitigation preserved).                                                                                                                                                                                                  |
| 13  | **SRC-13** KnowledgeBase Markdown generator ported (renderPagesMarkdown + renderPagePartsMarkdown + transitively-reachable helpers including loadAllColumns + CORE_TABLES + isSafeIdent).                    | ✓ VERIFIED | `src/source/KnowledgeBase.php` (660 LOC, exceeds 400 minimum). `renderPagesMarkdown` + `renderPagePartsMarkdown` present; transitive helpers `loadAllColumns`, `fetchSamples`, `formatSamples`, `truncateSample`, `isSafeIdent` all present. `private const CORE_TABLES` + `SAMPLE_MAX_LEN`/`MAX_SAMPLES`/`IDENT_RX` constants present. Excluded out-of-scope helpers (`buildPages`, `computeReachableSet`) absent (verified). `KnowledgeBaseSmokeTest.php` proves transitive walk completeness. |
| 14  | **SRC-14** BodyScanColumnFinder + MediaFkScanner + TopologicalOrderer ported as part of scanner output.                                                                                                      | ✓ VERIFIED | All 3 files exist with correct LOC, namespace flattened, regexes preserved verbatim: BodyScan `(content\|intro\|summary\|body\|description)/i`; MediaFk `(image\|icon\|logo\|banner\|video\|file\|media\|preview).*_?id$\|^image$\|^video$/i`; Topological with `PHP_INT_MAX` cycle/orphan sentinels + `array &$warnings` reshape replacing v1's MigrationReport.                                                              |
| 15  | **SRC-15** BlockAvailabilityValidator ported + wired as 4th MappingAuditor finding kind ('block-availability'); CoverageAuditor extended for page-part coverage (every page-part class needs accepted/dropped). | ✓ VERIFIED | `src/mapping/BlockAvailabilityValidator.php` (123 LOC, namespace `lameco\kunstmaanmigrator\mapping`); `validate()` signature preserved with `pagePartToNodeClasses` optional. MappingAuditor injects `?BlockAvailabilityValidator $blockAvailabilityValidator` + emits `'kind' => 'block-availability'` finding (line 196). CoverageAuditor::audit() takes `$pageStructure` 3rd arg + `auditPagePartCoverage()` private (line 117) emits `pagePartCoverage` violations. |
| 16  | **SRC-16** CoverageAuditor reconciliation against v1's bridge/schema/knowledgebase/CoverageAuditor.php (482 LOC); rule-by-rule disposition table in RECONCILIATION.md; accidentally-dropped rules patched.   | ✓ VERIFIED | RECONCILIATION.md exists (119 LOC), contains both reconciliation pair sections + summary. Patches landed: empty-table guard in CoverageAuditor (`Patched in Phase 02.1 / Plan 09` traceability comment present).                                                                                                                                                                                                                |
| 17  | **SRC-17** MappingValidator reconciliation against v1's bridge/mapping/MappingValidator.php (647 LOC); accidentally-dropped rules patched (drop-rationale-length finding kind).                              | ✓ VERIFIED | RECONCILIATION.md MappingValidator section + `drop-rationale` patch in MappingAuditor (2 occurrences of either trace-comment or kind value).                                                                                                                                                                                                                                                                                  |
| 18  | **SRC-18** mapping.yaml row-kind extension: `kind:` discriminator with backward-compat default `column`; pagePart rows key on STRUCTURAL-ONLY tuple `(pagePart, pagePartClass, parentPageClass, context)` — no targetEntryType in key (W1 fix). | ✓ VERIFIED | `MappingFile::buildPagePartRow()` (line 126) returns kind=pagePart with status='needs-review' (D-35 invariant). Merge-tuple constructor: `'pagePart\|' . pagePartClass . '\|' . parentPageClass . '\|' . context` (line 210), terminating BEFORE `targetEntryType`. `'column\|' . table . '\|' . column . '\|' . targetEntryType` (line 214). MappingFileTest covers `testPagePartRowDedupesOnStructuralTupleWhenTargetEntryTypeChanges` and 5 sibling tests. |
| 19  | **SRC-19** MapController two-mode walker: dispatches on row kind first; pagePart rows render via renderPagePartRowBlock + runRemapPickerForPagePart; column flow preserved verbatim; Phase 2 atomic + walk-order invariants preserved. | ✓ VERIFIED | MapController line 155 `if ($kind === 'pagePart')` branch in main loop; `renderPagePartRowBlock` (line 370) + `runRemapPickerForPagePart` (line 422); column branch keeps `renderRowBlock` (line 327) + `runRemapPicker` (line 544). Action menu includes `'r' => 'remap (block-type)'` for pagePart. |

**Score:** 19/19 truths verified (static)

### Required Artifacts

All artifacts exist with line counts at or above minimums declared in plan frontmatter.

| Artifact                                              | Expected                                              | Status     | Details                                                                                              |
| ----------------------------------------------------- | ----------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------- |
| `src/source/KunstmaanSourcePathResolver.php`          | Yii Component, ≥30 LOC, env fallback + realpath       | ✓ VERIFIED | 59 LOC, contains `is_dir($real . '/src/Entity')`, `final class ... extends Component`               |
| `src/source/DoctrineEntityParser.php`                 | ≥350 LOC, verbatim port, regex-only                   | ✓ VERIFIED | 366 LOC; getByFqcn/getByTable/getAll preserved; ORM\Table/Column/JoinColumn/relation regexes intact |
| `src/source/DetailTableResolver.php`                  | ≥200 LOC, 4-tier resolve, bound params                | ✓ VERIFIED | 225 LOC; ':db'/':prefix' bound parameters; KunstmaanCoreTables exclusion list                       |
| `src/source/DoctrineEntityInfo.php`                   | ≥20 LOC, immutable                                    | ✓ VERIFIED | 27 LOC; readonly properties                                                                          |
| `src/source/DoctrineColumnInfo.php`                   | ≥15 LOC, immutable                                    | ✓ VERIFIED | 22 LOC                                                                                                |
| `src/source/DoctrineRelationInfo.php`                 | ≥20 LOC, immutable                                    | ✓ VERIFIED | 29 LOC                                                                                                |
| `src/source/KunstmaanCoreTables.php`                  | 7 verbatim consts                                     | ✓ VERIFIED | NODES (plural), NODE_TRANSLATIONS, NODE_VERSIONS, PAGE_PART_REFS, MEDIA, SEO, REDIRECTS — all 7 present |
| `src/source/BodyScanColumnFinder.php`                 | ≥130 LOC, regex preserved                             | ✓ VERIFIED | 147 LOC; `(content\|intro\|summary\|body\|description)/i` literal preserved                          |
| `src/source/MediaFkScanner.php`                       | ≥130 LOC, regex preserved                             | ✓ VERIFIED | 150 LOC; `(image\|icon\|logo\|banner\|video\|file\|media\|preview).*_?id$\|^image$\|^video$/i` literal preserved |
| `src/source/TopologicalOrderer.php`                   | ≥120 LOC, cycle/orphan sentinels                      | ✓ VERIFIED | 134 LOC; `PHP_INT_MAX` + `kuma_parent_id` + `MAX_DEPTH_GUARD`; `array &$warnings` reshape           |
| `src/source/KnowledgeBase.php`                        | ≥400 LOC, render methods + transitive helpers         | ✓ VERIFIED | 660 LOC; render methods + loadAllColumns + CORE_TABLES + helpers all present                          |
| `src/source/KunstmaanPageStructureScanner.php`        | ≥150 LOC, AST-based, no include/require               | ✓ VERIFIED | 616 LOC; nikic/php-parser AST; YAML default-flags; `tableResolver->resolve` integration            |
| `src/source/KunstmaanSourceScanner.php`               | ≥100 LOC, orchestrator + drift                        | ✓ VERIFIED | 432 LOC; injects all five constituents + LegacyDbService; m2mJoins + drift                          |
| `src/mapping/BlockAvailabilityValidator.php`          | ≥110 LOC, validate() signature preserved              | ✓ VERIFIED | 123 LOC; namespace flattened; pagePartToNodeClasses optional param preserved                          |
| `src/mapping/MappingFile.php` (extension)             | buildPagePartRow + structural-tuple merge             | ✓ VERIFIED | 281 LOC (was ~196); `buildPagePartRow` + `'kind' => 'pagePart'/'column'`; structural-only pagePart key |
| `src/mapping/MappingAuditor.php` (extension)          | block-availability finding + drift section            | ✓ VERIFIED | 290 LOC (was ~172); 4 finding kinds; `## Drift findings` + DB/scan sub-headings                      |
| `src/mapping/CoverageAuditor.php` (extension)         | page-part coverage + empty-table guard patch          | ✓ VERIFIED | 222 LOC (was ~102); `auditPagePartCoverage` + `pagePartCoverage` violations + `Patched in Phase 02.1 / Plan 09` traceability comment |
| `src/console/AnalyzeController.php` (orchestration)   | 11-step + step 4.5 page-part emitter + KB adapter     | ✓ VERIFIED | 551 LOC (was ~331); `kunstmaanSourceScanner->scan`, `kunstmaanPageStructureScanner->scan`, `writeAtomicJson…pageStructure`, `buildPagePartRow`, `renderPagesMarkdown($adapter)`, `renderPagePartsMarkdown($adapter)`, `buildKbMappingAdapter` static helper |
| `src/console/MapController.php` (two-mode walker)     | pagePart branch + new render/picker                   | ✓ VERIFIED | 676 LOC (was ~390); `renderPagePartRowBlock`, `runRemapPickerForPagePart`, `kind === 'pagePart'` branch dispatch |
| `src/console/DoctorController.php` (5th check)        | checkKunstmaanSourcePath wired into && chain          | ✓ VERIFIED | 215 LOC (was ~179); FAIL/WARN/OK strings + && chain at line 60                                       |
| `src/analyze/SchemaDumper.php` (signature)            | ?array $sourceTableList = null                        | ✓ VERIFIED | 212 LOC (was ~201); signature extended; null fallback path retained                                  |
| `src/analyze/HeuristicProposer.php` (heuristic 1.5)   | entityIndex + acceptedRows + heuristic body           | ✓ VERIFIED | 418 LOC (was ~407); public arrays + heuristic 1.5 body emitting confidence=high                      |
| `src/Plugin.php` (component registration)             | All Phase 02.1 components registered                  | ✓ VERIFIED | 143 LOC; 19 components total (10 prior + 9 new); `'legacyDbService' => LegacyDbService::class,` byte-preserved (D-21 invariant) |
| `src/models/Settings.php` (kunstmaanSourcePath)       | property + env fallback + rules entry                 | ✓ VERIFIED | 114 LOC; ??= App::env('KUNSTMAAN_SOURCE_PATH') ?: null; rules entry present; EnvAttributeParserBehavior::attributes entry present |
| `composer.json` (nikic/php-parser)                    | direct require, ^5.0                                  | ✓ VERIFIED | `"nikic/php-parser": "^5.0"` declared in `require`                                                  |
| `tests/console/AnalyzeControllerKbAdapterTest.php`    | ≥30 LOC, B2 adapter shape assertions                  | ✓ VERIFIED | 139 LOC; 5 tests covering pageParts/sections buckets + empty input + nodeClasses-empty contract     |
| `tests/source/KnowledgeBaseSmokeTest.php`             | ≥40 LOC, transitive-walk verification                 | ✓ VERIFIED | ~110 LOC; both render methods exercised                                                              |
| `tests/mapping/MappingFileTest.php`                   | W1 structural-tuple test + sibling page-part tests    | ✓ VERIFIED | Includes `testPagePartRowDedupesOnStructuralTupleWhenTargetEntryTypeChanges` + 5 sibling tests       |
| `.planning/phases/02.1-source-introspection/RECONCILIATION.md` | ≥100 LOC, both pair sections + summary       | ✓ VERIFIED | 119 LOC; both reconciliation sections + dispositions documented                                      |
| `.planning/REQUIREMENTS.md`                           | SRC-01..SRC-19 codified + traceability                | ✓ VERIFIED | All 19 SRC-** entries present; `### Source introspection (SRC)` section; `\| SRC-01 … SRC-19 \| 02.1 \|` traceability row |
| `.planning/ROADMAP.md`                                | D-31 patches applied; 9 plans listed                  | ✓ VERIFIED | "Degrades gracefully when path unset" / "falls back to LIKE 'kuma_%'" both absent (0 occurrences); "KUNSTMAAN_SOURCE_PATH is required" present; "9 plans" present |

### Key Link Verification

| From                                   | To                                                | Via                                                                              | Status   | Details                                                                  |
| -------------------------------------- | ------------------------------------------------- | -------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------ |
| DoctorController::actionIndex          | KunstmaanSourcePathResolver::resolve              | `Plugin::getInstance()->kunstmaanSourcePathResolver->resolve()`                   | ✓ WIRED  | DoctorController line 60 wires the && chain; `checkKunstmaanSourcePath` calls resolver |
| Settings::init                         | App::env('KUNSTMAAN_SOURCE_PATH')                 | `??= App::env(...) ?: null` idiom                                                 | ✓ WIRED  | Settings has 6 occurrences of kunstmaanSourcePath/KUNSTMAAN_SOURCE_PATH  |
| DetailTableResolver                    | DoctrineEntityParser                              | `public ?DoctrineEntityParser $entityParser` injection                            | ✓ WIRED  | Plugin::config() registers both; KunstmaanSourceScanner injects both     |
| DetailTableResolver                    | LegacyDbService                                   | `public ?LegacyDbService $legacyDb` injection                                     | ✓ WIRED  | DetailTableResolver uses bound-param info_schema queries via legacyDb    |
| BodyScanColumnFinder + MediaFkScanner  | LegacyDbService                                   | `public ?LegacyDbService $legacyDb` injection                                     | ✓ WIRED  | Both scanners declare the field; bound-param SQL preserved               |
| KnowledgeBase                          | DoctrineEntityParser + LegacyDbService            | `public ?DoctrineEntityParser $entityParser` + `public ?LegacyDbService $legacyDb` | ✓ WIRED  | Render methods + loadAllColumns invoke both                              |
| KunstmaanPageStructureScanner          | KunstmaanSourcePathResolver + DetailTableResolver | `public ?KunstmaanSourcePathResolver $pathResolver` + `public ?DetailTableResolver $tableResolver` | ✓ WIRED | scan() resolves path; scanPageEntity calls tableResolver->resolve        |
| KunstmaanSourceScanner                 | DoctrineEntityParser/DetailTableResolver/BodyScan/MediaFk/LegacyDb | All 5 + pathResolver injected as Yii Component fields                            | ✓ WIRED  | scan() composes all; m2mJoins + drift findings emitted                    |
| Plugin::config                         | KunstmaanSourceScanner (and 8 others)             | components map registration                                                       | ✓ WIRED  | All 19 components registered; PluginBootstrapTest D-21 invariant intact   |
| AnalyzeController                      | KunstmaanSourceScanner                            | `Plugin::getInstance()->kunstmaanSourceScanner->scan()` at step 3                  | ✓ WIRED  | Line 115; result fed to schemaDumper at step 6 + adapter at step 8       |
| AnalyzeController                      | KunstmaanPageStructureScanner                     | `Plugin::getInstance()->kunstmaanPageStructureScanner->scan()` at step 4            | ✓ WIRED  | Line 129; result drives step 4.5 emitter + writeAtomicJson(pageStructure.json) |
| AnalyzeController                      | MappingFile::buildPagePartRow (step 4.5)          | `$plugin->mappingFile->buildPagePartRow(...)` for each (pagePartClass × parent × context) | ✓ WIRED  | Line 154; emitted with empty targetEntryType (operator fills via map)     |
| AnalyzeController                      | KnowledgeBase via v1-shaped adapter (B2 fix)      | `buildKbMappingAdapter($preLlmProposals)` → `renderPagesMarkdown($adapter, $now)` + `renderPagePartsMarkdown($adapter, $now)` | ✓ WIRED | Lines 260-263; `buildKbMappingAdapter` public static at line 526; no `'', ''` placeholders remain |
| MapController                          | MappingFile (kind dispatch)                       | `$kind = (string) ($row['kind'] ?? 'column')` branch in main loop                   | ✓ WIRED  | Line 155 column-vs-pagePart dispatch                                      |
| MappingAuditor                         | BlockAvailabilityValidator                        | `public ?BlockAvailabilityValidator $blockAvailabilityValidator` injection         | ✓ WIRED  | Line 34 declared; line 196 emits 'kind' => 'block-availability'         |
| AnalyzeController                      | MappingAuditor (drift findings)                   | `mappingAuditor->renderMarkdown($findings, $driftFindings)` with `$sourceScan['drift']` | ✓ WIRED | Lines 345-347; sourceStrict consulted at line 373                        |
| CoverageAuditor                        | pageStructure (page-part coverage)                | `audit($schemaDump, $mappingProposals, $pageStructure = [])` 3rd arg              | ✓ WIRED  | Line 50; auditPagePartCoverage() at line 117                              |

### Data-Flow Trace (Level 4)

| Artifact                                | Data Variable                              | Source                                                              | Produces Real Data | Status        |
| --------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------- | ------------------ | ------------- |
| AnalyzeController step 3                | `$sourceScan['tables']`                    | KunstmaanSourceScanner::scan() composes Doctrine + JoinTable lists  | Yes (when KUNSTMAAN_SOURCE_PATH valid) | ✓ FLOWING     |
| AnalyzeController step 4                | `$pageStructure`                           | KunstmaanPageStructureScanner::scan() AST + YAML reads              | Yes (FQCN-keyed map of page records)   | ✓ FLOWING     |
| AnalyzeController step 4.5              | `$pagePartProposals[]`                     | Iteration over pageStructure → buildPagePartRow                     | Yes (per allowed page-part class)      | ✓ FLOWING     |
| AnalyzeController step 5                | `pageStructure.json`                       | writeAtomicJson over $pageStructure                                 | Yes (atomic JSON write)                | ✓ FLOWING     |
| AnalyzeController step 6                | `schema-dump.json`                         | SchemaDumper::dump($filters, $sourceScan['tables'])                 | Yes (driven by source table list)      | ✓ FLOWING     |
| AnalyzeController step 8                | `$kbPages . "\n\n" . $kbPageParts`         | KnowledgeBase render methods over v1-shaped adapter                 | Yes (adapter buckets verified by test) | ✓ FLOWING     |
| MappingAuditor::renderMarkdown          | `## Drift findings` markdown               | `$driftFindings` arg ← `$sourceScan['drift']` from AnalyzeController | Yes (round-trip)                       | ✓ FLOWING     |
| CoverageAuditor::audit                  | page-part coverage violations              | $pageStructure → allPagePartClasses → set-difference over mapping   | Yes (additive to column-coverage)      | ✓ FLOWING     |

Note: For 5/8 of the above flows the produces-real-data check is contingent on the runtime environment (KUNSTMAAN_SOURCE_PATH set, legacy DB reachable, fixture corpus present). Static wiring is correct in every case; the runtime outcome is exercised by Human Verification item #1.

### Behavioral Spot-Checks

| Behavior                                                              | Command                                                                                       | Result                              | Status |
| --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ----------------------------------- | ------ |
| Test suite passes                                                     | `composer test`                                                                               | `Tests: 51, Assertions: 126`        | ✓ PASS |
| All Phase 02.1 PHP files have no syntax errors                       | `php -l` on each new file                                                                     | "No syntax errors" for all          | ✓ PASS |
| MappingFile structural-tuple test passes                             | `composer test --filter testPagePartRowDedupesOnStructuralTupleWhenTargetEntryTypeChanges`  | passes (verified in full suite run) | ✓ PASS |
| KbAdapterTest covers buckets                                          | `composer test --filter AnalyzeControllerKbAdapterTest`                                      | passes (5 tests)                    | ✓ PASS |
| KnowledgeBaseSmokeTest proves transitive walk                        | `composer test --filter KnowledgeBaseSmokeTest`                                              | passes                              | ✓ PASS |

Note: Behavioral spot-checks against the running CLI (analyze / doctor / map) require Craft + legacy DB + KUNSTMAAN_SOURCE_PATH and are routed to Human Verification.

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                              | Status      | Evidence                                                                                |
| ----------- | ----------- | -------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------- |
| SRC-01      | Plan 05     | KunstmaanSourceScanner reads KUNSTMAAN_SOURCE_PATH; project + vendor scan                                | ✓ SATISFIED | Truth #1                                                                                |
| SRC-02      | Plan 05     | Doctrine table + JoinTable + extends-chain discovery                                                      | ✓ SATISFIED | Truth #2                                                                                |
| SRC-03      | Plan 05     | SchemaDumper consumes scan tables; bidirectional drift                                                    | ✓ SATISFIED | Truth #3 + drift wiring grep evidence                                                    |
| SRC-04      | Plan 05     | M2M end-to-end resolution                                                                                 | ✓ SATISFIED | Truth #4                                                                                |
| SRC-05      | Plan 04     | KunstmaanPageStructureScanner AST-based with YAML reads                                                   | ✓ SATISFIED | Truth #5                                                                                |
| SRC-06      | Plan 04     | pageStructure.json structured per Page-FQCN                                                               | ✓ SATISFIED | Truth #6                                                                                |
| SRC-07      | Plan 04     | Single-table-inheritance discriminators extracted                                                         | ✓ SATISFIED | Truth #7                                                                                |
| SRC-08      | Plan 07     | HeuristicProposer entity-aware heuristic 1.5                                                              | ✓ SATISFIED | Truth #8                                                                                |
| SRC-09      | Plan 07     | LlmClassifier KB markdown via v1-shaped adapter (B2 fix)                                                  | ✓ SATISFIED | Truth #9 + AnalyzeControllerKbAdapterTest                                                |
| SRC-10      | Plan 01     | Doctor 5th check                                                                                          | ✓ SATISFIED | Truth #10 (runtime exercise covered by Human Verification #2)                            |
| SRC-11      | Plan 02     | DoctrineEntityParser ported verbatim                                                                      | ✓ SATISFIED | Truth #11                                                                                |
| SRC-12      | Plan 02     | DetailTableResolver 4-tier ladder + bound-param SQL                                                       | ✓ SATISFIED | Truth #12                                                                                |
| SRC-13      | Plan 03     | KnowledgeBase Markdown generator port (with transitive helpers)                                           | ✓ SATISFIED | Truth #13 + KnowledgeBaseSmokeTest                                                       |
| SRC-14      | Plan 03     | BodyScan + MediaFk + Topological port                                                                     | ✓ SATISFIED | Truth #14                                                                                |
| SRC-15      | Plan 08     | BlockAvailabilityValidator + page-part coverage gate                                                      | ✓ SATISFIED | Truth #15                                                                                |
| SRC-16      | Plan 09     | CoverageAuditor reconciliation; accidentally-dropped rules patched                                        | ✓ SATISFIED | Truth #16 + RECONCILIATION.md                                                            |
| SRC-17      | Plan 09     | MappingValidator reconciliation; accidentally-dropped rules patched                                       | ✓ SATISFIED | Truth #17 + RECONCILIATION.md                                                            |
| SRC-18      | Plan 06     | mapping.yaml row-kind extension with structural-only pagePart tuple (W1 fix)                              | ✓ SATISFIED | Truth #18 + 6 MappingFileTest tests                                                      |
| SRC-19      | Plan 06     | MapController two-mode walker                                                                             | ✓ SATISFIED | Truth #19                                                                                |

All 19 SRC IDs (SRC-01..SRC-19) declared in plans 01-09 — every ID has exactly one owning plan (W2 ownership audit passes per Plan 09's traceability section). No orphaned requirements (REQUIREMENTS.md only maps SRC-* to phase 02.1 — fully covered).

### Anti-Patterns Found

| File                              | Line     | Pattern                                                          | Severity   | Impact                                                                                                                                                                                |
| --------------------------------- | -------- | ---------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/console/MapController.php`   | 662-675  | `matchesEntitiesFilter()` reads `$row['table']` only             | ⚠️ Warning | REVIEW WR-01 — page-part rows carry `parentPageClass`/`sourceTable` not `table`, so `--entities=NewsPage` silently drops every kind=pagePart row. Affects map UX (SRC-19 surface) but not analyze (the phase goal). User flagged as "real but advisory"; not blocking the goal. |
| `src/source/KunstmaanCoreTables.php` | 1-3   | Missing `declare(strict_types=1);`                               | ℹ️ Info    | REVIEW IN-01 — every other src/source/ file declares strict types; consistency nit, no behavioral impact (file only declares consts).                                                  |
| `src/source/KnowledgeBase.php`    | various  | Backticked identifier interpolation in SQL helpers               | ℹ️ Info    | REVIEW IN-02 — defense-in-depth concern; verbatim port from v1; T-2-21 mitigation acceptable per dev/staging-only deployment + NeverProductionTrait.                                  |
| `src/Plugin.php`                  | 80-88    | `=>` alignment differs between Phase 1/2 and Phase 02.1 entries | ℹ️ Info    | REVIEW IN-03 — cosmetic; PluginBootstrapTest invariant preserved (legacyDbService line untouched).                                                                                    |
| `src/source/KunstmaanPageStructureScanner.php` | 197-215 / 131-148 | InvalidArgumentException silently sets tableName=''  | ℹ️ Info    | REVIEW IN-04 — observability nit; downstream pageStructure.json consumers see empty tableName for unresolvable Page entities.                                                          |

No blockers. No stubs. No dead code. No empty handlers. No silent placeholders left in AnalyzeController (`'', ''` count = 0).

Vendor-side deprecation in `vendor/craftcms/cms/src/console/Controller.php:229` (PHP 8.5 implicit-nullable parameter) flagged in `deferred-items.md` — out of scope for this phase per project policy of not patching vendor code.

### Human Verification Required

See frontmatter `human_verification:` block. Three items:

1. **Run analyze against the CQM rehearsal pair with KUNSTMAAN_SOURCE_PATH set** — required to satisfy ROADMAP success criterion #10 (the meaningful-coverage outcome). Static wiring is verified; the heuristic-coverage jump is the empirical outcome the phase exists to deliver.

2. **Doctor 5th check end-to-end against three states (unset / invalid / valid path)** — confirms FAIL/WARN/OK exit codes + console color output that grep alone can't verify.

3. **Verify B2 KB adapter actually surfaces per-row annotations in the rendered KB markdown end-to-end** — adapter unit test proves the bucket shape; smoke test proves the render methods don't crash; only an end-to-end run with real proposals proves the adapter→KnowledgeBase contract carries the per-row annotations through to the markdown overlay sent to LlmClassifier.

### Gaps Summary

No structural gaps blocking the phase goal. All 19 SRC requirements verified statically. All artifacts exist with required line counts; all key links are wired; all data flows are tracable end-to-end on the static surface. Test suite (51 tests / 126 assertions) is green.

The single review-flagged Warning (WR-01: MapController `--entities` filter silently drops pagePart rows) is real but tangential to the phase goal — it affects map-loop UX after analyze has emitted page-part proposals, not the analyze pipeline itself. The user explicitly classified it as "real but advisory". Captured under Anti-Patterns and the matchesEntitiesFilter location is documented for follow-up; not promoted to a closure-cycle gap.

The phase status is `human_needed` — not `gaps_found` — because the only outstanding work is operator-side runtime verification of three specific outcomes that cannot be exercised from `composer test`. Static verification is complete and passes.

---

_Verified: 2026-04-26T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
