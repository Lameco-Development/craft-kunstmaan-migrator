# Requirements

v1 scope for `lameco/craft-kunstmaan-migrator` (revisited). All requirements are
hypotheses until shipped and validated by the rehearsal pass at the end of v1.

## v1 Requirements

### Foundation (FND)

- [x] **FND-01**: Plugin scaffolds as a Craft 5 plugin (`composer.json` type `craft-plugin`, PSR-4 namespace `lameco\kunstmaanmigrator\` under `src/`, PHP 8.3+) and installs cleanly via `./craft plugin/install kunstmaan-migrator`. _(Phase 1 / Plan 01 — composer manifest + Plugin stub + NeverProductionTrait shipped; `composer install` succeeds, autoload contract verified.)_
- [x] **FND-02**: Install creates a state table `kunstmaanmigrator_state` (schema kept compatible with v1.x: `id`, `source`, `sourceKey`, `targetType`, `targetId`, `targetUid`, `siteId`, `meta`, `dateCreated`, `dateUpdated`, with a UNIQUE index on `(source, sourceKey, siteId)` and an INDEX on `(dateUpdated)`) and attaches a `kunstmaanSourceId` Plain Text field. If a `kunstmaanSourceId` field UID already exists from the v1.x plugin, the install reuses it. Plain Text type is preserved (over a narrower Number field) so the v2 install can swap-in on a host already migrated under v1.x without altering the field type. _(Phase 1 / Plan 03 — Install.php ships the byte-for-byte v1.x schema per D-06 (10 cols: id/source/sourceKey/targetType/targetId/targetUid/siteId/meta/dateCreated/dateUpdated; UNIQUE(source,sourceKey,siteId) + INDEX(dateUpdated)), guarded by tableExists; kunstmaanSourceId attached via D-09 three-step UID-reuse chain (project-config → getFieldByHandle → mint); PlainText with charLimit=255 preserved.)_
- [x] **FND-02a**: Programmatic install command (`kunstmaan-migrator/migrate/install`) runs the plugin's DB migrations on demand. Needed because Craft 5 dropped `--migrationPath` and additional migrations beyond `Install.php` (anticipated for future schema additions) have no first-class CLI route otherwise. v1.x ships this; we keep parity. _(Phase 1 / Plan 03 — MigrateController::actionInstall wires MigrationManager on track 'kunstmaanmigrator' with namespace lameco\\kunstmaanmigrator\\migrations and path src/migrations/, gated first by enforceNeverProduction() per D-20.)_
- [x] **FND-03**: Uninstall is a deliberate no-op on the state table and `kunstmaanSourceId` field — operator must remove manually for a full wipe. _(Phase 1 / Plan 03 — Install::safeDown() returns true unconditionally; no dropTable / removeField / delete anywhere in the method body. Verified by acceptance grep.)_
- [x] **FND-04**: `NeverProductionTrait` hard-blocks every legacy-reading or destructive command when `CRAFT_ENVIRONMENT=production`. _(Phase 1 / Plan 04 — DoctorController::actionIndex first-statement-gates on enforceNeverProduction() per D-20; combined with Plan 03's MigrateController::actionInstall guard, every Phase 1 controller action is now gated. Verified by acceptance-criteria ordering grep against the first 8 lines of actionIndex.)_
- [x] **FND-05**: PHPUnit 11 test suite scaffolded under `tests/`, wired to a `composer test` script and a CI workflow. Suite is non-empty on day one (smoke test on plugin bootstrap). _(Phase 1 / Plan 05 — phpunit.xml.dist + tests/bootstrap.php + tests/PluginBootstrapTest.php (3 assertions: Plugin loads, Settings + LegacyDbService load, Plugin::config() declares legacyDbService component) per D-21. composer test exits 0 with OK (3 tests, 3 assertions). .github/workflows/ci.yml runs validate + install + test on PHP 8.3 / ubuntu-latest on every push and pull_request per D-22 — single job, no Deptrac, no FQCN-lint, no matrix expansion.)_

### Connectivity (CONN)

- [x] **CONN-01**: Plugin owns the legacy DB connection internally — no Yii component required in the consuming site's `config/app.php`. Connection params come from env vars (`CRAFT_LEGACY_DB_*`) and/or plugin settings. _(Phase 1 / Plan 02 — Plugin::init() registers legacyDb Yii application component conditionally via !Craft::\$app->has('legacyDb', true) guard; swap-in hosts retain their config/app.php declaration, greenfield hosts get the plugin's env-driven Connection from Settings.)_
- [x] **CONN-02**: Anthropic API key sourced from `ANTHROPIC_API_KEY` env var or plugin settings (settings override env if both present, never logged). _(Phase 1 / Plan 02 — Settings::\$anthropicApiKey resolves from ANTHROPIC_API_KEY env via App::env() with `??=` so Settings property override wins; never echoed/logged from Settings or Twig template.)_
- [x] **CONN-03**: `kunstmaan-migrator/doctor` command reports OK/FAIL on: legacy DB reachability, Anthropic key presence, write permissions on `storage/migration/`. The mapping-file validity check ships in Phase 2 alongside the mapping loader (deferred per D-17 — no point shipping a hollow stub before the loader exists). No queue-worker check — v1's check was carried by v1's queue-heavy pipeline; v2 is CLI-inline by default. _(Phase 1 / Plans 04 + 05 — Plan 04 ships the 3 checks (legacyDb SELECT 1, anthropicApiKey presence-only-via-Settings-or-env, storage/migration writability with D-18 auto-create at 0755). Plan 05 patched this requirement's wording to acknowledge the mapping-file check ships in Phase 2 (3-of-3 in scope, not 3-of-4). Plain-text OK/FAIL output with ANSI colors; exit 0 on full pass / 1 on any FAIL.)_

### Schema + Mapping (MAP)

- [x] **MAP-01**: `kunstmaan-migrator/analyze` reads the legacy DB and emits a schema dump under `storage/migration/schema-dump.json` plus a human-readable `REPORT.md`. _(Phase 2 / Plan 03 — AnalyzeController::actionIndex orchestrates SchemaDumper::dump → MappingFile::writeAtomicJson(schema-dump.json) → HeuristicProposer + LlmClassifier → MappingFile::merge(mapping.yaml) → ReportBuilder::render → MappingFile::writeAtomic(REPORT.md). All three artifacts written atomically per Phase 2 / Plan 02 D-07.)_
- [x] **MAP-02**: Analyze runs the heuristic-first proposal pipeline: 9 deterministic heuristics (exact name, `*_id`→asset, `*_image`/`*_afbeelding`→asset, TEXT→ckeditor, `*_date`/`*_datum`→date, `*_url`/`*_link`→url, `*_email`→email, Dutch semantic alias map, zero-fill-rate auto-drop) before any LLM call. _(Phase 2 / Plan 03 — HeuristicProposer ported byte-for-byte from v1; 9-heuristic ordering and DUTCH_ALIASES const verbatim. AnalyzeController calls heuristicProposer->autoMatch first; only its `residual` output is fed to LlmClassifier.)_
- [x] **MAP-03**: Residual columns are sent to Anthropic in entry-type-grouped batches with `sqlType` and sample values in the prompt, returning typed proposals with confidence tiers. _(Phase 2 / Plan 03 — LlmClassifier ported byte-for-byte from v1; per-targetEntryType grouping, chunk=10, sleep(20) between batches; prompt includes `sqlType=...` per residual line and `samples=[...]` (truncated to 40 chars × max 3). Confidence read from LLM response and validated against {high,medium,low}.)_
- [x] **MAP-04**: All proposals (heuristic and LLM) land in a single `mapping.yaml` with a per-row `status:` field — `proposed`, `accepted`, `dropped`, `needs-review`. Existing rows are never overwritten without an explicit operator action. _(Phase 2 / Plans 02 + 03 — Plan 02 shipped MappingFile with the per-row status build + skip-existing merge keyed on (table, column, targetEntryType). Plan 03 wires the analyze orchestration: AnalyzeController applies D-02 confidence-tier → status mapping via statusForHeuristic + statusForLlm, then calls MappingFile::merge with existing-first semantics.)_
- [x] **MAP-05**: `kunstmaan-migrator/map` runs an interactive rubber-stamp loop walking every `proposed` / `needs-review` row: `[a]ccept / [d]rop / [r]emap / [s]kip / [q]uit`. `--auto-accept-high` non-interactively promotes high-confidence proposals. _(Phase 2 / Plan 03 partial — AnalyzeController implements `--auto-accept-high` post-pass that promotes proposed-high → accepted before the merge. The interactive `map` rubber-stamp loop ships in Phase 2 / Plan 04 — MapController.)_
- [x] **MAP-06**: A coverage gate refuses to run `migrate --live` while any data-bearing legacy column has no mapping decision. `migrate` (dry-run) prints coverage warnings but does not block. _(Phase 2 / Plan 05 — CoverageAuditor at src/mapping/CoverageAuditor.php ships the verdict producer per D-15: STRUCTURAL_IGNORE constant filters out bookkeeping columns; data-bearing = fillRate>0 AND not in STRUCTURAL_IGNORE; covered = mapping.yaml row with status ∈ {accepted, dropped}. Returns list of unmapped data-bearing columns. The hard-fail-vs-warn consumer wiring (Phase 3 migrate --live) is intentionally deferred — verdict producer ships here so Phase 3 just consumes it.)_
- [x] **MAP-07**: A mapping-audit checks every `(entryType, fieldHandle)` pair declared in `mapping.yaml` against the live Craft FieldLayout and emits drift findings. _(Phase 2 / Plan 05 — MappingAuditor at src/mapping/MappingAuditor.php walks every accepted/proposed (targetEntryType, targetHandle) row via Craft::\$app->entries->getEntryTypeByHandle + getFieldLayout->getCustomFields, returning structured findings (missing-entry-type / missing-handle / handler-classification-mismatch). EXCLUDED_HANDLES + CANONICAL_HANDLERS + HANDLER_ALIASES ported from v1 MappingValidator (lines 56-70 + 1794-1802). AnalyzeController calls audit after the merge step (Step 7), writes MAPPING-AUDIT.md atomically, WARN-by-default per D-16; --audit-strict elevates non-empty findings to ExitCode::UNSPECIFIED_ERROR.)_

### Filtering (FILT)

- [x] **FILT-01**: A `MigrationFilters` value object captures: included entity types (allow-list), locale subset, `--since=YYYY-MM-DD` floor. _(D-12 / Phase 2: per-entity row cap dropped from v1.0 scope per operator decision; rehearsal scoping is covered by `--entities` + `--since`.)_ _(Phase 2 / Plan 01 — MigrationFilters VO ships at src/filter/MigrationFilters.php with three readonly properties (entities, locales, since); Phase 2 / Plan 06 patched this wording per D-12.)_
- [x] **FILT-02**: Filters apply uniformly through every stage (extract / transform / load / verify) — a row excluded at extract is also absent from verify counts. _(Phase 2 / Plan 01 — VO is constructor-only / readonly, so it cannot be mutated mid-pipeline; FilterFactory builds it once per CLI invocation. Stage consumers land in Phase 3+ but the cross-stage primitive is in place.)_
- [x] **FILT-03**: All five top-level CLI commands accept the filter flags. `analyze` uses them to scope schema scanning. _(Phase 2 / Plan 01 — FilterFactory at src/filter/FilterFactory.php registered via Plugin::config() so every controller can resolve `Plugin::getInstance()->filterFactory->fromCli(...)`. Per-controller flag wiring lands in Plans 03 (analyze), 04 (map), and Phase 3 (migrate/verify); doctor accepts but ignores the flags per D-13.)_

### Locale handling (LOC)

- [x] **LOC-01**: `analyze` auto-detects Kunstmaan locales from `kuma_node_translations` and emits a paste-ready `sites:` YAML block when locales aren't yet mapped. _(Phase 2 / Plans 01 + 03 — Plan 01 shipped LocalePreflight::detect() (`SELECT DISTINCT lang FROM kuma_node_translations`). Plan 03 shipped ReportBuilder::renderLocales which emits a `## Locales` markdown section listing detected vs mapped (Craft sites + Settings::defaultLocales) and a fenced `config/sites.php` paste-ready block when any locale is unmapped. AnalyzeController writes the report via MappingFile::writeAtomic to storage/migration/REPORT.md.)_
- [x] **LOC-02**: A preflight gate FAILs hard on any unmapped locale (no silent default-locale fallthrough). _(Phase 2 / Plan 01 — LocalePreflight::ensure(MigrationFilters): ?array returns null on pass or list of unmapped locale codes on fail. NO silent fallthrough. Caller (AnalyzeController/MapController/future MigrateController + VerifyController) is responsible for hard-fail on non-null return; controller wiring lands in Plans 03/04/Phase 3.)_
- [x] **LOC-03**: `LocalePreflight` consumes `KunstmaanEnvReader::$defaultLocale` as a "Rung 0" authoritative primary-locale signal. Validates that the first handle in `Settings::localeMap` aligns with `defaultLocale`; mismatch → doctor WARN (advisory; some projects intentionally diverge — e.g. NL-default Kunstmaan migrating into an EN-primary Craft instance). No auto-correction, no hard-fail. Closes the gap that today's "primary site" determination depends only on operator-curated handle ordering with no source-of-truth alignment check. Phase 4.1 driver. _(Phase 4.1 / Plans 04.1-01 + 04.1-03 — `KunstmaanEnvReader::getDefaultLocale()` reads `DEFAULT_LOCALE` from `.env`; `DoctorController::checkLocalePreflightRung0()` consults it and WARNs on mismatch with `localeMap[0]`; `LocalePreflight::resolve()` 3-rung ladder unchanged per D-11.)_

### Source introspection (SRC)

- [x] **SRC-01**: `KunstmaanSourceScanner` reads `KUNSTMAAN_SOURCE_PATH` (env or `Settings::kunstmaanSourcePath` via the `??=` ladder per Phase 1 / D-12); scans `Entity/**/*.php` (project) and `vendor/kunstmaan/*/Entity/**/*.php` (vendor). Greenfield-fallback dropped per D-31 — KUNSTMAAN_SOURCE_PATH is required. _(Phase 02.1 / Plan 05.)_
- [x] **SRC-02**: Discovered tables: every `#[ORM\Table(name: '…')]` (and `@ORM\Table` legacy docblock syntax); every `#[ORM\JoinTable(name: '…')]` for M2M; vendor base-class tables (`AbstractArticlePage`, `AbstractEntityAdminPart`, etc.) resolved via `extends` chain. _(Phase 02.1 / Plan 05.)_
- [x] **SRC-03**: `SchemaDumper` consumes the discovered table list when source path is set. WARNs (or FAILs under `--source-strict`) when DB has tables not present in scanner output, OR when scanner declares tables missing from DB (D-32 bidirectional drift detection). _(Phase 02.1 / Plan 05.)_
- [x] **SRC-04**: M2M relations resolved end-to-end: owning side (with explicit `JoinTable`) and inverse side (with `mappedBy`) cross-reference; both target classes' tables are discovered; the join table is captured in `KunstmaanSourceScanner::scan()['m2mJoins']`. _(Phase 02.1 / Plan 05.)_
- [x] **SRC-05**: `KunstmaanPageStructureScanner` (separate from the schema scanner per D-40) reads each `Entity/Pages/*.php` for `getPagePartAdminConfigurations()` and `getPageTemplates()` returns; reads `config/kunstmaancms/pageparts/*.yml` for context → allowed-page-part-classes mappings. AST-based (nikic/php-parser) — never `include`/`require`/`eval` (T-02.1-04-01 mitigation). _(Phase 02.1 / Plan 04.)_
- [x] **SRC-06**: Output: a structured `pageStructure.json` that says, for each Page entity FQCN: `{ tableName, contexts: [{ name, allowedPagePartClasses: [{class, table}] }], templates: [...], possibleChildTypes: [...], discriminatorMap, sourcePath }`. Canonical input for Phase 3's Matrix-field construction; written via `MappingFile::writeAtomicJson` per Phase 2 / D-07. _(Phase 02.1 / Plan 04.)_
- [x] **SRC-07**: Single-table-inheritance discriminators (`#[ORM\InheritanceType('SINGLE_TABLE')]` + `#[ORM\DiscriminatorColumn]` + `#[ORM\DiscriminatorMap]`) extracted; each `*_pageparts` row's class can be resolved by discriminator value. _(Phase 02.1 / Plan 04.)_
- [x] **SRC-08**: `HeuristicProposer` gains an entity-aware heuristic (slot 1.5 per D-44, between zero-fill and exact-name): column → entity property → Craft field handle, scoped by entity-class context (higher confidence than name matching alone). Inherited columns (from `AbstractArticlePage` etc.) recognized and grouped. heuristic-high → status accepted (Phase 2 / D-02 ladder). _(Phase 02.1 / Plan 07.)_
- [x] **SRC-09**: `LlmClassifier::batchPropose` prompts include the parent-entity class signature + property docblock + (for page-parts) the contexts the parent page uses, for each residual chunk. KnowledgeBase markdown replaces the prior `'', ''` placeholders via `AnalyzeController::buildKbMappingAdapter()` (B2 fix — v1-shaped adapter over v2's flat proposals[]). 8000-char truncation contract preserved at the LlmClassifier boundary. _(Phase 02.1 / Plan 07.)_
- [x] **SRC-10**: Doctor gains a 5th check: "Kunstmaan source path" — verifies the path exists, contains `src/Entity/`, and (when present) contains `config/kunstmaancms/pageparts/`. FAILs (per D-31) when the path is unset or invalid; WARNs when YAML config is absent (PHP-only scan fallback). `KunstmaanSourcePathResolver` Yii Component (D-33) owns the `??=` ladder + path validation + realpath resolution. _(Phase 02.1 / Plan 01.)_
- [x] **SRC-11**: `DoctrineEntityParser` ported verbatim from v1 (`~/Sites/craft-kunstmaan-migrator/src/kunstmaan/schema/DoctrineEntityParser.php`, 366 LOC) with the use-map FQCN resolver intact (`Foo::class` / `\Full\Ns\Foo::class` / `'string'` / docblock fallback chain). `getByFqcn()` / `getByTable()` / `getAll()` API preserved. T-02.1-02-01 mitigation: regex-only parser, no `include`/`require` of source files. _(Phase 02.1 / Plan 02.)_
- [x] **SRC-12**: `DetailTableResolver` ported with the 4-tier resolution priority (operator override → `DoctrineEntityParser` source truth → information_schema prefix scan → all-tables suffix scan). Bound-parameter information_schema queries preserved per T-05.5-03-01 mitigation. v2 default `legacyTablePrefix = 'lameco_websitebundle_'` (CQM-rehearsal Lameco-bundle convention). _(Phase 02.1 / Plan 02.)_
- [x] **SRC-13**: `KnowledgeBase` Markdown generator ported (partial — `renderPagesMarkdown()` + `renderPagePartsMarkdown()` + every transitively-reachable private helper per Plan 03's B3 anti-pattern guard). Plan 07 wires it into `LlmClassifier::batchPropose` via the SRC-09 v1-shaped adapter as a downstream consumer. _(Phase 02.1 / Plan 03.)_
- [x] **SRC-14**: `BodyScanColumnFinder` + `MediaFkScanner` + `TopologicalOrderer` ported as part of the scanner output. Plan 05's `KunstmaanSourceScanner` composes them as a downstream consumer; Phase 3 consumes them in turn — Phase 02.1 owns the discovery surface. _(Phase 02.1 / Plan 03.)_
- [x] **SRC-15**: `BlockAvailabilityValidator` ported and wired into `MappingAuditor` as a fourth finding kind (`block-availability`) per D-36. Coverage gate (Phase 2 / D-14) extends: every page-part class declared in pageStructure must have a `(kind=pagePart)` row with `status ∈ {accepted, dropped}` in mapping.yaml — unmapped page-part classes block `migrate --live`. _(Phase 02.1 / Plan 08.)_
- [x] **SRC-16**: CoverageAuditor reconciliation against v1's `bridge/schema/knowledgebase/CoverageAuditor.php` (463 LOC) per D-37/D-38/D-39. Rule-by-rule disposition table in RECONCILIATION.md; accidentally-dropped rules patched into v2 with traceability comments. _(Phase 02.1 / Plan 09.)_
- [x] **SRC-17**: MappingValidator reconciliation against v1's `bridge/mapping/MappingValidator.php` (647 LOC) per D-37/D-38/D-39. Rule-by-rule disposition table in RECONCILIATION.md; accidentally-dropped rules patched into v2 (drop-rationale-length finding kind) with traceability comments. _(Phase 02.1 / Plan 09.)_
- [x] **SRC-18**: `mapping.yaml` row-kind extension. The `MappingFile` schema accepts a `kind:` discriminator on each row in `proposals[]`; absent `kind:` defaults to `column` for backward compatibility with Phase 2 mapping.yaml files. Page-part rows (`kind: pagePart`) carry the D-34 reference shape (`pagePartClass, sourceTable, parentPageClass, context, targetEntryType, targetMatrixField, targetBlockType, fields[], status=needs-review` per D-35) and key on a STRUCTURAL-ONLY identity tuple `(pagePart, pagePartClass, parentPageClass, context)` in `merge()` — `targetEntryType` is NOT part of the key (prevents idempotent re-run row duplication per W1 fix). _(Phase 02.1 / Plan 06.)_
- [x] **SRC-19**: `MapController` two-mode walker. The interactive rubber-stamp loop dispatches on row kind first, then keypress: column rows render via the existing `renderRowBlock` + `runRemapPicker` (Phase 2 / D-05 / D-06); page-part rows render via a new `renderPagePartRowBlock` + `runRemapPickerForPagePart` block-type picker that lists available Matrix block-types on the parent entry type's targetMatrixField. Phase 2 atomic per-keypress writes (D-07) and walk-order locking (D-08) preserved across both modes. _(Phase 02.1 / Plan 06.)_
- [x] **SRC-20**: `DoctrineEntityParser` parses PHP 8 attributes only. Annotation parsing (`@ORM\Table`, `@ORM\Column`, etc.) is removed (not deprecated). Pre-flight grep confirms cqm, simac, and enreach are all PHP 8+ attribute-only before this lands. Halves the parser's regex/state-machine surface and removes a long tail of subtle quirks. Phase 4.1 driver. _(Phase 4.1 / Plan 04.1-06 — annotation paths and tests removed from `DoctrineEntityParser`; cqm/simac/enreach pre-flight grep returned zero `@ORM\\` hits before the removal landed.)_

### ETL pipeline (ETL)

- [x] **ETL-01**: `kunstmaan-migrator/migrate` runs extract → transform → load → finalize in sequence. Top-level command is dry-run by default; `--live` writes to Craft. _(Phase 3 / Plan 13 — MigrateController::actionIndex orchestrates extract → transform → load → finalize sequence; --live writes, default dry-run.)_
- [x] **ETL-02**: Per-stage commands exist for resume (`migrate/extract`, `migrate/transform`, `migrate/load`, `migrate/finalize`). _(Phase 3 / Plan 13 — actionExtract / actionTransform / actionLoad / actionFinalize / actionTruncate sub-actions per CONTEXT D-48 in-process pipeline; standalone actionLoad re-runs extract+transform internally.)_
- [x] **ETL-03**: Topological ordering of entity migrations so foreign-key references resolve before they're written. _(Phase 3 / Plan 04 — ExtractService consumes Phase 02.1 / TopologicalOrderer for kuma_nodes parent-first hierarchical insertion; cycle-detection inherited.)_
- [x] **ETL-04**: Per-entry atomic load (always-on, no flag). On failure, the entry is rolled back; the next entry continues. _(Phase 3 / Plan 12 — AtomicMigrationService Phase B Craft::$app->db->transaction wrapping saveEntryForSites + state-row record; atomic-always-on, no flag, per CONTEXT decision.)_
- [x] **ETL-05**: Idempotent re-runs: a row already migrated (tracked via `kunstmaanmigrator_state.legacy_id`) is skipped or refreshed, never duplicated. _(Phase 3 / Plans 03 + 12 — MigrationStateService CRUD over kunstmaanmigrator_state + AtomicMigrationService idempotency gate (existingId !== null && !$overwrite → skip).)_
- [x] **ETL-06**: Per-entry console progress (`[N/total] slug → created|updated|skipped`). _(Phase 3 / Plan 13 — MigrateController per-entry progress emission [N/total] slug → created|updated|skipped|FAILED:reason; FAILED to stderr, others to stdout per Shared Pattern 4.)_
- [x] **ETL-07**: `migrate/truncate` resets state for a fresh rehearsal without rolling the whole DB. _(Phase 3 / Plan 13 — actionTruncate per CONTEXT D-51 wide+safety-rails: defaults to --dry-run, requires --live --confirm, honors --entities + --locales filters.)_

### Field handlers (FH)

- [x] **FH-01**: Pluggable `FieldHandler` interface with built-in handlers: PlainText, Asset, Matrix, Relation, SplitName (Dutch composite-name splitter), CKEditor. _(Phase 3 / Plans 01 + 08 + 09 + 10 — FieldHandler interface (verbatim port) + 5 handlers PlainText/Asset/Matrix/Relation/SplitName. CKEditor body-token rewrite is FH-04 inline path on PlainTextHandler 'ckeditor' mode.)_
- [x] **FH-02**: Handlers are registered in a `FieldHandlerRegistry` wired from the plugin bootstrap, allowing project-level overrides. _(Phase 3 / Plans 01 + 13 — FieldHandlerRegistry hash-keyed + Plugin::init() registers 4 PlainText modes via $registry->register(new PlainTextHandler('plain'/'ckeditor'/'link'/'dropdown')) + 4 other handlers.)_
- [x] **FH-03**: Asset handler supports JIT (default — page-driven; only assets referenced from migrated entries are pulled in) and `--preload-assets` (opt-in batch ingest of every referenced asset before the entries loop, useful for parallelisation). Neither mode migrates orphan assets — that's `NEXT-05`. _(Phase 3 / Plans 05 + 13 — AssetMigrationService::resolveFromLegacyId JIT default + ingestReferenced opt-in; MigrateController --preload-assets flag exposes the batch path. Orphan media deliberately not migrated (NEXT-05 deferred).)_
- [x] **FH-04**: CKEditor handler emits `[NT<id>]` / `[M<id>]` deferred tokens for cross-entry / media references that don't yet exist at load time. _(Phase 3 / Plans 01 + 06 + 08 + 12 — DeferredAssetToken::emit('asset:N') + CkeditorRewriterService [M<id>]/[NT<id>] regex constants + AssetHandler dual-token emission ([M<id>] for imgTag, asset:N for relation) + AtomicMigrationService /^asset:\d+$/ resolver pair.)_

### Finalize + CKEditor rewriting (FIN)

- [x] **FIN-01**: `migrate/finalize` walks every CKEditor field across every migrated entry and resolves `[NT<id>]` / `[M<id>]` tokens (plus URL-encoded `%5BNT<id>%5D` / `%5BM<id>%5D` forms). _(Phase 3 / Plan 06 — CkeditorRewriterService 6-step rewrite() pipeline preserved verbatim including KUMA_MEDIA_PLACEHOLDER_REGEX + KUMA_NT_PLACEHOLDER_REGEX (URL-encoded %5B/%5D variants with case-insensitive flag). FinalizeWalker greenfield walks Entry::find()->siteId('*'); MigrateController::actionFinalize entry point.)_
- [x] **FIN-02**: Strict policy on unresolvable tokens — left verbatim, annotated with `<!-- MIGRATION:UNRESOLVED ... -->`. No silent drops. _(Phase 3 / Plan 06 — `<!-- MIGRATION:UNRESOLVED source=... -->` HTML comment annotation policy preserved byte-for-byte from v1; no silent drops; FinalizeWalker counts unresolvable fields for REPORT.md.)_

### Optional adapters (ADP)

- [x] **ADP-01**: SEOmatic adapter — detected at runtime via `Craft::$app->plugins->getPlugin('seomatic')`. If present, migrate per-locale SEOmatic MetaBundles with default-locale fallback. If absent, skip with WARN. Plugin installs cleanly on hosts without SEOmatic. — Phase 4 closed via Plans 04-02 (SeomaticPayloadBuilder verbatim port, 165 LOC), 04-06 (SeoMigrationService verbatim port, 600 LOC), 04-09 (Plugin::config + Plugin::init 4-slot DI wiring + D-57 Settings::$seoTableName override), 04-10 (`migrate/seo` operator sub-action + actionIndex bolt-on per D-55), 04-11 (doctor 7th check INFO `<plugin> not installed (adapter will skip)` per D-69), 04-12 (SeomaticPayloadBuilderTest covers the locked 6-key column→payload contract via setResolver test seam; ComposerSuggestTest guards ADP-03 invariant).
- [x] **ADP-02**: Retour adapter — same shape. Migrate redirects from `kuma_redirects` if Retour is installed; skip with WARN otherwise. — Phase 4 closed via Plans 04-07 (RedirectMigrationService verbatim port, 692 LOC, with D-56 double gate, hardcoded `'default'`/`'en'` site-handle removal per PATTERNS flag #4, idempotent saveRedirect upsert), 04-09 (Plugin::config + Plugin::init 3-slot DI wiring + D-57 Settings::$redirectsTableName override), 04-10 (`migrate/retour` operator sub-action + actionIndex bolt-on per D-55), 04-11 (doctor 7th check INFO `retour not installed (adapter will skip)` per D-69), 04-12 (Retour-side coverage rolls up via the same pure-function tests + ADP-03 ComposerSuggestTest guard).
- [x] **ADP-03**: Composer requirements list SEOmatic and Retour as `suggest`, not `require`. — Shipped at Phase 1 manifest level (composer.json `suggest` block declares `nystudio107/craft-seomatic` and `nystudio107/craft-retour`). Phase 4 / Plan 12 (2026-04-26) added `tests/ComposerSuggestTest.php` as a programmatic invariant guard so a future maintainer can't silently flip either to a hard requirement; also exposed via doctor 7th check INFO row (Plan 04-11) so operators see the runtime-detection contract working at install time.
- [x] **ADP-04**: `Settings::seoEnabled` + `Settings::retourEnabled` booleans (default `true` to preserve current behavior) gate the SEOmatic + Retour adapters even when the respective plugin IS installed. CLI flags `--no-seo` / `--no-retour` give the same effect per-run without persisting to settings. Closes the operator-feedback gap from Phase 4 UAT — today only plugin presence/absence governs adapter execution. Phase 4.1 driver. _(Phase 4.1 / Plan 04.1-05 — `Settings::seoEnabled` + `retourEnabled` defaults `true`; `SeoMigrationService` + `RedirectMigrationService` gain a Settings-disabled gate prepended before the existing plugin-absent gate (D-25); `MigrateController --no-seo` / `--no-retour` flags emit a distinct warn-line copy per D-27 so REPORT.md skipped-stages can pattern-match the four disable paths.)_

### Verify (VER)

- [x] **VER-01**: `kunstmaan-migrator/verify capture-baseline` snapshots pre-migration counts (entries, assets, taxonomies, redirects, SEOmatic bundles) into a JSON artifact. — Phase 4 / Plan 09 (2026-04-26): `VerifyController::actionCaptureBaseline` wires `BaselineCounterService::capture()` (Plan 04-04 D-59 light-counts shape `format: counts-v1`) → `MappingFile::writeAtomicJson` → stable `storage/migration/baseline.json` (NOT timestamped per D-59). SHA-heavy snapshot path remains deferred to a future `verify capture-baseline --deep` flag.
- [x] **VER-02**: Optional `verify capture-baseline-html` snapshots rendered HTML for a configurable URL set. — Phase 4 / Plan 09 (2026-04-26): `VerifyController::actionCaptureBaselineHtml` wires `CaptureBaselineHtmlService::capture()` (Plan 04-08) via the `$fetcher` DI seam wired in `Plugin::init()` (Plan 04-09 Task 01). Reads operator-curated `storage/migration/spot-check-urls.txt`, writes `<baselineDir>/<slug>.html` files; partial-success contract (one bad URL doesn't kill the run) preserved.
- [x] **VER-03**: `verify` runs the parity gate — counts diff vs baseline plus optional URL spot-check — and writes a `VERIFY-<timestamp>.md` report under `storage/migration/` regardless of outcome. — Phase 4 / Plan 09 (2026-04-26): `VerifyController::actionIndex` runs Gate 1 (`CountGateService::run` count-match per Plan 04-04) + Gate 2 (`SpotCheckUrlFetcher::diff` per Plan 04-03 against captured baseline HTML) + writes markdown report via `MappingFile::writeAtomic` (D-61 / Phase 2 / D-07 atomic seam) regardless of pass/fail. Tolerance ladder per D-60: `$countTolerance ?? Settings::$verifyCountTolerance ?? 0.01`. D-58 missing-baseline semantic: WARN + flip overall pass to false. Optional-plugin SKIP rows (seomatic / retour) synthesized at controller seam when CountGateService omits the gate. `baselineToExpectedCounts()` translator helper bridges BaselineCounterService 'counts-v1' shape ↔ CountGateService expectedCounts shape.
- [x] **VER-04**: `VerifyController` CLI filter options (`--entity=`, `--locale=`, `--since=`) flow into `CountGateService::run()` and `BaselineCounterService::capture()` so per-run scope narrowing actually narrows. Closes the intelephense `$filters unused` finding from Phase 4 (VerifyController declares the options at `actionIndex:109`, `actionCaptureBaseline:292`, `actionCaptureBaselineHtml:323` but never passes them through). Phase 4.1 driver. _(Phase 4.1 / Plan 04.1-05 — `CountGateService::run()` and `BaselineCounterService::capture()` accept `?MigrationFilters $filters = null`; `VerifyController` passes `$filters` through; baseline JSON gains a `filterScope` header per D-29; intelephense warning closed.)_

### Settings + observability (CFG)

- [x] **CFG-01**: CP Settings page (Settings → Plugins → Kunstmaan Migrator): Anthropic key (secret), LLM model, LLM timeout, default filters, `dryRunDefault` toggle. — Phase 4 / Plan 05 (2026-04-26): grouped-section form (Connectivity / AI / Defaults / Verify / Adapters), all 23 Settings properties rendered, masked password + env hints (D-64), editable tables for arrays (D-63).
- [x] **CFG-02**: Console verbosity flag (`-v` through `-vvv`) toggles per-stage timing + asset RCA logging into `storage/migration/*.log`. — Phase 4 / Plan 10 (2026-04-26): D-65 `-v..-vvv` ladder via `string|int $verbose` MigrateController property + private `verbosityLevel()` char-counter helper handling Yii's option-parser idiom (`-v`/`-vv`/`-vvv` lowered to string `'v'`/`'vv'`/`'vvv'`); D-67 per-run timestamped log file at `storage/migration/migrate-<gmdate('Y-m-d--H-i-s')>.log` opened idempotently via private `openLogFile()` at every action entry (actionIndex/actionSeo/actionRetour); private `logLine()` helper appends ISO-8601-prefixed lines when verbosity meets supplied minimum level; D-66 asset RCA emission to dedicated `kunstmaanmigrator.rca` Craft::info channel via private `classifyAssetFailureReason()` closed-set classifier (filesystem_404 / mime_mismatch / too_large / deferred_unresolved). Phase 4 / Plan 12 (2026-04-26): tests/load/AssetMigrationServiceRcaTest.php characterization-tests the classifier via Reflection — all 4 reasons + the catch-all fallback covered against the source's exact wording matchers (`'No such file' / 'not found' / 'Copy failed' / 'mime' / 'content_type' / 'allowedFileExtensions' / 'too large' / 'PostMaxSize'`).
- [x] **CFG-03**: A "rehearsal report" produced at the end of every `migrate --live` run summarising what was migrated, what was dropped, and why — distinct from `verify`'s parity gate. — Phase 4 / Plan 10 (2026-04-26): D-68 REPORT.md three new sections in PATTERNS.md-locked order (`## Rehearsal summary` with totals + wall-clock duration via private `formatWallClock(hh:mm:ss)` + filter scope `entities/locales/since` + `--live`/`--dry-run` flag + log file path; `## Skipped stages` filtered from `$report->warnings` for SEOmatic/Retour absence patterns and omitted entirely when no stages skipped; `## Asset RCA` markdown table from `$report->assetRcaRows` snapshotted from `$plugin->assetMigrationService->assetRcaRows` at writeReport entry, omitted entirely when no rows); `MigrationReport::pushAssetRca()` + `public array $assetRcaRows` cross-cutting added; writeReport signature extended `(string $storageDir, MigrationReport $report, ?MigrationFilters $filters = null, ?float $tRunStart = null)` so the rehearsal summary renders filter scope + wall-clock duration without re-introspecting controller state.
- [x] **CFG-04**: When `Settings::legacyDb*` are blank, plugin auto-fills from `DATABASE_URL` parsed from `KUNSTMAAN_SOURCE_PATH/.env.example` then `.env` (operator-supplied Settings always override env-derived defaults). Phase 4.1 driver: drift between Symfony `.env` and Craft Settings is a real failure mode — saw it in Phase 2 UAT; operator currently configures the same DSN twice. _(Phase 4.1 / Plans 04.1-01 + 04.1-02 — `KunstmaanEnvReader` Yii Component reads `.env`/`.env.example` and parses `DATABASE_URL`; `Settings::beforeValidate()` auto-fills blank `legacyDb*` from the parsed values; operator-supplied Settings always win.)_
- [x] **CFG-05**: CP Settings page slimmed to ~9 operator-touch-essential fields across two H2 groups (Connectivity + Mapping); 14 advanced fields move to `config/kunstmaan-migrator.example.php` documentation. Craft's auto-loaded `config/{handle}.php` mechanism (already supported, no new code) handles runtime overrides. Phase 4.1 driver: G-02 from Phase 4 UAT — current 23-field page is too long for daily operator use; SEOmatic/Retour/FeedMe all keep CP minimal and route advanced config through file overrides. _(Phase 4.1 / Plan 04.1-04 — `_settings.twig` slimmed to 9 fields across Connectivity + Mapping H2 groups; greenfield `config/kunstmaan-migrator.example.php` documents the 14 advanced fields with multi-env shape per D-17.)_
- [x] **CFG-06**: Surviving CP editable-table columns become Craft select-dropdowns where source data is available: Craft site handles (always), legacy locale codes (when DB reachable), Kunstmaan entity basenames (when `KUNSTMAAN_SOURCE_PATH` set + scanner can run). Free-text fallback when source data isn't available. Phase 4.1 driver: G-04 from Phase 4 UAT — typo class is silently breaking; constrained dropdowns prevent it. _(Phase 4.1 / Plan 04.1-04 — `localeMap` editable-table uses dropdowns: Craft site handles always; legacy locale codes from `LocalePreflight::detect` when DB reachable; free-text fallback otherwise.)_
- [x] **CFG-07**: REPORT.md `## Skipped stages` and `## Asset RCA` sections always emit, even when empty (placeholder copy). Constant shape makes "did this code path run?" trivially answerable. Phase 4.1 driver: G-05 from Phase 4 UAT — current "omit when empty" guard hides whether code paths ran on 0/0/0/0 dry-runs. _(Phase 4.1 / Plan 04.1-05 — `MigrateController::renderSkippedStagesSection` and `renderAssetRcaSection` emit the heading + placeholder copy when empty; characterization tests in `MigrateControllerReportEmptyStateTest` lock the contract per D-41/D-42/D-43.)_

### Recovery commands (REC)

- [x] **REC-01**: `migrate sync-assets` ingests every `kuma_media` row that any prior atomic run referenced but skipped (filesystem_404 / mime_mismatch / too_large / etc.). Idempotent: re-running with everything healed exits cleanly. Permanently-failed assets get a terminal-state marker in `kunstmaanmigrator_state` to prevent retry loops. Closes the per-entry JIT default's information loss — today operators have no recovery path short of re-running the whole migration. Phase 4.1 driver. _(Phase 4.1 / Plan 04.1-07 — `MigrateController::actionSyncAssets` re-ingests state-rows under `source='media'` whose `targetId` is unset; idempotent (re-runs with everything healed exit with `candidates=0`); terminal-state via `meta.terminalState='permanently_failed'` written by `MigrationStateService::markTerminal` (no schema migration per D-37 / PATTERNS recommendation b); SYNC-ASSETS-{ts}.md emitted via `MappingFile::writeAtomic` per D-39.)_
- [ ] **REC-02**: `migrate sync-relations` resolves any `relation:deferred` rows in `kunstmaanmigrator_state` that couldn't be resolved during their owning entry's atomic save (typical: forward-references, taxonomies migrated after the entry that references them). Same idempotence + terminal-state contract as REC-01. Phase 4.1 driver. _(**Deferred to Phase 4.2** — Plan 04.1-07 Task 1 discovery resolved Finding B: no existing `relation:deferred` marker in `kunstmaanmigrator_state`. Deferred references live as in-memory `asset:N` / `[NT<id>]` tokens resolved at finalize time, with no state-row to query. Implementing the marker requires extending `RelationHandler` / `AtomicMigrationService` state-write surface, which is out of scope for Phase 4.1 per the plan's hard-scope restriction. See `04.1-CONTEXT.md` `## Deferred Ideas`.)_

### Tests (TST)

- [ ] **TST-01**: PHPUnit suite runs `tests/unit` and `tests/integration`. Unit suite covers `MigrationFilters`, `MappingLoader`, every field handler, `CkeditorRewriter`, and the heuristic proposer.
- [ ] **TST-02**: Characterization fixtures captured from a real Kunstmaan dump exercise the Transform stage end-to-end (golden-file diffs).
- [ ] **TST-03**: CI workflow runs `composer validate --strict`, PHPUnit, and a smoke test that the plugin loads in a scratch Craft install.
- [ ] **TST-04**: `kunstmaan-migrator/doctor` and the rehearsal smoke check are part of the release checklist before any tag.

## v2 Requirements (deferred)

- [ ] **NEXT-01**: Writer seam — abstract the Craft-write side behind a `Writer` interface so a future `ProjectConfigWriter` can scaffold sections / fields / entry types from a Kunstmaan dump + `~/Sites/craft-starter-kit` instead of writing into pre-existing schema.
- [ ] **NEXT-02**: Read-only CP status mirror — a small CP page that surfaces last run, mapping coverage, and counts. Not a runner.
- [ ] **NEXT-03**: Multi-provider AI — abstract the proposer behind an interface so OpenAI / local models can plug in.
- [ ] **NEXT-04**: Cross-client rehearsal matrix (Simac, Enreach, Joulz) — exercising the migrator against more than the CQM corpus.
- [ ] **NEXT-05**: Orphan-media sync pass — optional `migrate/sync-orphan-assets` command that walks the legacy media table and migrates any asset not yet present in Craft, for stakeholders who want every legacy asset preserved regardless of reference. Strictly opt-in; the page-driven default stays.

## Out of Scope (v1)

- **Building Craft from scratch with the starter-kit** — Future milestone (NEXT-01).
- **CP "Migration Pipeline" runner utility** — Dropped from v1; CLI is canonical. Re-evaluate when there's a real operator complaint.
- **Inline mapping authoring in the CP** — Dropped. CLI rubber-stamp loop is the only authoring surface.
- **`.claude/skills/` skill bundle** — Dropped. Rubber-stamp loop is just a CLI command.
- **Three-tier `kunstmaan/` / `craft/` / `bridge/` source layout + Deptrac** — Replaced with a flatter, vertical-slice structure. Mechanism without proportionate benefit at this size.
- **Multiple mapping files** — Single `mapping.yaml` with per-row `status:` replaces `mapping.yaml.draft` and `mapping-drops-{ts}.yaml`.
- **Multi-provider AI** — Anthropic only in v1. Re-evaluate when a real driver appears (NEXT-03).
- **Production-environment migration** — `NeverProductionTrait` hard-blocks. Plugin is dev/staging only by design.
- **Atomic flag** — Atomic-always-on. v1.x's `--atomic` was the right thing to remove; we don't reintroduce it.

## Traceability

Filled in by the roadmap step. Every requirement above must map to exactly one phase.

| REQ | Phase |
|-----|-------|
| FND-01 … FND-05, FND-02a | 1 |
| CONN-01 … CONN-03 | 1 |
| MAP-01 … MAP-07 | 2 |
| FILT-01 … FILT-03 | 2 |
| LOC-01, LOC-02 | 2 |
| SRC-01 … SRC-19 | 02.1 |
| ETL-01 … ETL-07 | 3 |
| FH-01 … FH-04 | 3 |
| FIN-01, FIN-02 | 3 |
| ADP-01 … ADP-03 | 4 |
| VER-01 … VER-03 | 4 |
| CFG-01 … CFG-03 | 4 |
| CFG-04, CFG-05, CFG-06, CFG-07, LOC-03, ADP-04, VER-04, SRC-20, REC-01 | 4.1 |
| REC-02 | 4.2 (deferred from 4.1 — see RECONCILIATION.md) |
| TST-01 … TST-04 | 5 |
